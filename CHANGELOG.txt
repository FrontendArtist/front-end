ط
[2026-08-05] - Unified Shared Chat Modal Component & Admin Contact Messages Redesign

── Unified Shared Chat Modal Architecture ──────────────────────────────────────────────────
- Created reusable `ChatModal` component (`src/components/common/ChatModal/ChatModal.jsx` & `ChatModal.module.scss`):
  - Built a glassmorphism SPA chat view overlay matching user profile chat design system.
  - Supports both User and Admin modes (`isAdmin` prop).
  - Features sender info header bar (Name, Email/Phone), Shamsi date tags, status badges, status selector dropdown, and delete button.
  - Formats message stream with distinct user (right) and staff/admin (left) bubbles, auto-scroll, enter key submit, and loading states.

── Admin Contact Messages Redesign ──────────────────────────────────────────────────────────
- Refactored `MessageReadModal` (`src/components/admin/Messages/MessageReadModal.jsx`):
  - Updated admin contact messages view to render `ChatModal` with `isAdmin={true}`.
  - Fully integrated reply posting, status updates, and message deletion.

── User Profile Chat Refactoring ────────────────────────────────────────────────────────────
- Refactored `MessageDetailModal` (`src/modules/profile/MessageDetailModal.jsx`):
  - Updated user profile chat to delegate UI rendering to `ChatModal`, retaining user API handlers and 5-second polling.

##latest- 2026-08-05

### Feature & UI: Unified Shared Chat Modal & Admin Contact Messages Redesign
**Status**: ✅ COMPLETED (100%)

#### Overview:
Unified the chat modal interface across user profile messages (`profile/messages`) and admin contact messages (`admin/contact-messages`) into a single reusable `ChatModal` component with glassmorphism styling, responsive layouts, and full admin/user feature support.

#### Changes Made:
1. **Shared Chat Component** (`ChatModal.jsx` & `ChatModal.module.scss`):
   - Created standalone reusable chat modal overlay under `@/components/common/ChatModal/`.
   - Supports user and admin modes with status management, sender metadata, keyboard shortcuts, and auto-scroll.
2. **Admin Contact Messages** (`MessageReadModal.jsx`):
   - Redesigned admin read modal to use `ChatModal`.
3. **User Profile Messages** (`MessageDetailModal.jsx`):
   - Refactored user chat modal to use `ChatModal`.

---

[2026-08-05] - Strapi v5 Document Service Draft & Publish Fix for Courses, Articles & Products

── Strapi v5 Backend Document Service Controllers ────────────────────────────────────
- Updated Strapi v5 Course controller (`backend/src/api/course/controllers/course.js`):
  - Overrode `update` method to support Strapi v5 Document Service `publish` and `unpublish` functions.
  - When `publishedAt: null` is sent in `update` payload, triggers `strapi.documents('api::course.course').unpublish({ documentId })` to remove the published entry from the database.
  - When `publishedAt` date string is sent, triggers `strapi.documents('api::course.course').publish({ documentId })`.
- Verified identical Document Service publish/unpublish controller logic in `article.js` and `product.js`.

── Public API Querying & Filtering ──────────────────────────────────────────────────
- Added explicit `'status': 'published'` parameter to all public fetchers in `src/lib/coursesApi.js`, `src/lib/articlesApi.js`, and `src/lib/productsApi.js`:
  - Enforces that draft/unpublished courses, articles, and products are strictly excluded from public site listings, sliders, and page routes (`/courses`, `/articles`, `/products`, `/`).

##latest- 2026-08-05

### Fix & Architecture: Strapi v5 Draft & Publish Document Service Unpublish Fix
**Status**: ✅ COMPLETED (100%)

#### Overview:
Fixed an issue where unpublishing courses in the admin panel did not unpublish the active course entry in Strapi v5. Added Strapi v5 Document Service controller overrides and enforced `status=published` query parameters across public API fetchers.

#### Changes Made:
1. **Backend Course Controller** (`backend/src/api/course/controllers/course.js`):
   - Added Document Service `unpublish` and `publish` lifecycle hooks upon receiving `publishedAt` changes.
2. **Frontend API Layers** (`src/lib/coursesApi.js`, `src/lib/articlesApi.js`, `src/lib/productsApi.js`):
   - Configured `status: 'published'` on all public query endpoints to prevent draft items from displaying on public routes.

---

[2026-08-04] - Course Teaser Video Player, Title Enrolled Badge & Curriculum Accordion Redesign

── Course Teaser Video Player & Dynamic Media ───────────────────────────────────────
- Integrated `teaserUrl` field in Strapi course formatting (`src/lib/strapiUtils.js`) and course page (`src/app/courses/[slug]/page.js`).
- Dynamic media switcher: renders HTML5 `<video>` player with poster fallback if `teaserUrl` is present; falls back to Next.js `<Image>` banner if empty.
- Added top-right floating badge overlay: `🏆 تیزر معرفی دوره` with backdrop blur, dark overlay, and responsive positioning (`page.module.scss`).

── Course Details Header & Enrolled Status Badge ─────────────────────────────────────
- Refactored single course page hero details section (`src/app/courses/[slug]/page.js` & `.module.scss`):
  - Created `.titleWrapper` flex container placing course title on right and enrollment status on left.
  - Styled "شما دانشجوی این دوره هستید" indicator into a modern green status badge (`rgba(26, 153, 91, 0.1)` bg, border, and checkmark icon).

── Curriculum Accordion & Individual Chapter Sales Redesign ─────────────────────────
- Completely overhauled `CourseContentManager.jsx` & `CourseContentManager.module.scss`:
  - **Removed Dead Space**: Eliminated top empty player placeholder cards. Media player section only renders when an active lesson is selected.
  - **Clean Accordion Rows**: Re-architected chapter row header to display Chapter Number badge + Title on right, and Sessions count + Duration + Lock/Unlock Lucide SVG icons on left. Removed cluttering buy buttons from main header rows.
  - **In-Content Buy Chapter CTA**: Added stylish in-content call-to-action banner (`lockedBanner`) inside expanded locked chapters with gold accent border and "تهیه فصل" purchase button.
  - **Muted Locked Content**: Styled lesson lists within locked chapters in a disabled/muted state (`grayscale` filter, `opacity: 0.5`, `pointer-events: none`).
  - **Luxurious Dark/Gold SCSS Styling**: Replaced heavy boxed cards with subtle `border-bottom` row dividers, responsive mobile metadata rules, and customized Lucide SVG icons.

##latest- 2026-08-04

### UI & UX: Modernize Add To Cart Buttons System
**Status**: ✅ COMPLETED (100%)

#### Overview:
Revamped the entire "Add to Cart" button architecture across the platform (Course Pages, Product Pages, and Product Cards) to feature a highly premium, modern aesthetic with glassmorphism effects, SVG icons, and smooth micro-animations. Additionally, fixed a missing call-to-action on non-chaptered course pages.

#### Changes Made:
1. **Course Page Missing Cart Action** (`src/app/courses/[slug]/page.js`):
   - Added conditional logic to render the `AddToCartButton` for non-chaptered courses that have a price and haven't been purchased yet.
   - Designed a new `.actionWrapper` layout to position the button perfectly below the price.

2. **Premium AddToCartButton Component** (`AddToCartButton.jsx` & `.module.scss`):
   - Migrated from generic global classes to a dedicated SCSS module.
   - Implemented a vibrant gold-to-orange gradient background (`linear-gradient(135deg)`).
   - Added deep 3D box-shadows, scale-on-hover animations, and an animated glassmorphic shine effect (`&::before` pseudo-element).
   - Integrated dynamic inline SVG icons (Cart icon for default, Checkmark for purchased state).

3. **ProductAddToCart & ProductCard Standardization** (`ProductAddToCart.jsx`, `ProductCard.jsx`, & respective SCSS):
   - Applied the same modern premium button styles to product additions.
   - Upgraded the quantity controller (`+`, `-`, count) with elegant transparent backgrounds, hover scales, and bordered glass aesthetics.
   - Inserted specific SVG icons (Plus, Minus, Trash, Cart) for a much richer user experience.

---

### Feature: Add Rich Text Content Field to Course Page
**Status**: ✅ COMPLETED (100%)

#### Overview:
Added a new `content` field to the Course model to support rendering deep Markdown-based descriptions on the Course Details page above the curriculum.

#### Changes Made:
1. **Backend Schema Update** (`schema.json`):
   - Added `content` field of type `richtext` to the Course content type in Strapi.
2. **API Data Extraction** (`strapiUtils.js`):
   - Updated `formatStrapiCourses` to parse and extract the `content` field.
3. **Frontend Implementation** (`page.js` & `page.module.scss`):
   - Integrated the existing `ArticleReader` component into the Course Details page.
   - Parsed Markdown dynamically on the server-side using `marked.parse()`.
   - Added dedicated `.contentSection` container with premium bordered styling.

[2026-08-01] - Category Filter Responsiveness, Unified Mobile Layout, Articles Swiper Slider & Dynamic Breadcrumbs

── Products Category Filter Responsiveness & Unified Layout ────────────────────────
- Refactored `CategoryFilter` (`src/modules/products/ProductsPageClient/components/CategoryFilter.jsx` & `.module.scss`):
  - Passed dynamic responsive `breakpoints` to `BaseSlider` (scaling from 6 cards on desktop to 2-3 cards on mobile).
  - Merged active category box and subcategory list into a single unified Glassmorphism card container (`selectedLayout`) on mobile/tablet to eliminate redundant visual borders and padding.
  - Adapted active category header (`selectedCategoryCard`) to a compact horizontal layout (row flex) on mobile, preserving screen real estate.
  - Unified desktop view into a single horizontal glassmorphic card with a subtle vertical divider line separating active category details from subcategories.
  - Applied flat glassmorphic background overlay (`fade(var(--color-bg-primary-overlay), 60%)`) on mobile.

── Articles Category Filter Alignment & Swiper Slider Integration ──────────────────
- Aligned `ArticleCategoryFilter` (`src/modules/articles/components/ArticleCategoryFilter.jsx` & `.module.scss`) to mirror Product `CategoryFilter`:
  - Replaced manual flex grid with `BaseSlider` (Swiper) for smooth sliding, touch gestures, mouse dragging (`cursor: grab`), and responsive breakpoints across all screen sizes.
  - Applied identical card styling, image aspect ratios, hover animations, and active state highlights (`categoryCardActive`).

── Dynamic Category Breadcrumbs ───────────────────────────────────────────────────
- Updated `src/app/articles/page.js` to dynamically append the active category item to the breadcrumb trail when `?category=slug` filter is selected: `خانه` > `مقالات` > `[نام دسته]`.
- Configured category item as active and `مقالات` item as clickable link back to main `/articles` route.

[2026-08-01] - Automatic Stock Deduction & Out-of-Stock Status Handling (Frontend & Backend)

── Backend Inventory & Order Lifecycles ─────────────────────────────────────────
- Added `stockDeducted` boolean attribute (`default: false`) to Strapi Order schema (`backend/src/api/order/content-types/order/schema.json`) to prevent duplicate inventory deductions on order updates.
- Added `processProductStock` lifecycle helper in `backend/src/api/order/content-types/order/lifecycles.js` executing on `afterCreate` and `afterUpdate`:
  - Automatically calculates `newStock = Math.max(0, currentStock - quantity)` for all physical product items in order.
  - Automatically toggles `isAvailable` status to `false` when `newStock` reaches 0 ("ناموجود").
  - Marks order as `stockDeducted: true` upon successful deduction.

── Frontend Cart & Stock Limit Enforcement ──────────────────────────────────────
- Enforced available inventory cap (`maxStock`) inside `addItem` and `updateQuantity` in Zustand Cart Store (`src/store/useCartStore.js`), preventing users from adding or incrementing item quantities beyond actual product stock.
- Updated `ProductAddToCart.jsx` and `ProductCard.jsx` components:
  - Added out-of-stock check (`isAvailable === false || stock <= 0`).
  - Rendered disabled **«ناموجود»** badge when product is unavailable or stock is 0.
  - Disabled `+` quantity controller button when user reaches available stock limit.
- Updated Cart page (`src/app/cart/page.jsx`) to disable `+` quantity button when cart quantity hits product stock limit.
- Integrated `revalidatePath` in Next.js orders API route (`src/app/api/orders/route.js`) to flush Next.js server page cache for product routes after successful order creation.

[2026-07-31] - Single Product Page Overhaul, SEO JSON-LD, Stock FOMO, Specs Table & Admin TinyMCE Integration

── Single Product Page Architecture & SEO ────────────────────────────────────────
- Overhauled `/products/[category]/[subcategory]/[slug]/page.js` with Google `Product` JSON-LD schema (offers, priceCurrency IRR, dynamic availability InStock/LimitedAvailability/OutOfStock).
- Used native HTML `<script type="application/ld+json">` tag to eliminate React 19 / Next.js script warnings in Server Components.
- Refactored page data loading using `Promise.all` for parallel fetching of product, comments, related products, category tree, and markdown parsing.
- Integrated `ArticleReader` with `safeParseMarkdown` (`marked` parser with try/catch fallback) for rich text content rendering.

── Hero Section & Stock FOMO Logic ───────────────────────────────────────────────
- Implemented Stock FOMO badge in `ProductDetails.jsx` with 3 dynamic states:
  - `stock > 10`: "موجود در انبار" (Green success badge)
  - `0 < stock <= 10`: "تنها X عدد در انبار باقیست!" (Warning badge with pulsing CSS red dot animation)
  - `stock === 0`: "ناموجود" (Gray disabled badge)
- Displayed `shortDescription` in styled hero banner with gold accent border.
- Formatted Persian currency numbers for main and discount prices.

── Product Specifications Component ─────────────────────────────────────────────
- Created `ProductSpecs.jsx` Server Component to render key-value array (`[{ key, value }]`) in a Zebra-Striped table.
- Styled with CSS custom properties from `variables.css`, gold title underlines, and mobile-friendly horizontal scrolling.
- Returns `null` when no specifications are provided to avoid empty section rendering.

── Related Products Slider Component ─────────────────────────────────────────────
- Created `RelatedProducts.jsx` Client Component using `BaseSlider` and `ProductCard`.
- Implemented `getRelatedProducts` in `productsApi.js` with slug-based exclusion of the current product (resolving Strapi v5 draft/publish ID filter anomalies).
- Added fallback algorithm to auto-fetch general catalog items when category siblings are fewer than requested limit.

── Backend Schema & API Layer Extensions ─────────────────────────────────────────
- Added `shortDescription` (text), `content` (text), and `specifications` (json) fields to Strapi Product schema (`backend/src/api/product/content-types/product/schema.json`).
- Updated `formatStrapiProducts` in `strapiUtils.js` and `getProductBySlug` in `productsApi.js` to return all new attributes.

── Admin Panel TinyMCE & Specs Management ──────────────────────────────────────
- Integrated TinyMCE Visual Editor (`@tinymce/tinymce-react`) into `ProductForm.jsx` for `content` field, supporting Persian RTL, custom headings (H2/H3 for automatic TOC creation), lists, tables, and media formatting.
- Added `shortDescription` textarea input for hero summary.
- Built dynamic Key-Value Specifications Editor (`specsEditor`) with live row addition, key/value inputs, and deletion.
- Updated `getAdminProductById` in `adminApi.js` and POST/PUT API routes in `/api/admin/products` to save and populate all new product fields.

##latest- 2026-07-31

### Feature & UI: Search Redesign & Category Filter System
**Status**: ✅ COMPLETED (100%)

#### Overview:
Fully modernized and redesigned the global search components (SearchBox, SearchOverlay, SearchResults) with glassmorphic aesthetics, CSS variables (`variables.css`), responsive layouts, and a dedicated category filtering system ("محصولات", "مقالات", "دوره‌ها").

#### Changes Made:
1. **SearchBox Component** (`SearchBox.jsx` & `SearchBox.module.scss`):
   - Refactored UI to a glassmorphic pill search bar (`backdrop-filter: blur(16px)`).
   - Styled custom `<select>` category dropdown with custom RTL arrow and focus rings.
   - Added an inline clear button (`X`) to erase query text.
   - Replaced invalid SCSS `fade()` calls with standard `color-mix(in srgb, ...)` CSS functions.
   - Added `initialType` prop support to stay synchronized with search page URL parameters.

2. **SearchOverlay Component** (`SearchOverlay.jsx` & `SearchOverlay.module.scss`):
   - Transformed overlay backdrop to deep glass blur (`backdrop-filter: blur(20px)`).
   - Upgraded search overlay to a floating card modal with animated entrance scale.
   - Added interactive quick category chips ("همه دسته‌ها", "محصولات", "مقالات", "دوره‌ها").
   - Added active category filter state (`selectedType`) to restrict search query execution strictly to the chosen category.
   - Integrated keyboard shortcut hint badge (`ESC`) and 90-degree rotating close button.

3. **SearchResults Module** (`SearchResults.jsx` & `SearchResults.module.scss`):
   - Enhanced header title with query highlight underline and total result count badge.
   - Upgraded category filter tabs with live item count badges and smooth active pill indicators.
   - Redesigned empty state card with visual search icon and helpful search suggestions.

4. **Page & Search API Integration** (`src/app/search/page.js`):
   - Synchronized `userType` parameter between server-side search fetcher and client component tree.

---

[2026-07-29] - Fix Nested Array Prop Handling for RelatedProductCTA

── Bug Fix & Array Flattening ────────────────────────────────────────────────
- Resolved nested array bug (`[ [course, product] ]`) in `RelatedProductCTA.jsx` by implementing `rawList.flat(Infinity).filter(Boolean)`.
- Updated `page.js` to pass `<RelatedProductCTA enableCta={rawArticle.enable_cta} items={ctaItems} />`.
- Verified that selecting course "سایه‌ها" and product "گردنبند" renders both exact items on the article detail page.

[2026-07-29] - Dual CTA Support for Simultaneous Course & Product Rendering

── Frontend Multi-Item CTA Rendering ─────────────────────────────────────────
- Refactored `getArticleCTA` in `articlesApi.js` to return an array of all connected CTA items (`featured_course` and `featured_product`).
- Enhanced `RelatedProductCTA.jsx` to render all items seamlessly in `.ctaWrapper` layout.
- When both a course and a product are connected to an article, both cards render with their respective badges, icons, prices, and CTA action buttons.

── Admin Panel Simultaneous Selection ────────────────────────────────────────
- Updated `ArticleForm.jsx` to allow simultaneous selection of both `featured_course` and `featured_product`.

[2026-07-29] - Featured Product Support for RelatedProductCTA & Admin Panel

── Frontend Featured Product CTA Support ─────────────────────────────────────
- Enhanced `RelatedProductCTA.jsx` to seamlessly support both `featured_course` and `featured_product`.
- Added product-specific UI details: `ShoppingBag` icon, "محصول مرتبط" tag label, "پیشنهاد ویژه محصول مرتبط" badge, and "مشاهده و خرید محصول" action button targeting `/products/[slug]`.
- Exported `formatStrapiProducts` in `strapiUtils.js` and updated `getArticleCTA` in `articlesApi.js` to fetch full product details with images and prices.

── Admin Panel Featured Product Management ──────────────────────────────────
- Added `getAdminProducts(jwt)` in `adminApi.js` to retrieve available products list.
- Updated `ArticleForm.jsx` to include "انتخاب محصول مرتبط" dropdown selector with auto-clearing logic between course and product.
- Updated `getAdminArticleById` and POST/PUT API routes to send and read `featured_product`.

[2026-07-29] - RelatedProductCTA Method A Refactoring (Manual Connection & Enable Switch)

── Refactored RelatedProductCTA Component ─────────────────────────────────────
- Updated `RelatedProductCTA` to accept `enableCta` (Boolean) and `course` (Object) props.
- Implemented strict conditional rendering: returns `null` immediately if `enableCta === false` or `!course`.
- Removed automatic tag querying and default fallback cards in accordance with Method A specifications.

── Backend Schema & API Updates ──────────────────────────────────────────────
- Added `enable_cta` boolean field (default: `true`) to Strapi Article schema (`backend/src/api/article/content-types/article/schema.json`).
- Updated `getArticleCTA(article)` in `articlesApi.js` and `formatStrapiArticles` in `strapiUtils.js` to return `course` details or `null`.
- Updated `app/articles/[slug]/page.js` to pass `<RelatedProductCTA enableCta={rawArticle.enable_cta} course={ctaCourse} />`.

[2026-07-29] - RelatedProductCTA Banner & Backend Schema Enhancements

── Created RelatedProductCTA Component ───────────────────────────────────────
- Implemented `RelatedProductCTA` Server Component to display featured courses/products as banner cards.
- Implemented 3-tier fallback algorithm in `getArticleCTA(article)`: 1) Direct Strapi relation, 2) Tag/Category matching query, 3) System default fallback CTA card.
- Formatted prices in Iranian Tomans (`toLocaleString('fa-IR')`), badge highlights, and responsive 2-column layout.

── Backend Schema Upgrade ────────────────────────────────────────────────────
- Extended Article content-type schema in Strapi (`backend/src/api/article/content-types/article/schema.json`) to include `featured_course` and `featured_product` relations.

[2026-07-29] - Single Article Modules (ArticleTags, ArticleNav, RelatedArticles) & Related Enhancements

── Created ArticleTags Component ──────────────────────────────────────────────
- Implemented `ArticleTags` component to render article tags as capsule pills with modern CSS animations.
- Configured dynamic linking to `/articles?tag={tag.slug}`.
- Added support for both string tags and Strapi Tag objects.

── Created ArticleNav Component ───────────────────────────────────────────────
- Implemented `ArticleNav` component featuring RTL-optimized previous and next article navigation cards.
- Displays guide arrows (`ArrowRight` and `ArrowLeft`) and truncated article titles to maintain visual consistency.
- Automatically handles layout alignment when only one of the adjacent articles is available.

── Created RelatedArticles Component ──────────────────────────────────────────
- Implemented `'use client';` `RelatedArticles` component using `BaseSlider` and `ArticleCard`.
- Configured server-side pre-fetching in page route for SSR and passed results as props to bypass Next.js RSC client boundary function passing limitations.
- Automatically filters out the current article from the slider suggestions.

── API & Utils Enhancements ──────────────────────────────────────────────────
- Added `getAdjacentArticles(createdAt)` to `articlesApi.js` to fetch previous (older) and next (newer) articles.
- Added `getRelatedArticles({ categoryId, currentId })` to `articlesApi.js` to retrieve related posts.
- Enhanced `formatStrapiArticles` in `strapiUtils.js` to map tags, category, and creation date parameters.

── Created & Modified Files ──────────────────────────────────────────────────
- src/modules/articles/ArticleTags.jsx [NEW]
- src/modules/articles/ArticleTags.module.scss [NEW]
- src/modules/articles/ArticleNav.jsx [NEW]
- src/modules/articles/ArticleNav.module.scss [NEW]
- src/modules/articles/RelatedArticles.jsx [NEW]
- src/modules/articles/RelatedArticles.module.scss [NEW]
- src/lib/articlesApi.js
- src/lib/strapiUtils.js
- src/app/articles/[slug]/page.js

[2026-07-29] - Article Reading Mode, Sticky Flex TOC & Performance Optimizations

── Fixed Layout Sticky Flex TOC ──────────────────────────────────────────────
- Replaced the absolute/fixed position calculation with a robust CSS Flex Row sticky layout:
  - Container `.articleLayout` matches centered layout with `align-items: flex-start` to preserve vertical sticky boundary.
  - TOC Sidebar `.tocSidebar` uses `position: sticky; top: 120px; align-self: flex-start` to remain fixed only when scrolled to, tracking along the article container correctly and stopping at the article end.
  - Automatically sits on the right side in RTL layouts (as the first child of the flex container) without throwing off the main article box alignment.
- Resolved layout issues by keeping the main article content `.content` and `.excerpt` perfectly centered on the page (`max-width: 800px; margin: 0 auto;`).

── Scroll Spy Active Section Highlighting ───────────────────────────────────────
- Implemented client-side scroll position detection in `ArticleReader.jsx` using a scroll event listener:
  - Tracks user scroll position against heading tags (`h2` and `h3`) with dynamic offset calculations (`window.scrollY + 130`).
  - Sets `data-active="true"` on the currently viewed heading link in the TOC sidebar.
  - Styled `data-active` links in SCSS with subtle gold highlights, border-right indicator, and bold font weight.

── Typography Scaling ──────────────────────────────────────────────────────────
- Scaled down font sizes across the single article template for a cleaner, more compact, and balanced reading layout:
  - Body text (`p`, `li`, `td`) decreased from `16px` to `14px`.
  - Section headings (`h2`) scaled down from `24px` to `20px`.
  - Sub-headings (`h3`) scaled down from `20px` to `17px`.
  - Excerpt description text reduced from `18px` to `15px`.
  - Blockquotes, Quranic texts, and poem quotations scaled down proportionally (`quran-text`: `24px`, `poem-quote`: `16px`).

── Scroll Performance Optimization ─────────────────────────────────────────────
- Removed processor-heavy `backdrop-filter: blur(...)` styles from long scrolling components (`.content`, `.tocCard`, excerpt box, theme toggle button) to eliminate paint/redraw lag and ensure 60fps smooth scrolling.
- Applied clean solid backgrounds (`#061e1e` for dark content box, `#052222` / `#042222` for TOC) to reduce GPU rendering load.

── Page-Specific Temporary Body Theme Toggle ───────────────────────────────────
- Integrated dynamic `body` background switching when Day Reading Mode is active on the article:
  - Listens to `isLight` state inside `ArticleReader.jsx` and appends `article-light-mode-active` class to the HTML `body` element.
  - Turns page background into soft sage/light shade (`#F4F6F0`) exclusively while reading in day mode.
  - Cleans up and removes the class from `body` on unmount/navigation to prevent side-effects on other pages.

── Layout Sticky Context Safeguard ──────────────────────────────────────────────
- Replaced `overflow-x: hidden` with `overflow-x: clip` on `html, body` inside `globals.css` to prevent breaking sticky positioning context, while safely clipping horizontal scroll overflows.

── Created & Modified Files ──────────────────────────────────────────────────────
- src/app/articles/[slug]/ArticleReader.jsx
- src/app/articles/[slug]/page.module.scss
- src/app/globals.css

## [2026-07-29]

### Features: Article Tags, Navigation & Related Slider
**Status**: ✅ COMPLETED (100%)

#### Overview:
Implemented three dynamic modules at the end of the single article page: ArticleTags for capsule pills, ArticleNav for previous/next navigation cards, and RelatedArticles for a category-based slider, following the context specification.

#### Changes Made:
1. **ArticleTags Component**:
   - Renders tags in capsule badges with tag icon.
   - Links to search page at `/articles?tag={tag}`.
2. **ArticleNav Component**:
   - Displays previous and next article links as styled cards with truncation.
   - Built backend API endpoint helper `getAdjacentArticles(createdAt)` to query adjacent posts.
3. **RelatedArticles Component**:
   - Integrates `BaseSlider` and `ArticleCard` client-side.
   - Queries category posts via `getRelatedArticles({ categoryId, currentId })`.
4. **Integration**:
   - Formatted in `page.js` after `ArticleReader` and before `CommentsSection`.

##latest- 2026-07-29

### Feature: Single Article Page Breadcrumb with Dynamic Category Navigation
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Article Page (`src/app/articles/[slug]/page.js`)**:
   - Integrated `Breadcrumb` component (`@/components/ui/BreadCrumb/Breadcrumb`).
   - Added dynamic hierarchy: `خانه` -> `مقالات` -> `[نام دسته‌بندی]` -> `[عنوان مقاله]`.
   - Category item links directly to `/articles?category=${category.slug}` for filtered navigation.
   - Gracefully handles articles with or without an assigned category.

2. **Strapi Articles Formatter (`src/lib/strapiUtils.js`)**:
   - Updated `formatStrapiArticles` to extract and format article categories (`articles_categories` / `categories`).
   - Returns structured `categories` array containing `id`, `documentId`, `name`, and `slug`.

#### Impact:
- Improved User Navigation (UX) across single article pages.
- Enhanced SEO with structured `BreadcrumbList` schema JSON-LD injection on article detail routes.
[2026-07-28] - Table of Contents (TOC) Refactoring, Sticky Sidebar & Auto Extraction System

── Smart Auto TOC Extraction & SSR Hydration Safety ──────────────────────────────
- Implemented smart automatic Table of Contents (TOC) extraction in `ArticleReader.jsx` using `useMemo`:
  - Parses `<h2>` and `<h3>` tags safely via regex for SSR hydration safety (no DOM API dependency during initial render).
  - Preserves existing heading `id`s if present; automatically generates dynamic unique IDs (`id="heading-1"`, `id="heading-2"`, ...) and injects them into title tags in `cleanContent`.
  - Strips HTML tags and decodes common Persian HTML entities (`&zwnj;`, `&nbsp;`, etc.) to generate clean plain text titles for TOC list items.
  - Generates structured TOC array: `Array<{ id: string, text: string, level: number }>` (`level: 2` for `h2`, `level: 3` for `h3`).
  - Added smart `<nav class="table-of-contents">` unwrapping logic: detects if legacy `<nav>` tags wrap the article body content (e.g. Strapi TinyMCE exports) and unwraps opening/closing tags to prevent wiping article content.

── Sticky TOC Sidebar & Scroll Spy Highlighting ──────────────────────────────────
- Created Sticky TOC Sidebar (`<aside className={styles.tocSidebar}>`):
  - Conditionally renders when at least 1 heading is extracted.
  - Applies `styles.subHeading` class to level 3 (`h3`) items for RTL indentation (`padding-right: 0.75rem`).
  - Integrated smooth scrolling on click with `window.scrollTo` and offset subtraction (`110px`) for sticky navbar header.
  - Implemented real-time passive scroll listener (`handleScroll`) to dynamically track and highlight the currently read section (`activeId` state & `styles.active` class).
  - Retained collapsible mobile drawer view (`isTocOpen` state) with Lucide `List`, `ChevronUp`, and `ChevronDown` icons.

── Article Page Route Integration & Strapi Data Resilience ───────────────────────
- Updated `ArticlePage` (`src/app/articles/[slug]/page.js`):
  - Imported and rendered `<ArticleReader excerpt={article.excerpt} content={article.content} />`.
  - Added safe Markdown parsing fallback via `marked` if article content from Strapi is in Markdown format.
- Extended `src/lib/strapiUtils.js`:
  - Added `strapiBlocksToHtml` helper function to convert Strapi v5 Blocks, RichText objects, and Markdown strings into clean HTML.
- Updated `src/app/articles/[slug]/page.module.scss`:
  - Added `.subHeading`, `.active`, and `[data-active="true"]` styles for both Dark Mode and Light Mode (`.lightTheme`).

── Created & Modified Files ──────────────────────────────────────────────────────
- src/app/articles/[slug]/ArticleReader.jsx [NEW]
- src/app/articles/[slug]/page.js
- src/app/articles/[slug]/page.module.scss
- src/lib/strapiUtils.js

[2026-07-28] - Admin Panel UI Harmonization & Shared Component Library

── Unified Admin Styling & Shared Component Library ───────────────────────────────
- Created a centralized, responsive shared component library for the Admin Panel (`src/components/admin/Shared/`):
  - `AdminPage`: Page container.
  - `AdminHeader`: Header wrapper supporting dynamic action buttons.
  - `AdminTableContainer` & `AdminTable`: Wrappers providing standard card layout, scrolling, and table structures.
  - `AdminSearch`: Redesigned standalone glassmorphic search input combined with a separate search button, eliminating placeholder overlap.
  - `AdminButton`: Clean action buttons with presets (`default`, `primary`, `edit`, `delete`, `approve`).
  - `AdminBadge`: Unified badges for statuses (`success`, `warning`, `error`, `info`, `default`).
- Refactored all admin pages to use the new shared components, ensuring absolute consistency in layout, margins, paddings, and borders across:
  - Products (`src/components/admin/Products/ProductsTable.jsx`)
  - Articles (`src/components/admin/Articles/ArticlesTable.jsx`)
  - Users (`src/components/admin/Users/UsersTable.jsx`)
  - Orders (`src/components/admin/Orders/OrdersTable.jsx`)
  - Comments (`src/components/admin/Comments/CommentsManager.jsx`)
  - Messages (`src/components/admin/Messages/MessagesTable.jsx`)

── Visual Enhancements & Grid Harmonization ───────────────────────────────────────
- Implemented card-like table rows utilizing `border-collapse: separate` and `border-spacing: 0 12px` to distinctively space out items.
- Styled rows with a solid golden right border, subtle box-shadows, and smooth translate-up hover micro-animations.
- Compacted layout by reducing padding inside table items (`padding: 0.85rem 1rem`).
- Fixed Orders page expanded accordion row to smoothly stick to its parent row using a negative translation offset, keeping the visual card outline continuous.
- Removed arbitrary `max-width: 1200px` limitation from `/admin/orders/orders.module.scss` to make all admin tables full-width.

── Created & Modified Files ──────────────────────────────────────────────────
- src/components/admin/Shared/AdminBadge.jsx [NEW]
- src/components/admin/Shared/AdminButton.jsx [NEW]
- src/components/admin/Shared/AdminHeader.jsx [NEW]
- src/components/admin/Shared/AdminPage.jsx [NEW]
- src/components/admin/Shared/AdminSearch.jsx [NEW]
- src/components/admin/Shared/AdminTable.jsx [NEW]
- src/components/admin/Shared/AdminShared.module.scss [NEW]
- src/components/admin/Products/ProductsTable.jsx
- src/components/admin/Articles/ArticlesTable.jsx
- src/components/admin/Users/UsersTable.jsx
- src/components/admin/Orders/OrdersTable.jsx
- src/components/admin/Orders/OrdersTable.module.scss
- src/components/admin/Comments/CommentsManager.jsx
- src/components/admin/Messages/MessagesTable.jsx
- src/app/admin/orders/orders.module.scss

[2026-07-26] - Recent Updates Dashboard Feed & Admin Comments Management System

── Admin Dashboard Recent Updates Section ─────────────────────────────────────
- Implemented `RecentUpdates` Server Component and API (`src/lib/recentUpdatesApi.js`, `src/components/home/RecentUpdates/`, `src/components/cards/UpdateCard/`):
  - Fetches latest products, articles, services, and comments in parallel using `Promise.all`.
  - Displayed in a responsive 4-column layout on the Admin Dashboard (`/admin`).
  - Styled as horizontal list tiles with type badges, titles, and Persian relative timestamps (`getRelativeDate`).

── Admin Comments Management System ─────────────────────────────────────────
- Created Admin Comments Page (`src/app/admin/comments/page.jsx`):
  - Added "نظرات" link with `MessageSquare` icon to `AdminSidebar.jsx` (`/admin/comments`).
  - Created `CommentsManager` Client Component & SCSS (`src/components/admin/Comments/`):
    - Table view with tabs: "همه نظرات", "در انتظار تأیید", "تأیید شده", plus real-time search.
    - Approval Toggle: Toggle comments between approved/pending states (`PUT /api/admin/comments/[id]`).
    - Comment Deletion: Delete comments with confirmation modal (`DELETE /api/admin/comments/[id]`).
    - Comment Reply: Send admin replies linked to parent comment and original product/article/course relation (`POST /api/admin/comments`).
    - Nested Tree View: Displays child replies nested directly under their parent comments with visual indentations and reply icons.
    - Clickable Target Badges: Clicking on product/article/course target badges opens the corresponding page in a new tab (`/product/[slug]`, `/articles/[slug]`, `/courses/[slug]`).
- Added admin comments endpoints to `src/lib/adminApi.js` (`getAdminComments`, `updateCommentApproval`, `deleteCommentAdmin`, `replyCommentAdmin`).
- Extended `/api/admin/comments/route.js` and `/api/admin/comments/[id]/route.js` to handle `POST`, `PUT`, and `DELETE` requests securely with admin session JWT.

── Admin Dashboard Layout & Stats Card Adjustments ─────────────────────────────
- Fixed revenue overflow in `StatsCard.module.scss` (`font-size: clamp(20px, 2.2vw, 28px)`, `word-break: break-word`).
- Removed `RecentUpdates` from the public home page and integrated it cleanly into the Admin Dashboard (`src/app/admin/page.jsx`).

── Created & Modified Files ──────────────────────────────────────────────────
- src/app/admin/comments/page.jsx [NEW]
- src/components/admin/Comments/CommentsManager.jsx [NEW]
- src/components/admin/Comments/CommentsManager.module.scss [NEW]
- src/components/home/RecentUpdates/RecentUpdates.jsx [NEW]
- src/components/home/RecentUpdates/RecentUpdates.module.scss [NEW]
- src/components/cards/UpdateCard/UpdateCard.jsx [NEW]
- src/components/cards/UpdateCard/UpdateCard.module.scss [NEW]
- src/lib/recentUpdatesApi.js [NEW]
- src/app/admin/page.jsx
- src/app/admin/page.module.scss
- src/components/admin/Sidebar/AdminSidebar.jsx
- src/components/admin/StatsCard/StatsCard.module.scss
- src/lib/adminApi.js
- src/app/api/admin/comments/route.js
- src/app/api/admin/comments/[id]/route.js
[2026-07-25] - Custom 404 Not Found Page Implementation

── Custom 404 Not Found Page ──────────────────────────────────────────────────
- Created custom 404 page (`src/app/not-found.jsx` & `src/app/not-found.module.scss`):
  - Displays `/images/error404.webp` image with rounded corners (`border-radius: 28px`), backdrop blur, subtle gold glow, and hover transitions.
  - Added two action buttons:
    1) "بازگشت به صفحه قبل" (Back to previous page) using `router.back()`.
    2) "صفحه اصلی" (Home page) linking to `/`.
  - Fully responsive, RTL layout styled to match the dark/gold theme of the application.

── Created Files ───────────────────────────────────────────────────────────────
- src/app/not-found.jsx [NEW]
- src/app/not-found.module.scss [NEW]

[2026-07-25] - Profile Overview Recent Orders Removal & ChatItem User Display Name Resolution

── Profile Overview Clean-up ──────────────────────────────────────────────────
- Updated `ProfilePage` (`src/app/profile/page.js`):
  - Removed `<OrdersList limit={2} />` section completely from the main account information / overview page (`/profile`).

── User Display Name Resolution ──────────────────────────────────────────────
- Updated `InstructorChatPanel.jsx` (`src/components/chat/InstructorChatPanel.jsx`):
  - Replaced single-letter text initials in `chatItem__avatar` and `chatHeader__avatar` with a modern SVG user person icon.
  - Created `getUserDisplayName(msg)` helper function to intelligently resolve user names instead of displaying raw phone numbers in sidebar chat items (`chatItem__name`), search filter, and chat headers (`chatHeader__name`).
  - Priorities: 1) Complete profile name (requires BOTH `firstName` AND `lastName`, or `fullName`), 2) Form filled name in `metaData` / `msg.name` if profile name is incomplete/missing, 3) Fallback to `کاربر ناشناس`.

[2026-07-24] - Mentor Pre-Chat Form Settings Inline Editor

── Mentor Pre-Chat Form Settings Inline Editor ──────────────────────────────────────
- Added Inline Form Editor to Mentor Panel (`src/components/chat/InstructorChatPanel.jsx` & `InstructorChatPanel.module.scss`):
  - Added a "ویرایش فرم سالکان" button in the sidebar footer area of the Mentor Dashboard.
  - Wired up `MentorFormEditor` modal to allow the instructor to configure form titles, descriptions, and dynamic questions inline.
- Created `MentorFormEditor.jsx` & `MentorFormEditor.module.scss` (`src/components/chat/`):
  - Allows full CRUD operations (add, remove, update keys/labels/placeholders/options, reorder questions) directly from the UI.
  - Supports automatic comma-separated parsing and JSON parsing for drop-down Select questions.
- Updated Messages API (`src/lib/messagesApi.js`):
  - Created `updateMentorFormSetting(payload, token)` function using `PUT /api/mentor-form-setting` to save updated schemas back to Strapi.

── Created & Modified Files ──────────────────────────────────────────────────────────
- src/components/chat/MentorFormEditor.jsx [NEW]
- src/components/chat/MentorFormEditor.module.scss [NEW]
- src/components/chat/InstructorChatPanel.jsx
- src/components/chat/InstructorChatPanel.module.scss
- src/lib/messagesApi.js

[2026-07-24] - Chat Bubble Alignment, Auto-Scroll Optimization & Smart Real-Time Polling

── Chat Bubble Alignment & RTL Layout Fixes ──────────────────────────────────
- Updated `InstructorChatPanel` (`src/components/chat/InstructorChatPanel.jsx` & `InstructorChatPanel.module.scss`):
  - Fixed bubble alignment in RTL layout: Mentor/Instructor's own messages (`bubble--instructor`) now align to the RIGHT side (`align-self: flex-start` in RTL) with label 'استاد (شما)'.
  - Salik/User messages (`bubble--user`) now align to the LEFT side (`align-self: flex-end` in RTL).
- Updated `MessageDetailModal` (`src/modules/profile/MessageDetailModal.jsx` & `MessageDetailModal.module.scss`):
  - Explicitly set `direction: rtl` on `.chatWindow` and `.messagesStream`.
  - User's own messages (`bubble--user`) align to the RIGHT side (`align-self: flex-start` in RTL) with label 'شما'.
  - Staff/Instructor messages (`bubble--staff`) align to the LEFT side (`align-self: flex-end` in RTL).

── Auto-Scroll & Smooth UX Improvements ─────────────────────────────────────
- Removed window/page `scrollIntoView()` on thread selection in `InstructorChatPanel.jsx`:
  - Prevents unwanted page jump/scroll when selecting a chat item in the sidebar or opening a thread.
  - Attached `messagesContainerRef` to internal `.messages` stream to handle inner container scrolling (`scrollTop`) when new replies are sent.

── Smart Real-Time Polling (5s Interval) ────────────────────────────────────
- Implemented 5-second smart polling in `InstructorChatPanel.jsx` and `MessageDetailModal.jsx`:
  - Automatically fetches updated messages via `getInstructorMessages` / `getMyMessages` every 5 seconds without full page refresh.
  - Automatically pauses polling when chat view/modal is unmounted or when browser tab is inactive (`document.visibilityState !== 'visible'`).

── Strapi API Payload Fix ───────────────────────────────────────────────────
- Updated `handleSubmitReply` in `MessageDetailModal.jsx`:
  - Removed invalid `isRead` attribute from PUT request payload to fix HTTP 400 `ValidationError: Invalid key isRead` error from Strapi backend.

── Modified Files ───────────────────────────────────────────────────────────
- src/components/chat/InstructorChatPanel.jsx
- src/components/chat/InstructorChatPanel.module.scss
- src/modules/profile/MessageDetailModal.jsx
- src/modules/profile/MessageDetailModal.module.scss

[2026-07-24] - Dynamic PreChatForm, Strapi MentorFormSetting Schema & MessagesList Unique Key Fix

── Strapi v5 Backend Schema & API ──────────────────────────────────────────────────
- Created `form.question` Repeatable Component (`backend/src/components/form/question.json`):
  - Attributes: `key` (string, required), `label` (string, required), `fieldType` (enum: 'text', 'number', 'textarea', 'select'), `isRequired` (boolean), `options` (json), `placeholder` (string).
- Created Single Type `MentorFormSetting` (`backend/src/api/mentor-form-setting/content-types/mentor-form-setting/schema.json`):
  - Attributes: `title` (string), `description` (text), `questions` (component: 'form.question', repeatable: true).
- Created Strapi Core Router, Controller, and Service (`backend/src/api/mentor-form-setting/`):
  - Router (`routes/mentor-form-setting.js`): Configured `config: { find: { auth: false } }` to allow public fetching of pre-chat form settings.
  - Controller (`controllers/mentor-form-setting.js`) and Service (`services/mentor-form-setting.js`).

── Dynamic PreChatForm Component Refactoring ─────────────────────────────────────────
- Updated `PreChatForm.jsx` (`src/components/chat/PreChatForm.jsx`):
  - Added dynamic schema fetching from `/api/mentor-form-setting?populate=questions`.
  - Implemented dynamic field rendering (`text`, `number`, `textarea`, `select`) and dynamic validation rules with `react-hook-form`.
  - Serializes all dynamic inputs into a single JSON object inside `metaData` parameter sent to `/api/messages`.
  - Added skeleton loader state during schema fetch and silent fallback to default questions (`DEFAULT_QUESTIONS`) when backend settings are uninitialized or missing.
- Updated `PreChatForm.module.scss` (`src/components/chat/PreChatForm.module.scss`):
  - Added pulse skeleton animation and retry button styles.

── API Client & Messages API Updates ────────────────────────────────────────────────
- Updated `apiClient.js` (`src/lib/apiClient.js`):
  - Added `suppressErrorLog` option to allow suppress error logging on non-200 responses when fallback mechanisms are active.
- Updated `messagesApi.js` (`src/lib/messagesApi.js`):
  - Added `getMentorFormSetting(token)` function with `suppressErrorLog: true`.
  - Updated `submitInstructorMessage` to accept dynamic `metaData` JSON objects.

── MessagesList Duplicate React Key Fix ─────────────────────────────────────────────
- Fixed `MessagesList.jsx` (`src/modules/profile/MessagesList.jsx`):
  - Changed React key from `msg.id` to `msg.documentId || `${msg.id}-${msg.messageType || 'contact'}-${index}`` to resolve duplicate key warning when merging `/api/contact-messages` and `/api/messages`.

── Modified & Created Files ──────────────────────────────────────────────────────────
- backend/src/components/form/question.json [NEW]
- backend/src/api/mentor-form-setting/content-types/mentor-form-setting/schema.json [NEW]
- backend/src/api/mentor-form-setting/routes/mentor-form-setting.js [NEW]
- backend/src/api/mentor-form-setting/controllers/mentor-form-setting.js [NEW]
- backend/src/api/mentor-form-setting/services/mentor-form-setting.js [NEW]
- src/lib/apiClient.js
- src/lib/messagesApi.js
- src/components/chat/PreChatForm.jsx
- src/components/chat/PreChatForm.module.scss
- src/modules/profile/MessagesList.jsx

[2026-07-23] - Instructor Chat System, Mentor Dashboard, SPA Profile Chat & Backend Strapi Integration (100% Complete)

── Server-Side Auth Guard & Mentor Dashboard ──────────────────────────────────────
- Implemented `/mentor` route layout (`src/app/mentor/layout.jsx`):
  - Server Component Auth Guard retrieving NextAuth session server-side (`getServerSession(authOptions)`).
  - Enforces `session.user.role.type === 'administrator'` gate; issues server-side 307 HTTP redirect (`redirect('/')`) before any HTML or client bundle is shipped to unauthorized users.
- Created `/mentor` page (`src/app/mentor/page.jsx`):
  - Server Component pre-fetching instructor messages server-side (`getInstructorMessages`) for waterfall-free initial render while keeping JWT tokens off client bundles.

── Instructor Chat Panel & Complete UI Redesign ───────────────────────────────────
- Built `InstructorChatPanel.jsx` & `InstructorChatPanel.module.scss`:
  - Modern two-column CSS grid layout (340px sidebar + flex chat window) with responsive mobile drawer view.
  - Complete UI overhaul with glassmorphism backdrop blur (`backdrop-filter: blur(20px)`), ambient radial background gradients, and micro-animations.
  - Sidebar: search bar with gold focus ring, conversation item cards with active gold indicator bar, status pills ('پاسخ داده شد' / 'در انتظار'), and conversation counter badge.
  - Chat Window: conversation header, Salik metadata panel (age, marital status, job, spiritual background), message stream with distinct Salik vs Instructor bubbles and author badges, and floating bottom reply input bar.
  - 100% strict adherence to global design system in `variables.css` using `var(--color-...)` and `color-mix(in srgb, ...)` for opacities (zero raw hex or hardcoded inline RGB values).

── Salik PreChatForm & Contact Mentor Route ───────────────────────────────────────
- Created `PreChatForm.jsx` & `PreChatForm.module.scss`:
  - Interactive client form using `react-hook-form` for collecting Salik info (age, marital status, job, spiritual background, and inquiry).
  - Serializes form inputs into JSON `metaData` and posts to `/api/messages` with `messageType: 'instructor'`.
- Created `/contact-mentor` page (`src/app/contact-mentor/page.jsx` & `contact-mentor.module.scss`):
  - User-facing page wrapping `PreChatForm` with auth redirect and success confirmation screen.

── Full SPA User Profile Chat Overlay ─────────────────────────────────────────────
- Redesigned `MessageDetailModal.jsx` & `MessageDetailModal.module.scss`:
  - Replaced the small popup modal dialog with an expanded full-screen/SPA glassmorphism chat overlay (`position: fixed; inset: 0; z-index: 1000`).
  - Top header with message subject, status badge, date timestamp, and close (X) button / Escape key handler.
  - Message stream with Salik vs Instructor/Support bubble distinction and author labels ('استاد', 'پشتیبانی', 'شما').
  - Smart reply routing: automatically detects thread type and calls `updateInstructorMessage` for instructor threads or `updateMyMessage` for contact threads.
- Updated `getMyMessages` in `src/lib/messagesApi.js`:
  - Uses `Promise.allSettled` to fetch both `/api/contact-messages` and `/api/messages` simultaneously, merging and sorting them chronologically for `/profile/messages`.

── About Mentor Section CTA Button ───────────────────────────────────────────────
- Updated `AboutMentorSection.jsx` & `AboutMentorSection.module.scss`:
  - Added 'ارتباط با استاد' CTA button linking to `/contact-mentor` inside `contentWrapper`.
  - Styled with gold gradient background, hover scale, box-shadow, and responsive full-width layout on mobile.

── Strapi v5 Backend Integration ──────────────────────────────────────────────────
- Updated Message Content-Type Schema (`backend/src/api/message/content-types/message/schema.json`):
  - Added `messageType` enumeration (`instructor`, `contact`, `support`), `metaData` (json), `status` enumeration (`open`, `answered`, `closed`), `replies` (json), and `user` relation.
  - Changed `body` from `blocks` to `text` to accept string input directly.
- Updated Message Controller (`backend/src/api/message/controllers/message.js`):
  - Overrode `create`, `find`, `findOne`, and `update` to automatically link logged-in users, restrict non-admin queries to own messages, enforce ownership checks, and manage thread replies.

── Frontend Auth & API Client Safeguards ──────────────────────────────────────────
- Updated `src/lib/auth.js`:
  - Added content-type checks (`response.headers.get('content-type')`) before parsing JSON in `authorize` and `session` callbacks to prevent raw HTML `<!DOCTYPE...` syntax error crashes when backend services return HTML pages or non-JSON responses.
  - Added fallback secret in `authOptions` (`process.env.NEXTAUTH_SECRET || 'dev-secret-key-change-this'`).

── Modified & Created Files ───────────────────────────────────────────────────────
- backend/src/api/message/content-types/message/schema.json
- backend/src/api/message/controllers/message.js
- src/lib/messagesApi.js
- src/lib/auth.js
- src/app/mentor/layout.jsx [NEW]
- src/app/mentor/mentor.module.scss [NEW]
- src/app/mentor/page.jsx [NEW]
- src/app/contact-mentor/page.jsx [NEW]
- src/app/contact-mentor/contact-mentor.module.scss [NEW]
- src/components/chat/PreChatForm.jsx [NEW]
- src/components/chat/PreChatForm.module.scss [NEW]
- src/components/chat/InstructorChatPanel.jsx [NEW]
- src/components/chat/InstructorChatPanel.module.scss [NEW]
- src/modules/profile/MessagesList.jsx
- src/modules/profile/MessageDetailModal.jsx
- src/modules/profile/MessageDetailModal.module.scss
- src/modules/home/AboutMentorSection/AboutMentorSection.jsx
- src/modules/home/AboutMentorSection/AboutMentorSection.module.scss


[2026-07-22] - Expandable Admin Order Details, Strapi v5 Order Lifecycles & Admin Panel Fixes (100% Complete)

── Admin Orders Expandable Details Panel ──────────────────────────────────────────
- Added an Expandable Details Row (Accordion) for each order in the Admin Dashboard (`OrdersTable.jsx`):
  - Clicking anywhere on an order row (`<tr>`) smoothly opens/closes the detailed view panel, with `cursor: pointer` indicator and event propagation guards for action buttons.
  - Buyer Information section: displays real full name, phone number, email address, registration/order date, payment method, and tracking number.
  - Shipping Address section: displays complete postal address and postal code.
  - Purchased Items section: lists all courses, chapters, and physical products with icons, badges, quantities, individual prices, and order total summary.
  - Order Notes section: displays auto-generated item summaries and custom buyer notes.
- Updated `src/lib/adminApi.js`:
  - Enhanced `getOrders` query to include `populate[items]=true`.
  - Normalized `fullName`, `address`, `postalCode`, `phone`, `email`, `notes`, and Dynamic Zone `items` array (`order.course-order-item` & `order.product-order-item`).
- Redesigned `OrdersTable.module.scss`:
  - Added smooth CSS animations (`expandDown`), responsive grid layout, product item cards, and styled notes/address sections.

── Strapi v5 Order Lifecycle & Purchase Automation ─────────────────────────────
- Created backend Order lifecycle hook (`backend/src/api/order/content-types/order/lifecycles.js`):
  - Handled `afterCreate` and `afterUpdate` events to connect purchased courses & chapters automatically upon order payment completion.
  - Implemented relation updates from the **Course (Owner) side** (`api::course.course` -> `users_permissions_users`) to ensure rows write to PostgreSQL join tables and render properly in the Strapi Admin Panel UI.
  - Added dual support for numeric IDs and string `documentId` identifiers to eliminate PostgreSQL type errors and prevent database transaction aborts.
  - Deduplicated draft/published course entries during sync to prevent duplicate course assignments for users.

── Backend Schema Clean-up ───────────────────────────────────────────────────
- Removed trailing whitespace from `orderStatus` enum in `backend/src/api/order/content-types/order/schema.json` (`paid`, `shipped`, `delivered`, `canceled`, `pending`).
- Removed legacy/broken fields `enrolledCourses` and `enrolled_courses` from `User` schema (`backend/src/extensions/users-permissions/content-types/user/schema.json`).
- Removed broken `users` relation from `Article` schema (`backend/src/api/article/content-types/article/schema.json`).

── Checkout & Payment Status Alignment ──────────────────────────────────────────
- Fixed `PaymentStep.jsx`:
  - Updated `initialPaymentStatus` so online payment sets `paymentStatus: 'paid'` instead of defaulting to `'pending_payment'`.
- Updated Next.js API Proxy (`src/app/api/orders/route.js`):
  - Resolved `paymentStatus` and `orderStatus` alignment: online purchases now set both `orderStatus` and `paymentStatus` to `'paid'`, while card-to-card sets both to `'pending'` / `'pending_payment'`.
  - Smart Buyer Name Resolution (`fullName`): prioritized real user names (`firstName` + `lastName`) and fallback to formatted phone numbers (e.g. `کاربر (09219872433)`), preventing dummy address strings like `"1"`.
  - Item Summary Notes (`notes`): auto-generated clear, numbered item lists (courses, chapters, and physical products with quantities) in the `notes` field for quick admin reference.
- Fixed Admin Dashboard Order Status Modals (`StatusUpdateModal.jsx` and `OrdersTable.jsx`):
  - Removed legacy trailing spaces from `ORDER_STATUSES` and `ORDER_STATUS_CONFIG` keys (`'paid'`, `'shipped'`, `'delivered'`, `'canceled'`).
  - Added `.trim()` normalization to prevent Strapi enum validation errors (`orderStatus must be one of...`) when saving order statuses in the custom admin panel.

── Modified Files ─────────────────────────────────────────────────────────────
- backend/src/api/order/content-types/order/lifecycles.js
- backend/src/api/order/content-types/order/schema.json
- backend/src/extensions/users-permissions/content-types/user/schema.json
- backend/src/api/article/content-types/article/schema.json
- src/components/checkout/PaymentStep.jsx
- src/app/api/orders/route.js
- src/components/admin/Orders/StatusUpdateModal.jsx
- src/components/admin/Orders/OrdersTable.jsx
- src/components/admin/Orders/OrdersTable.module.scss
- src/lib/adminApi.js


[2026-07-22] - Dual-Mode Curriculum Architecture, Chapter Purchasing & Resume Playback (100% Complete)

── Dual-Mode Curriculum Architecture ──────────────────────────────────────────
- Implemented dual-mode curriculum manager in `CourseContentManager.jsx` following `@CONTEXT_CourseCurriculum.md`:
  - Branch A (Non-Chaptered / Flat List): Renders linear lesson list with locks on paid lessons for non-owners.
  - Branch B (Chaptered / Accordion): Renders chapter accordions with per-chapter ownership checks and standalone chapter purchase options.
- Added strict authentication wrapper around `curriculumSection`:
  - When the user is not authenticated (`!isAuthenticated`), the curriculum section and lesson list are completely hidden, showing only the authentication login CTA placeholder.

── Strapi v5 Backend & Order Integration for Chapters ─────────────────────────
- Updated Strapi v5 deep populate query in `coursesApi.js` (`getCourseBySlug`) for `chapters.lessons`, `curriculum`, and `media`.
- Created component schema `chapter.json` (`course-parts/chapter`) and extended `Course` (`isChaptered`, `chapters`) and `User` (`enrolledCourses`, `enrolledChapters`) content-types in backend.
- Updated `CourseOrderItem` component schema in backend (`components/order/course-order-item.json`) to include `chapterId` integer attribute.
- Fixed `POST /api/orders` route handler (`src/app/api/orders/route.js`):
  - Added support for cart items of `type === 'chapter'`.
  - Mapped chapter items to `order.course-order-item` with clean course URLs.
  - Updated user record mutation (`PUT /api/users/:id`) to persist purchased `chapterId`s into `user.enrolledChapters`.

── Cart & Purchases Navigation Fixes ──────────────────────────────────────────
- Fixed `/cart` page (`src/app/cart/page.jsx`):
  - Updated item filtering to include `type === 'chapter'` alongside full courses under "دوره‌ها و فصل‌های آموزشی".
- Fixed `CartIcon.jsx` navbar dropdown to format URLs and labels (`فصل آموزشی`) for chapter items.
- Fixed `/profile/purchases` (`PurchasesList.jsx`):
  - Extracted base course slug (`slug.split('-chapter-')[0]`) so clicking "مشاهده" on a purchased chapter navigates cleanly to `/courses/[courseSlug]`.

── Media Resume Playback (VideoJS & Plyr Audio) ───────────────────────────────
- Fixed video player seeking freeze bug in `VideoJSPlayer.jsx`:
  - Resolved issue where `player.currentTime(savedTime)` was overwritten prior to `loadedmetadata`.
  - Added source URL comparison (`currentSrcRef`) to prevent re-initializing `player.src()` on component re-renders mid-playback.
- Fixed audio player seeking bug in `PlyrAudioPlayer.jsx`:
  - Replaced `player.on('canplay')` with `player.once('canplay')` and `hasRestored` flag to prevent seek events from resetting audio time back to initial saved time.
  - Added `seeked` listener to instantly persist new position when skipping forward or backward.
  - Unified local storage key (`media_progress_c${courseId}_l${cleanLessonId}`) across both audio and video players.

── Modified Files ─────────────────────────────────────────────────────────────
- src/modules/courses/CourseContentManager.jsx
- src/modules/courses/VideoJSPlayer.jsx
- src/modules/courses/PlyrAudioPlayer.jsx
- src/app/cart/page.jsx
- src/components/layout/Navbar/CartIcon.jsx
- src/components/profile/PurchasesList.jsx
- src/components/checkout/CartReviewStep.jsx
- src/app/api/orders/route.js
- backend/src/components/order/course-order-item.json
- backend/src/components/course-parts/chapter.json
- backend/src/api/course/content-types/course/schema.json
- backend/src/extensions/users-permissions/content-types/user/schema.json

[2026-07-20] - Admin & Profile Messaging (Ticketing) Subsystems (100% Complete)

── Backend (Strapi v5) ─────────────────────────────────────────────────────
- Fully refactored `contact-message` controller overridden helpers (`find`, `findOne`, `create`, `update`):
  - Ensured secure record filtering where normal users can only list and modify their own messages, while administrative users (`admin` / `administrator`) can manage all messages.
  - Replaced legacy `entityService` queries with Strapi v5 Document Service queries (`strapi.documents().findOne`) to parse string collection document IDs (`documentId`) in PostgreSQL, resolving database casting exceptions (`invalid input syntax for type integer: "[hash]"`).
  - Enforced strict payload security in `update` to filter allowed fields (`replies` and `status`) for normal users, preventing email/name spoofing.

── Profile (User Messaging) ────────────────────────────────────────────────
- Implemented bidirectional chat threads in user profile `MessageDetailModal.jsx`:
  - Added user replying section allowing users to send replies directly under their active tickets.
  - Configured tick status to automatically transition back to `'open'` and `'isRead: false'` upon user reply, alerting administrative support.
  - Linked page state dynamically to update local threads instantly.

── Admin Messaging ─────────────────────────────────────────────────────────
- Expanded `MessageReadModal.jsx` to feature complete interactive chat-style history log (initial message + replies).
- Integrated reply text box and status updating dropdown (`open` / `pending` / `closed`) with PUT request forwarding.
- Fixed mapping in `getContactMessages()` (`adminApi.js`) which previously stripped out `replies` and `status` keys, causing empty conversation threads in the dashboard.
- Modified messages state management in `MessagesTable.jsx` to update message and status fields after submitting a reply, showing success toasts immediately.

[2026-06-24] - Admin Product & Blog & Contact Messages Subsystems (100% Complete)

── Product Management ───────────────────────────────────────────────────
- Created admin product management pages: `/admin/products` (list), `/admin/products/new` (create), and `/admin/products/[id]` (edit).
- Refactored and fixed the `description` field (Rich Text / Blocks):
  - Resolved issue where text failed to save by converting plain strings to Strapi's Blocks structure (paragraph type) during submission.
  - Extracted all paragraph texts from the Blocks array and joined them with newlines (`\n`) when loading into the edit form.
- Fixed the availability toggle (`isAvailable`) double-toggle issue:
  - Removed the `onClick` event handler from the parent `div` and converted it to a standard checkbox with `label htmlFor` to prevent event bubbling.
- Resolved Next.js product list caching issue:
  - Added `router.refresh()` call immediately before redirecting to the product management page to invalidate the Router Cache and display the latest values.
- Implemented server-side API proxy routes:
  - Secured JWT by keeping it server-side, routing requests through `/api/admin/products` and `/api/admin/products/[id]`.
  - Resolved Strapi v5 publication status (`publishedAt`) unpublishing/publishing bug by overriding the Strapi update controller method to call the Document Service and forwarding `publishedAt` in standard PUT request metadata.

── Articles & Blog ──────────────────────────────────────────────────────
- Created admin blog system based on `@CONTEXT_AdminArticles.md` including `/admin/articles` and article management form.
- Designed smart `ArticleForm.jsx` component:
  - Integrated `@tinymce/tinymce-react` editor styled to match the admin panel for saving standard `content` fields.
  - Implemented article cover image uploads via connection to the centralized upload proxy.
  - Added a reusable `MultiSelect` component to assign categories and tags from allowed options.
- Implemented corresponding API Routes:
  - Created `/api/admin/articles` and `/api/admin/articles/[id]` supporting standard PUT requests with `publishedAt` payload.
  - Added an interactive publish status toggle switch in `ArticlesTable.jsx` with optimistic updates, local state reloads, and detailed server-side error propagation in toast notifications.

── Contact Messages ─────────────────────────────────────────────────────
- Implemented message management panel based on `@CONTEXT_AdminMessages.md` at `/admin/contact-messages`.
- Created `MessagesTable` component and table, highlighting unread messages with bold text and visual indicators.
- Designed `MessageReadModal` with auto-read capability:
  - Opening a message automatically sends a `PUT` request to the proxy, marking the message as `isRead: true` in the database.
- Created corresponding API Routes and service methods in `adminApi.js`.

── Sidebar Navigation & Layout Fixes ────────────────────────────────────
- Updated `AdminSidebar.jsx` to add easy access to messages and articles using standard Lucide React icons (`FileText` and `Mail`).
- Resolved admin dashboard overlapping issue under the 100px fixed header:
  - Added `margin-top: 100px` and `min-height: calc(100vh - 100px)` style rules to `.dashboard` in `admin.module.scss`.
  - Shifted sticky `.sidebar` position by updating `top` to `100px` and adjusting its height to `min-height: calc(100vh - 100px)` in `AdminSidebar.module.scss`.

[2026-06-23] - Admin Comment Moderation Panel (100% Complete)

── UserCommentItem.jsx (New/Complete Rewrite) ───────────────────────────
- Completely rewrote `src/components/admin/Users/UserCommentItem.jsx`.
- Approve/Reject toggle: PUT `/api/admin/comments/:id` updates `isApproved` and immediately updates UI badge.
- Inline reply capability: clicking "Reply" opens an animated expanding textarea.
  - Replies are submitted with `isApproved: true` and correctly linked to `product` / `article` / `course` using `documentId`.
- "View on Site" dynamic link based on `product.slug`, `article.slug`, or `course.slug`.
- Used Lucide React icons: `Check`, `X`, `MessageSquare`, `ExternalLink`, `Send`, `Loader2`.
- Loading state: disables all buttons during submission and shows a spinner.

── New API Route ────────────────────────────────────────────────────────
- Created `src/app/api/admin/comments/route.js` (POST handler) as a secure proxy for admin replies to Strapi (JWT kept server-side).

── Users.module.scss ────────────────────────────────────────────────────
- Added `btnReply` (blue) and `btnLive` (neutral) classes for new buttons.
- Added `replyBox`, `replyTextarea`, and `replyBoxActions` classes for inline reply box.
- All styles utilize variables from `variables.css`.

── adminApi.js — Fixed Duplicate Comments Bug ───────────────────────────
- Root cause: Strapi query with multiple deep relations (orders + courses + comments) in a single JOIN caused a Cartesian product, duplicating comments.
- Solution: Split `getUserDetails` into two parallel API calls (`Promise.all`):
  1. `GET /api/users/:id` - basic info + orders + courses (no comments).
  2. `GET /api/comments?filters[user][id][$eq]=:userId` - comments separately.
- Fully mapped comment relations (`product`, `article`, `course`) with `slug` and `documentId` to enable the "View on Site" button.

── Modified Files ───────────────────────────────────────────────────────
- src/components/admin/Users/UserCommentItem.jsx (Complete rewrite)
- src/components/admin/Users/Users.module.scss
- src/app/api/admin/comments/route.js (New)
- src/lib/adminApi.js

[2026-06-22] - Order Management UX Improvements (100% Complete)

── Order Navigation ─────────────────────────────────────────────────────
- Removed Modal and redirected all "View Details" buttons in `OrdersList.jsx` to the dedicated `/profile/orders/[documentId]` page.
- Added "Order Status" section with Badge in the order details page (`[id]/page.jsx`).
- Added conditional display of "Tracking Code" if the order is shipped.

── Status Prioritization ────────────────────────────────────────────────
- Implemented `CombinedStatusBadge` in `OrdersList.jsx`:
  - If order status is `shipped`, `delivered`, or `canceled` -> this status takes priority.
  - Otherwise, displays `PaymentStatusBadge` for card-to-card and `StatusBadge` for online payments.
- Corrected translations: "Approved" -> "Paid", "shipped" -> "Shipped".

── Badge Styles ─────────────────────────────────────────────────────────
- Updated `badgeDefault` in `OrdersList.module.scss` to align with the blue `badgeInfo` theme.

── Modified Files ───────────────────────────────────────────────────────
- src/components/profile/OrdersList.jsx
- src/app/profile/orders/[id]/page.jsx
- src/components/profile/OrdersList.module.scss
- src/lib/adminApi.js
```

[2026-06-22] - Admin Dashboard & Orders Subsystem (100% Complete)

- Implemented `/admin/layout.jsx` and secured it using NextAuth to guard routes solely for `administrator` roles. Redirects unauthorized users seamlessly.
- Built responsive `AdminSidebar` with active link detection, using centralized modular SCSS.
- Created `/admin/page.jsx` (Dashboard) fetching stats (Users, Orders, Revenue) securely on the server via `adminApi.js`. Implemented `Graceful Degradation` using `Promise.allSettled`.
- Created reusable `StatsCard` component with dynamic accent colors and modular SCSS.
- Implemented `/admin/orders/page.jsx` fetching order history concurrently with Next.js 15 route parameters.
- Designed `OrdersTable`, `ReceiptModal`, and `StatusUpdateModal` client components for interactive order management.
- Implemented `/api/admin/orders/[id]` proxy route acting as a secure gateway, utilizing server-side JWT to securely update records and preventing client-side token exposure.
- Solved Strapi v5 `documentId` compatibility issue by forwarding `documentId` correctly across client modals and API routes, avoiding numeric ID 404 errors.
- Handled Strapi UI validation edge cases by correctly mapping unique legacy status enumerations with trailing spaces (e.g. `paid `, `shipped `).

[2026-06-x] - Product Comments Display Fix (100% Complete)
- Fixed an issue where comments were not displayed for products without categories or products only belonging to a top-level category (without subcategories).
- Imported `getComments` and `CommentsSection` to both `src/app/product/[slug]/page.js` and `src/app/products/[category]/[subcategory]/page.js`.
- Rendered the `CommentsSection` component dynamically by utilizing the `product.documentId` to query and showcase the corresponding initial comments correctly on these specific product routes.

[2026-06-x] - UI Refinements & Breadcrumb Optimization (100% Complete)
- Added missing `Breadcrumb` component to the Articles listing page (`/articles`).
- Removed redundant page titles (`<h1>`) that duplicated Breadcrumb labels across all main domain pages:
  - Products, Courses, Articles, Services, Contact, and About pages.
- Added `Breadcrumb` navigation to the Cart page (`/cart`) for both empty and populated states.
- Integrated `Breadcrumb` into the Profile layout (`/profile`), providing consistent global navigation above the sidebar.

[2026-06-x] - Font Size Variable Centralization (100% Complete)
- Refactored `src/styles/variables.css` to comprehensively centralize all font sizes.
- Mapped all hardcoded `px` and `rem` typography values into a complete spacing scale variable system (from `--font-2xs: 10px` to `--font-4xl: 64px`).
- Retained legacy alias variables (e.g. `--font-size-md`, `--font-size-xl`) to gracefully maintain backward compatibility.

[2026-06-07] - Backend Error Handling Resilience (100% Complete)
- Implemented global resilience pattern to gracefully handle Strapi backend outages without caching empty states.
- Enhanced API layer (`productsApi`, `articlesApi`, `coursesApi`, `servicesApi`, `categoriesApi`, `faqApi`, `testimonialsApi`) to catch 'BACKEND_UNAVAILABLE' and return a fallback array with an `error` property.
- Refactored `HomePage` (`app/page.js`) to use `Promise.allSettled` instead of `Promise.all`, isolating failures so one endpoint outage doesn't clear the entire page's data.
- Created `ServerErrorBlock` component (`src/components/ui/ServerErrorBlock`) featuring a user-friendly outage message and a retry/refresh action.
- Updated main page routes (`/articles`, `/products`, `/courses`, `/services`) to render the `ServerErrorBlock` and invoke `unstable_noStore()` when the backend flag is detected, preventing Next.js from caching empty states.
- Updated `Navbar` to also utilize `unstable_noStore()` on backend failure to prevent header cache poisoning.
- Refactored Home sections (`ProductsSection`, `ServicesSection`, `FaqSection`, etc.) to accept a `serverError` boolean prop (avoiding JSON serialization loss of the array `.error` property) and conditionally render a matching inline outage message.

[2026-05-23] - Dark/Light Theme Switcher Feature (100% Complete)
 
─── FRONTEND SYSTEM ───────────────────────────────────────────────────────────
 
| Styles: /src/app/layout.js & /src/styles/light-theme.css & /src/styles/variables.css
|- Created light mode system under data-theme="light" with full color variable inversion
|- Refined background to high-contrast Sage Pastel (#F6F8F3) and card overlay panels to pure white (#FFFFFF)
|- Overhauled font color (--color-white equivalent variables) to premium emerald-black (#0c1815)
|- Reflowed lower opacity variables (--color-bg-primary-op-XX and --color-white-op-XX) to use dark-translucent colors (rgba(12, 24, 21, opacity)) over light backgrounds to keep layout contrast pristine
|- Upgraded gold accents (--color-gold / --color-title-hover) to vibrant premium hues (#C79A2B / #E4BE63)
|- Added a render-blocking synchronous inline <script> in layout.js <head> to retrieve theme state from localStorage before first frame paint, completely eliminating Hydration Flash (FOUC)
 
| Navbar: /src/modules/layout/Navbar/NavbarClient.jsx
|- Integrated theme toggle state utilizing localStorage and state syncing
|- Added an interactive switcher button to the header with animations and dynamic moon/sun indicators
 
─── FILES MODIFIED ───────────────────────────────────────────────────────────
- front-end/src/styles/light-theme.css (new)
- front-end/src/app/layout.js
- front-end/src/modules/layout/Navbar/NavbarClient.jsx
- front-end/src/styles/variables.css
 
##latest- 2026-05-21

### Features & Security: Comments API Proxy & Name Field Support
**Status**: ✅ COMPLETED (100%)

#### Overview:
Revamped the comments posting and rendering pipeline by resolving backend 403/401 token authentication errors. Transitioned comment submission from direct client-to-Strapi calls to an internal Next.js server-side API proxy method utilizing elevated admin tokens. Users are now properly enforced to login before commenting, and an extra, fully-functional "Name" field was explicitly integrated across the frontend comment UI and Strapi payload mappings for a personalized review experience.

#### Changes Made:
1. **Secure API Proxy Pattern**:
   - Built a new reliable API Proxy endpoint for comments at `src/app/api/comments/route.js`.
   - Updated `submitComment` function (`commentsApi.js`) to target the local proxy rather than directly communicating with the Strapi API endpoint to avoid tricky JWT and 401 Unauthorized limitations.
2. **Comment Submissions & Auth Binding**:
   - Refactored `CommentsSection.jsx` to parse and require `next-auth/react` `useSession()` user logins prior to triggering the submit functionality.
   - Tied backend comment entities reliably to their author by mapping `dataPayload.user` using the authenticated NextAuth user ID.
3. **Robust Name Field Integration**:
   - Introduced a new required `name` parameter logic seamlessly passed from UI inputs to the server requests and directly injected it toward the database schema payload.
   - Crafted and linked a matching styled input block layout (`.inputStyle`) into `CommentsSection.module.scss`.
   - Updated `.map` extractions in `formatComments` (within `commentsApi.js`) and `<CommentItem />` rendering logic precisely to show custom entered names (overriding `user.username` relation if present).

---

[2026-05-19] - Card-to-Card Payment Feature Implementation (100% Complete)

─── BACKEND (Strapi v5) ───────────────────────────────────────────────────────

| Schema: Order Content-Type
|- Added field: paymentMethod (enum: online | card_to_card)
|- Added field: paymentStatus (enum: pending_payment | pending_verification | paid | failed)
|- Added field: receiptImage (single media relation)
|- Added field: trackingNumber (string)
|- Added field: cardHolderName (string) 


| Schema: bank-setting (new Single Type)
|- Created with fields: bankName (string), cardNumber (string), accountHolder (string)
|- Used to store store bank account info displayed to card-to-card customers

─── API PROXY ROUTES (Next.js 15) ────────────────────────────────────────────

| /src/app/api/orders/route.js — GET handler fix
|- Added ?documentId= query param support so /profile/orders/[id] can fetch a single order
|- Added ownership verification: filters by both documentId and session user.id
|- Previously: GET handler ignored all query params and always returned all orders

| /src/app/api/orders/route.js — POST handler fix
|- card_to_card orders now set orderStatus: 'pending' (was incorrectly 'paid ' for all orders)
|- online orders continue to use orderStatus: 'paid '
|- paymentMethod and paymentStatus fields now correctly forwarded to Strapi payload

| /src/app/api/orders/upload-receipt/route.js — new server-side proxy
|- Accepts multipart/form-data with: orderId, file, trackingNumber, cardHolderName
|- STEP 1: validates file type (JPEG/PNG/WebP/GIF), size (max 5MB), and required text fields
|- STEP 2: uploads image to Strapi /api/upload using server-side STRAPI_API_TOKEN (never exposed to client)
|- STEP 3: PATCHes the order via documentId — sets receiptImage, trackingNumber, cardHolderName, paymentStatus → 'pending_verification'
|- Returns { success: true, order: { documentId, paymentStatus, trackingNumber } }

─── FRONTEND COMPONENTS ──────────────────────────────────────────────────────

| /src/components/checkout/PaymentStep.jsx
|- Added "پرداخت کارت به کارت" radio option alongside online payment
|- Added "بدون کارمزد" badge on the card-to-card option
|- Submit button label changes to "ثبت نهایی سفارش" when card-to-card is selected
|- Post-order redirect for card_to_card: /payment/callback?status=success&source=card_to_card&orderId=[documentId]
|- Post-order redirect for online: /payment/callback?status=success
|- Added inline error state display via errorMessage

| /src/components/checkout/PaymentStep.module.scss
|- Added .methodBadge class for the "بدون کارمزد" inline badge
|- Added .errorBox class for inline submission error display

| /src/components/profile/OrderReceiptUpload.jsx — new component
|- Fetches bank-setting data from Strapi (bankName, cardNumber, accountHolder)
|- Displays bank info with a one-click copy button for card number (navigator.clipboard)
|- Form fields: trackingNumber (text), cardHolderName (text), receiptImage (file)
|- Drag-and-drop zone + hidden file input for image upload
|- Client-side validation: MIME type, max 5MB, required text fields
|- Submits via POST multipart/form-data to /api/orders/upload-receipt proxy
|- Shows success message and calls onSuccess() callback after confirmed upload
|- BUG FIX: moved hidden <input type="file"> outside the clickable dropzone <div> to prevent double file-picker open caused by click event bubbling

| /src/components/profile/OrdersList.jsx
|- Added PaymentStatusBadge sub-component for card_to_card paymentStatus display
|- StatusBadge now handles 'paid ' (with trailing space) Strapi enum correctly
|- For card_to_card orders: only PaymentStatusBadge shown (not StatusBadge) to avoid redundant "در انتظار پرداخت" badge after status advances
|- For card_to_card with paymentStatus='pending_payment': shows link "آپلود فیش پرداخت" → /profile/orders/[documentId]
|- For card_to_card with other paymentStatus: shows link "مشاهده جزئیات" → /profile/orders/[documentId]
|- For non-card_to_card orders: shows modal button "مشاهده جزئیات" (unchanged)

| /src/components/profile/OrdersList.module.scss
|- Added .orders__badgePending (indigo/purple) for pending_verification paymentStatus

| /src/app/profile/orders/[id]/page.jsx — new dynamic page
|- Fetches single order by documentId via /api/orders?documentId=[id]
|- Conditionally renders <OrderReceiptUpload> if paymentMethod=card_to_card and paymentStatus=pending_payment
|- Hides upload form and shows success message after onSuccess callback fires
|- Added console.log of raw orderData for development diagnostics

| /src/app/payment/callback/page.jsx
|- Reads ?source=card_to_card and ?orderId= from URL (via window.location.search after mount)
|- card_to_card: shows clock icon, amber/gold styling, title "در انتظار پرداخت",
     message guiding user to upload receipt, button "رفتن به سفارش و آپلود فیش" → /profile/orders/[orderId]
|- online: shows green checkmark icon, title "پرداخت موفق!", standard success message
|- Root fix: uses window.location.search (not searchParams hook) after mount to guarantee params
     are read from the real browser URL and not a stale SSR/hydration snapshot
|- Removed unused useRouter import

| /src/app/payment/callback/page.module.scss
|- Added .pending class: amber/gold icon ring, gold title color, amber gradient primary button
|- Added @keyframes tickPulse: pulsing ring animation on the clock icon

| /src/app/checkout/page.jsx
|- REMOVED useEffect that called router.replace('/payment/callback?status=success') when totalPrice===0
|- This effect was firing immediately after clearCart() (triggered by PaymentStep) and overwriting
     the ?source=card_to_card URL that PaymentStep had just built — causing card_to_card users
     to always see the online-payment success screen

─── FILES MODIFIED ───────────────────────────────────────────────────────────
- front-end/src/app/api/orders/route.js
- front-end/src/app/api/orders/upload-receipt/route.js  (new)
- front-end/src/components/checkout/PaymentStep.jsx
- front-end/src/components/checkout/PaymentStep.module.scss
- front-end/src/components/profile/OrderReceiptUpload.jsx  (new)
- front-end/src/components/profile/OrdersList.jsx
- front-end/src/components/profile/OrdersList.module.scss
- front-end/src/app/profile/orders/[id]/page.jsx  (new)
- front-end/src/app/payment/callback/page.jsx
- front-end/src/app/payment/callback/page.module.scss
- front-end/src/app/checkout/page.jsx
- backend/src/api/order/content-types/order/schema.json
- backend/src/api/bank-setting/content-types/bank-setting/schema.json  (new)

##latest- 2026-05-18

### Security & UX: Secure Course Access & Dynamic Media Gating
**Status**: ✅ COMPLETED (100%)

#### Overview:
Implemented a robust, high-security course access control system to protect premium and free resources. Restricts all media links dynamically on the server based on authenticated purchase status, completely preventing inspect-element leaks. Enhances the frontend playlist UX with strict lock guards, auto-login modals, immediate cart integration, and seamless router hydration upon authentication.

#### Changes Made:
1. **Server-Side Media Decryption & Protection** (`src/app/courses/[slug]/page.js`):
   - Integrated secure server-side purchase verification using the Strapi admin API. If the user is unauthenticated or hasn't purchased the course, all dynamic `videoUrl` and `audioUrl` fields are completely stripped and replaced with `null` before React rendering.
   - This prevents unpaid users from stealing media links via Inspect Element, Network tools, or the serialized `__NEXT_DATA__` JSON hydration payload.
   - Resolved dynamic page rendering crash by fixing a `ReferenceError` regarding `initialComments`.
2. **Interactive Playlist Access Control** (`CourseContentManager.jsx`):
   - Refined the curriculum list logic so all premium lessons are completely locked (🔒 icon, low opacity) for unauthenticated or unpaid users.
   - Updated the click handler (`handleLessonClick`) to strictly enforce the locked state, blocking playback and triggering either the `AuthModal` (for unlogged users) or a purchase alert.
   - Integrated `useOrdersStore` from Zustand inside the component to track bought items seamlessly and reactively.
   - Set up an auto-refresh listener using Next.js `router.refresh()` to immediately hydration-update the page and retrieve media links once the user logs in.
3. **Inline Call-To-Action (CTA) Integration** (`CourseContentManager.jsx` & `AddToCartButton.jsx`):
   - Developed a placeholder view inside the player section for paid but unpurchased courses, showing "برای مشاهده این دوره باید آن را خریداری کنید" and the restored `AddToCartButton`.
   - Restored and uncommented the `AddToCartButton.jsx` component to allow users to directly add the course to the cart.
4. **Enhanced Course Cards Purchase Badges** (`CourseCard.jsx` & `CourseCard.module.scss`):
   - Integrated the Zustand order store in the main listing view to check if the current user purchased a course.
   - Displays a green "خریداری شده" (Purchased) badge and changes the "بیشتر بدانید" CTA to "مشاهده دوره" for students, dramatically improving the course catalog experience.
5. **Real-time Orders List Refresh & Cache Eviction** (`page.jsx`, `OrdersList.jsx`, & `PurchasesList.jsx`):
   - Integrated the `useOrdersStore` in the payment success callback page (`/payment/callback/page.jsx`) to automatically clear and reset the cached orders store (`hasFetched: false`, `orders: []`) upon successful payment.
   - Updated the mounting `useEffect` in `OrdersList.jsx` and `PurchasesList.jsx` to call `fetchOrders(true)` with the force parameter enabled.
   - This ensures that whenever a user navigates to their profile orders or purchases tab after a successful checkout, the newly purchased orders/courses are fetched and displayed in real-time without requiring a manual browser refresh.

---

### Feature & Bug Fix: Complete Order History System & Nested Routing
**Status**: ✅ COMPLETED (100%)

#### Overview:
Finalized the integration of a robust, secure order history feature connecting Strapi v5 dynamic zones to the Next.js user profile dashboard. Resolved strict Strapi schema validation errors, ensured accurate item metadata persistence (saving product/course attributes directly to the invoice), and solved complex nested routing constraints for clickable product links within the order details modal.

#### Changes Made:
1. **Dynamic Zone Schema Expansion** (`backend/src/components/order/...`):
   - Added `title`, `price`, `courseId`/`productId`, `slug`, and `itemUrl` directly to the `course-order-item.json` and `product-order-item.json` component schemas in Strapi. This ensures all purchased item metadata is immutably captured at checkout and prevents missing data on deleted products.
2. **Enhanced API Proxy Logic** (`src/app/api/orders/route.js`):
   - Implemented dynamic `itemUrl` construction utilizing `categorySlug` and `subcategorySlug` stored within the cart state to construct the correct canonical nested routes (e.g. `/products/books/spiritualbooks/product-8`) before sending payload to Strapi.
3. **Robust Key Iteration & React Fixes** (`OrderDetailsModal.jsx`):
   - Resolved `Encountered two children with the same key` console errors by combining the iterator index (`idx`) to generate a guaranteed unique key for dynamic zone components rather than relying on component DB IDs.
4. **Order History UI - Clickable Modals** (`OrderDetailsModal.jsx` & `.module.scss`):
   - Wrapped entire order items (icon, title, badges, pricing) inside Next.js `<Link>` components pointing to the preserved `itemUrl`.
   - Applied specific `.modal__itemClickable` SCSS properties including smooth pointer transitions, background hover states, and golden text glow on the title when the row is hovered to dramatically improve UX.
5. **Unified Global Caching Store** (`src/store/useOrdersStore.js`):
   - Created a Zustand-powered caching state manager for orders (`useOrdersStore`). This ensures order history and purchased items are fetched exactly once and reused across different tabs inside the user profile, completely avoiding redundant backend requests.
6. **"My Purchases" Unified View & Caching Integration** (`src/app/profile/purchases/...` & `src/components/profile/PurchasesList.jsx`):
   - Created a new navigation link called **"سفارش های من"** (My Orders) in the `ProfileSidebar.jsx` that routes to `/profile/purchases`.
   - Developed `PurchasesList.jsx` which flattens the items from all orders, automatically deduplicates items (and aggregates total quantity bought for physical products), and visually splits them into separate sections: "دوره‌های آموزشی" (Courses) and "محصولات فیزیکی" (Physical Products).
   - Styled the items using the exact 5-column CSS Grid layout from the checkout cart (`Cart.module.scss`), substituting the deletion action with an elegant custom "مشاهده" (View) button that redirects users directly to their purchased content.
   - Standardized the physical product fallback route to use `/product/[slug]` rather than `/products/[slug]`, ensuring old order items (which lack a saved `itemUrl` in the database) safely trigger Next.js canonical category routing redirects instead of hitting a blank page.

---

### Feature & Optimization: Free Courses Instant Enrollment & UI Refinements
**Status**: ✅ COMPLETED (100%)

#### Overview:
Streamlined the enrollment process for free courses (total order price = 0) by completely bypassing shipping/address entry and payment method selection steps. Once a user logs in (or if they are already logged in), they are immediately redirected to the payment success callback. Additionally, multiple UI refinements and consistency updates were applied across Course Cards, Course Detail Pages, Cart, Checkout Steps, and Payment Callbacks.

#### Changes Made:
1. **Instant Enrollment for Free Courses** (`src/app/checkout/page.jsx` & `src/components/checkout/AuthStep.jsx`):
   - Implemented a `useEffect` inside `CheckoutPage` to automatically redirect users to `/payment/callback?status=success` if the order is free (`totalPrice === 0`) and they are already authenticated (`status === 'authenticated'`).
   - Updated `AuthStep` to receive the `totalPrice` prop and redirect directly to `/payment/callback?status=success` after a successful login if the order is free, completely skipping subsequent checkout steps.

2. **Integration of "Add to Cart" on Course Pages** (`src/app/courses/[slug]/page.js`, `CourseCard.jsx`, & `AddToCartButton.jsx`):
   - Safely commented out the old inline cart/hydration logic (lines 123-162) in `CourseCard.jsx` to clean up old direct shopping actions and consistently show the "بیشتر بدانید" CTA button.
   - Built a robust, reusable `AddToCartButton` Client Component (`src/components/ui/AddToCartButton/AddToCartButton.jsx`) designed to safely interact with Zustand and `localStorage` using a hydration-safe state pattern to prevent SSR mismatch errors.
   - Replaced the static "ثبت‌نام در دوره" registration `Link` on the Course Detail Page (`src/app/courses/[slug]/page.js`) with the new dynamic `AddToCartButton` component, passing dynamic course properties to handle full cart additions.

3. **Cart & Checkout Pricing UI Adjustments** (`src/app/cart/page.jsx` & `CartReviewStep.jsx`):
   - Cleaned up redundant price breakdowns by commenting out the `جمع جزء` (subtotal) row in both the Cart Sidebar and Checkout Step 1 (Cart Review), showing only the `جمع کل` (total price) for a cleaner UI.

4. **Premium Visual Styling & Layout Enhancements** (Multiple `.module.scss` files):
   - Applied consistent `border-radius: 12px` to the checkout components (`CartReviewStep`, `ShippingStep`, `AuthStep`, `PaymentStep`) and the final Payment Callback Card (`page.module.scss`) to match the premium design system.
   - Updated `.cartItem` background on the Cart Page to use `var(--gradient-card-horizontal-rtl)` for a cohesive horizontal flow.
   - Adjusted item totals and summary total font sizes (`--font-lg` & `--font-md` respectively) for better typography hierarchy in `Cart.module.scss` and `PaymentStep.module.scss`.
   - Commented out the redundant `انتخاب روش پرداخت` title in `PaymentStep.jsx`.

---

## [2026-05-17]

### Enhancement: Interactive Course Player Styling & Auto-Scroll
**Status**: ✅ COMPLETED (100%)

#### Overview:
Enhanced the user interface and functionality of the course video/audio player (`CourseContentManager.jsx`), providing a more seamless, integrated design between the media player and the curriculum list. Customized the default Plyr styling to match the site's premium dark and golden aesthetics and implemented automatic scrolling for the curriculum list.

#### Changes Made:
1. **Course Content Manager Layout** (`src/app/courses/[slug]/page.module.scss`):
   - Merged the `.playerSection` and `.curriculumSection` inside a single unified `.contentManager` wrapper.
   - Applied a unified `border` and `var(--color-overlay)` background with `16px` border-radius to the entire wrapper.
   - Replaced `.activeTitle` with `.sectionTitle` to ensure typography consistency between the "Now Playing" and "Course Syllabus" headers.
   - Added appropriate `padding` to the curriculum section to separate it from the new wrapper border.

2. **Lesson List Styling & Auto-Scroll** (`src/modules/courses/CourseContentManager.jsx` & `page.module.scss`):
   - Restricted the `.lessonList` to a fixed height of `290px` with `overflow-y: auto` and `overflow-x: hidden`.
   - Designed a custom, elegant scrollbar (`::-webkit-scrollbar`) styled with the site's primary golden colors.
   - Added `useRef` to the lesson list and implemented a `scrollToItem` function in `CourseContentManager.jsx`.
   - Now, when a lesson is clicked, the list automatically and smoothly scrolls to position the active lesson exactly `10px` from the top of the container.

3. **Plyr Player Theming** (`src/app/courses/[slug]/page.module.scss`):
   - Overrode Plyr CSS variables globally for `.plyr` components inside the player wrapper.
   - Set `--plyr-color-main` to `var(--color-primary, #f6d982)`.
   - Set control backgrounds to `var(--color-overlay, #111)` with `16px` border-radius and customized hover effects.
   - Styled volume slider, progress bar, and play buttons to perfectly match the application's golden/dark theme.

---

### Feature: Complete Interactive Course Player System
**Status**: ✅ COMPLETED (100%)

#### Overview:
Designed and implemented a full-stack, robust, and interactive course player system. This includes fetching curriculum data from Strapi, enforcing access controls (Free vs. Premium), handling multiple media types (Direct Video/Audio, Aparat Embeds), bypassing browser CSP constraints, and persistently saving user playback progress.

#### Changes Made:

1. **API Integration & Bug Fixes** (`apiClient.js` & `strapiUtils.js`)
   - **Fixed Next.js Caching Bug (400 Errors)**: Cleared the `.next` cache and implemented workarounds for Next.js persistently sending outdated query parameters to Strapi.
   - **Data Mapping**: Updated `formatStrapiCourses` in `strapiUtils.js` to correctly map the `curriculum` array (including `videoUrl`, `audioUrl`, `isFree`, `duration`) from the raw API response to the frontend components.

2. **Access Control Guards** (`CourseContentManager.jsx`)
   - Integrated `next-auth/react` to verify authentication status.
   - **Free Sessions**: Allows anyone (or logged-in users, based on requirements) to play sessions marked as `isFree: true`.
   - **Premium Sessions**: Verifies if the currently logged-in user owns the specific course (`session.user.courses.some(...)`).
   - Visually locks inaccessible sessions in the playlist (🔒 icon) and shows browser alerts if a user tries to play them without permission.

3. **Content Manager Structure & Layout** (`CourseContentManager.jsx` & `page.module.scss`)
   - Built a dynamic layout featuring a **Player Section** on top and a **Playlist Section** (Curriculum) at the bottom.
   - The Player Section is completely hidden until a user selects a session to avoid empty boxes.
   - Active lessons are highlighted in the playlist with dynamic CSS (`clsx`).

4. **Dual Player Architecture** (`CourseContentManager.jsx`)
   - Implemented a smart media toggle to switch between Video and Audio modes when a lesson provides both formats.
   - Defaults to Audio playback if audio is present.
   - Supports 3 distinct playback methods dynamically:
     - **Aparat Embeds**: Renders `iframe` for Aparat video links with responsive 16:9 wrappers.
     - **Video.js**: Handles direct `.mp4` video files with default styling (`VideoJSPlayer.jsx`).
     - **Plyr**: Handles direct `.mp3` audio files, replacing Video.js for audio to provide a better native audio UI (`PlyrAudioPlayer.jsx`).

5. **Playback Progress Persistence** (`VideoJSPlayer.jsx` & `PlyrAudioPlayer.jsx`)
   - Implemented `localStorage` syncing for playback time uniquely per course and lesson.
   - Automatically saves progress every 5 seconds (`timeupdate` event on 5-second multiples).
   - Instantly saves exact progress upon `pause` or `stop`.
   - Automatically resumes from the saved timestamp upon revisiting the lesson.

6. **CORS & CSP Resolution (Media Proxy)** (`src/app/api/media/[...path]/route.js`)
   - Fixed an issue where Strapi's CSP header (`media-src 'self'`) forced browsers to download audio files instead of streaming them.
   - Created a Next.js API route (`/api/media`) that proxies file requests to Strapi.
   - Maintains byte-range support for seeking inside media files.
   - Updated `strapiUtils.js` to automatically rewrite Strapi `/uploads/` URLs to the Next.js `/api/media/uploads/` proxy.

#### Files Created/Modified:
- `src/lib/apiClient.js` & `src/app/courses/[slug]/page.js`: Fixed API fetching and curriculum prop passing.
- `src/app/courses/[slug]/page.module.scss`: Styling for layout, access control badges, and responsive iframe wrappers.
- `src/modules/courses/CourseContentManager.jsx`: Added core logic for access control, state management, and media toggling.
- `src/modules/courses/PlyrAudioPlayer.jsx`: Created new component for audio-specific playback.
- `src/modules/courses/VideoJSPlayer.jsx`: Created and later refined resume logic for video playback.
- `src/app/api/media/[...path]/route.js`: Created Next.js media proxy.
- `src/lib/strapiUtils.js`: Implemented URL rewriting for proxied media and curriculum mapping.

---

## [Previous] - 2026-02-24
### Bug Fix: Strapi Error Resilience & Logo Asset
**Status**: ✅ COMPLETED (100%)

#### Overview:
Resolved critical visual and stability issues by implementing soft-failure logic for Strapi connection errors and fixing asset path casing.

#### Changes Made:
1. **Home Page Error Resilience** (`src/app/page.js`)
   - Wrapped the main data fetch in a `try...catch` block to prevent site-wide 500 crashes when the Strapi backend is unreachable.
   - Implemented default empty array values for all sections (`categories`, `products`, `services`, etc.).
   - This ensures the Hero and layout render correctly even if data is missing, allowing components to show their own "no items" states gracefully.
2. **Logo Filename Case Sensitivity** (`src/modules/layout/Navbar/NavbarClient.jsx`)
   - Fixed a 404 error where the logo was referenced as `Logo.png` (uppercase) while the file on disk was `logo.png` (lowercase).
   - This ensures the logo displays correctly on case-sensitive hosting environments (Linux).

---

### Bug Fix: Environment Variable Build Safety
**Status**: ✅ COMPLETED (100%)

#### Problem:
Encountered `TypeError: Cannot read properties of undefined (reading 'replace')` during the build process. This occurred because `process.env.NEXT_PUBLIC_STRAPI_API_URL` was being accessed without a safety check in API routes.

#### Changes Made:
- **API Route Hardening** (`src/app/api/addresses/route.js`):
    - Added a fallback empty string to the `STRAPI_URL` definition: `(process.env.NEXT_PUBLIC_STRAPI_API_URL || '').replace('/api', '')`.
    - This ensures the build process doesn't crash if environment variables are missing or not yet loaded.

---

### Architecture: Standardized Global CSS Variables
**Status**: ✅ COMPLETED (100%)

#### Overview:
Resolved the "Selector :root is not pure" build error by migrating CSS custom properties from SCSS files to a dedicated global CSS file. This architectural change ensures that design tokens are loaded globally without being incorrectly processed as local CSS Modules.

#### Changes Made:

1. **Global Variables Core** (`src/styles/variables.css`)
   - Created a new pure CSS file dedicated to `:root` definitions.
   - Moved all color, typography, spacing, and shadow variables here.
   - This file is treated as "Global CSS" by Next.js, bypassing CSS Modules purity checks.

2. **SCSS Variables Cleanup** (`src/styles/base/_variables.scss`)
   - Removed the `:root` block to prevent duplicate definitions in SCSS context.
   - Added documentation explaining the new variable management strategy.
   - Kept the file for future SCSS-specific variables if needed.

3. **Layout Integration** (`src/app/layout.js`)
   - Imported `variables.css` as the very first styling import in the root layout.
   - Ensures CSS variables are available to all components before any other styles are loaded.

#### Benefits:
- ✅ **Build Error Fixed**: Eliminated the "Selector :root is not pure" error in Turbopack/Webpack.
- ✅ **Architecture**: Clear separation between design tokens (Global CSS) and component styles (CSS Modules).
- ✅ **Reliability**: Direct CSS injection ensures variables are always available in the DOM.
- ✅ **Maintainability**: Centralized source of truth for the entire design system.

### Maintenance: Strapi API URL Safety & Build Stability
**Status**: ✅ COMPLETED (100%)

#### Overview:
Refactored Strapi API URL handling to prevent build-time crashes and ensured compatibility with Next.js dynamic route requirements.

#### Changes Made:
1. **Build Safety (API Proxy Routes)**
   - Added fallbacks `|| 'http://127.0.0.1:1337'` before string manipulation (`.replace()`) on `NEXT_PUBLIC_STRAPI_API_URL`.
   - This prevents `TypeError: Cannot read properties of undefined (reading 'replace')` during build or SSR when environment variables are unavailable.
   - Affected: `src/app/api/profile/route.js`, `src/app/api/addresses/route.js`, `src/app/api/addresses/[id]/route.js`.
2. **Auth Library Robustness**
   - Applied consistent fallback logic to `STRAPI_API_URL` in `src/lib/auth.js`.
3. **Next.js Dynamic Route Compliance**
   - Verified that dynamic API routes await `params` before accessing IDs, adhering to Next.js requirements.

#### Files Modified:
- `src/app/api/profile/route.js`
- `src/app/api/addresses/route.js`
- `src/app/api/addresses/[id]/route.js`
- `src/lib/auth.js`

---
---

### Styling: Sass Refactoring & Font Build Fixes
**Status**: ✅ COMPLETED (100%)

#### Overview:
Resolved critical build failures related to the modern Sass module system and font asset resolution. Standardized the project to use a single, reliable font loading system.

#### Changes Made:

1. **Sass Module Dependencies** (`src/styles/components/`)
   - Added explicit `@use '../base/mixins' as *;` imports to `_cards.scss`, `_containers.scss`, and `_sections.scss`.
   - Fixed "Undefined mixin" errors where `hover-transition` and `respond` were missing during compilation.

2. **Font Asset Resolution & OS Compatibility**
   - **Case Sensitivity**: Corrected font filenames (e.g., `LALEZAR-REGULAR.TTF`) to ensure compatibility with case-sensitive Linux build environments.
   - **Path Standardisation**: Fixed SCSS `url()` resolution by using correct relative paths for assets.

3. **NextFont Cleanup** (`src/app/layout.js`)
   - Removed redundant `localFont` configurations that were conflicting with Turbopack.
   - Resolved "Unknown font file extension" errors caused by uppercase file extensions in `next/font`.
   - Centralised all font loading through the global `@font-face` definitions in `src/styles/base/_fonts.scss`.

### Refactor: Project-wide Sass @use Migration
**Status**: ✅ COMPLETED (100%)

#### Overview:
Migrated the entire styling architecture from legacy `@import` to the modern Sass `@use` module system. This resolves Sass deprecation warnings and ensures full compatibility with Next.js Turbopack.

#### Changes Made:
1. **Global Styles Refactor** (`src/styles/main.scss`):
   - Converted the main stylesheet to use `@use` and `@forward` patterns.
   - Base mixins and variables are now shared using `as *` to maintain global accessibility.
2. **Project-wide Module Migration** (52 files):
   - Replaced all instances of `@import` with `@use` in all `.module.scss` and page-level SCSS files across the project.
   - Updated paths to use both `@/styles/` aliases and relative paths where appropriate.
   - Verified that all `@use` declarations are at the absolute top of files to meet Sass compiler requirements.
3. **Redundancy Cleanup**:
   - Removed legacy imports that were causing duplicate symbol errors and "Undefined mixin" warnings during build.

#### Benefits:
- ✅ **Future-Proof**: Complies with the modern Sass module specification.
- ✅ **Turbopack Ready**: Fixes internal Next.js build errors related to CSS evaluation.
- ✅ **Reliability**: Ensures that mixins like `respond` and `hover-transition` are correctly scoped and available in all modules.

---

## [2025-12-31]

### Feature: Persistent Cart Sync & Commerce Loop (Commerce Loop)
**Status**: ✅ COMPLETED (100%)

#### Overview:
Successfully implemented a critical part of the Commerce Loop using a Proxy architecture. The cart has been transformed from a temporary browser-based state to a persistent database-backed state, ensuring data retention across different devices and browsers.

#### Changes Made:

1. **Security and Proxy Layer (Next.js API Proxy)**
   - **Route**: `src/app/api/profile/route.js`
   - **GET**: Fetches `cartData` from Strapi alongside the user profile.
   - **PUT**: Accepts `cartData` from the frontend and updates Strapi using the Admin Token (bypassing 401/403 restrictions on direct user updates).

2. **Client Intelligence Layer (Custom Sync Hook)**
   - **Custom Hook**: `src/hooks/useCartSync.js`
   - **Debounce Save**: Implemented a 2-second timer to prevent server spamming; saves only after inactivity.
   - **Auto-Hydration**: Checks on login if the local cart is empty. If so, fetches the last saved cart from the DB and injects it into the Zustand store.

3. **Global Connection Layer (Global Provider)**
   - **Provider**: `src/components/layout/CartSyncProvider.jsx`
   - **Integration**: `src/app/layout.js`
   - Created `CartSyncProvider` (Client Component) to execute the sync hook.
   - Integrated into the root layout to ensure the sync process runs in the background immediately upon site entry.

#### Files Created/Modified:
- `context/CONTEXT_CartSync.md`
- `src/app/layout.js`
- `src/app/api/profile/route.js`
- `src/components/layout/CartSyncProvider.jsx`
- `src/hooks/useCartSync.js`

#### Result:
- ✅ **Cloud-Synced Cart**: User's cart is fully synced with the cloud.
- ✅ **Cross-Device Persistence**: Add products on mobile, complete purchase on laptop.
- ✅ **Seamless Experience**: No data loss when switching contexts.

### Bug Fix: Product Card Quantity Reactivity
**Status**: ✅ COMPLETED (100%)

#### Problem:
Clicking the `+` button on a product card after adding it to the cart did not update the displayed quantity number, even though the internal logic was executing.

#### Diagnosis:
The `useCartStore` was mutating item objects directly within the state array. React and Zustand rely on immutable updates (reference changes) to detect when to re-render components. Because the object reference didn't change, the `ProductCard` component didn't know it needed to update.

#### Solution:
Refactored `addItem` and `updateQuantity` in `src/store/useCartStore.js` to use immutable update patterns (creating shallow copies of the item objects before modifying them).

#### Result:
- ✅ **UI Updates**: Product quantity now updates immediately when clicking `+` or `-`.
- ✅ **Code Quality**: Enforced immutable state best practices.

---

## [2025-12-24]

### Refactor: Services Pagination Standardization
**Status**: ✅ COMPLETED (100%)

#### Overview:
Standardized the services pagination to display 3 services initially and load 3 more with each "Load More" action. Previously, the system had inconsistent values (2 in ServiceGrid, 6 in constants), which has been unified across all layers.

#### Changes Made:

1. **Constants File** (`src/lib/constants.js`)
   - Updated `SERVICES_PAGE_SIZE` from `6` to `3`
   - This constant is now the single source of truth for services pagination

2. **ServiceGrid Component** (`src/modules/services/ServiceGrid/ServiceGrid.jsx`)
   - Removed local `PAGE_SIZE = 2` constant
   - Imported and used `SERVICES_PAGE_SIZE` from `@/lib/constants`
   - Updated `hasMore` state initialization to use `SERVICES_PAGE_SIZE`
   - Updated fetch URL to use `SERVICES_PAGE_SIZE` in pageSize parameter

3. **Services Page** (`src/app/services/page.js`)
   - Updated SSR initial fetch from `getServicesPaginated(1, 2, ...)` to `getServicesPaginated(1, 3, ...)`
   - Ensures initial server-side render shows exactly 3 services

4. **Services API Route** (`src/app/api/services/route.js`)
   - Updated default pageSize from `'6'` to `'3'`
   - Ensures consistency when pageSize is not explicitly provided

#### Technical Details:
```javascript
// Before:
const PAGE_SIZE = 2; // Local constant in ServiceGrid
export const SERVICES_PAGE_SIZE = 6; // Unused in constants

// After:
export const SERVICES_PAGE_SIZE = 3; // Single source of truth
import { SERVICES_PAGE_SIZE } from '@/lib/constants'; // Used everywhere
```

#### Benefits:
- ✅ **Consistency**: All layers (SSR, Client, API) use the same page size
- ✅ **Maintainability**: Single constant to change pagination behavior
- ✅ **Performance**: Optimized initial load with 3 items instead of 2 or 6
- ✅ **User Experience**: Balanced between initial load speed and content visibility
- ✅ **Architecture**: Follows DRY principle with centralized constants

---

## [2025-12-24]

### Refactor: HakimElahiSection Standardization
**Status**: ✅ COMPLETED (100%)

#### Overview:
Refactored the `HakimElahiSection` component to match the exact structure and styling of `AboutMentorSection`, ensuring visual consistency across home page sections. This standardization improves maintainability and provides a unified design language.

#### Changes Made:

1. **HakimElahiSection Component** (`src/modules/home/HakimElahiSection/HakimElahiSection.jsx`)
   - Added `GradientBorderCard` wrapper with `gradient="horizontal"`, `enableHover={false}`, `variant="aboutMentor"`
   - Changed section class from `hakimElahiSection` to `aboutSection` (matching AboutMentor)
   - Added section `id="hakim-elahi"` for anchor navigation
   - Reordered layout: contentWrapper (left) → imageWrapper (right)
   - Replaced Next.js `<Image>` component with regular `<img>` tag
   - Removed CTA button to match AboutMentor structure
   - Applied flex layout wrapper for content positioning

2. **HakimElahiSection Styles** (`src/modules/home/HakimElahiSection/HakimElahiSection.module.scss`)
   - Changed class name from `.hakimElahiSection` to `.aboutSection`
   - Updated `.container` from CSS Grid to Flexbox layout
   - Set width percentages: contentWrapper (65%), imageWrapper (37%)
   - Added `var(--color-overlay)` background to imageWrapper
   - Positioned image absolutely at bottom with `left: 10px`
   - Updated title font to `'Far Shafigh'` with `font-weight: 400`
   - Applied consistent padding: `32px 50px 50px 50px` to contentWrapper
   - Updated button gradient: `linear-gradient(135deg, #F6D982 0%, #F5C452 100%)`
   - Maintained responsive breakpoints for mobile layout

#### Technical Details:
```jsx
<GradientBorderCard gradient="horizontal" enableHover={false} variant="aboutMentor">
  <div style={{ display: 'flex' }}>
    <div className={styles.contentWrapper}>
      {/* Content */}
    </div>
    <div className={styles.imageWrapper}>
      <img src="/images/hakimelahi.png" alt="hakimElahiImage" />
    </div>
  </div>
</GradientBorderCard>
```

#### Benefits:
- ✅ **Visual Consistency**: Identical structure to AboutMentorSection
- ✅ **Maintainability**: Shared styling patterns reduce code duplication
- ✅ **Design System**: Follows established component architecture
- ✅ **Responsive**: Consistent mobile behavior across sections
- ✅ **RTL Support**: Proper right-to-left layout handling

---

## [2025-12-24]

### Feature: BaseSlider Conditional Navigation
**Status**: ✅ COMPLETED (100%)

#### Overview:
Enhanced the `BaseSlider` component to intelligently show/hide navigation buttons based on whether the slider actually needs scrolling. Previously, navigation buttons were always visible even when the number of items was less than the viewport capacity, creating unnecessary UI clutter.

#### Changes Made:

1. **BaseSlider Component** (`src/components/layout/BaseSlider/BaseSlider.jsx`)
   - Added `showNavigation` state to track if navigation is needed
   - Implemented logic to check if `slides.length > slidesPerView`
   - Navigation buttons now conditionally hidden via CSS class instead of conditional rendering
   - Added `onInit` event handler to check navigation needs on slider initialization
   - Added `onResize` event handler to update navigation visibility on window resize
   - Ensures refs remain in DOM for Swiper to properly bind navigation

2. **BaseSlider Styles** (`src/components/layout/BaseSlider/BaseSlider.module.scss`)
   - Added `.hidden` class for navigation buttons:
     * `opacity: 0`
     * `pointer-events: none`
     * `visibility: hidden`
   - Buttons remain in DOM but are visually and functionally hidden when not needed

#### Technical Details:
```javascript
// Check if navigation is needed
const needsNavigation = swiper.slides.length > swiper.params.slidesPerView;
setShowNavigation(needsNavigation);

// Apply hidden class conditionally
className={`${styles.navBtn} ${styles.prev} ${!showNavigation ? styles.hidden : ''}`}
```

#### Benefits:
- ✅ **Smart UI**: Navigation buttons only appear when actually needed
- ✅ **Responsive**: Works correctly across all breakpoints (mobile, tablet, desktop)
- ✅ **Clean Design**: Eliminates visual clutter on sliders with few items
- ✅ **Functional**: Buttons remain in DOM so Swiper refs work properly
- ✅ **Dynamic**: Updates on window resize to handle responsive slidesPerView changes

---

## [2025-12-24]

### Feature: Reusable Gradient Border Card Component
**Status**: ✅ COMPLETED (100%)

#### Overview:
Created a highly reusable `GradientBorderCard` component implementing a premium three-layer gradient border technique. Applied this component across all card types (ProductCard, ArticleCard, CourseCard, ServiceCard) and implemented comprehensive DRY improvements to card styling using SCSS mixins.

#### Changes Made:

1. **GradientBorderCard Component** (`src/components/ui/GradientBorderCard/`)
   - Three-layer architecture: gradient border wrapper → background layer → content
   - Flexible wrapper component support (div, Link, or any React component)
   - Multiple gradient directions:
     * `vertical`: Border fades from top to bottom
     * `horizontal`: Border fades from left to right (LTR)
     * `horizontal-rtl`: Border fades from right to left (RTL)
   - Customizable props: gradient direction, hover effects, CSS classes
   - 2px border thickness with smooth hover animations
   - Lift effect on hover (translateY -5px)
   - Image scale effect on hover (1.05)

2. **Card Components Refactoring**
   - **ProductCard**: Wrapped with GradientBorderCard, vertical gradient
   - **ArticleCard**: Wrapped with GradientBorderCard, vertical gradient
   - **CourseCard**: Wrapped with GradientBorderCard, vertical gradient
   - **ServiceCard**: Wrapped with GradientBorderCard, vertical gradient
   - All cards now share consistent gradient border styling

3. **DRY SCSS Improvements** (`src/styles/base/_mixins.scss`)
   - Created reusable mixins for common card patterns:
     * `@mixin base-card`: Base card structure with hover effects
     * `@mixin card-image-wrapper`: Responsive aspect ratios (16/10, 4/3, 16/9)
     * `@mixin card-image`: Image styling with transitions
     * `@mixin card-content`: Content padding and layout
     * `@mixin card-title`: Typography for card titles
     * `@mixin card-text-truncate($lines)`: Multi-line text truncation
   
4. **Card SCSS Cleanup**
   - **ArticleCard.module.scss**: Reduced from 110 lines to 46 lines (58% reduction)
   - **ServiceCard.module.scss**: Reduced from 166 lines to 73 lines (56% reduction)
   - **CourseCard.module.scss**: Reduced from 210 lines to 157 lines (25% reduction)
   - **ProductCard.module.scss**: Reduced from 196 lines to 142 lines (28% reduction)
   - All cards now use shared mixins while maintaining unique features

5. **Horizontal Gradient Support**
   - Border gradient classes for all directions:
     * `.borderVertical`: top to bottom
     * `.borderHorizontal`: left to right (LTR)
     * `.borderHorizontalRtl`: right to left (RTL)
   - Dynamic class application based on gradient prop
   - Used in AboutMentorSection with horizontal LTR gradient

#### Technical Details:
```jsx
// Example Usage
<GradientBorderCard 
  gradient="horizontal"
  as={Link}
  wrapperProps={{ href: '/path' }}
  enableHover={true}
>
  {children}
</GradientBorderCard>
```

#### Benefits:
- ✅ **Reusability**: Single component for all gradient border needs
- ✅ **Consistency**: All cards use identical border technique
- ✅ **DRY Compliance**: Eliminated 300+ lines of duplicate SCSS code
- ✅ **Flexibility**: Support for vertical and horizontal gradients
- ✅ **Maintainability**: Style updates in one place affect all cards
- ✅ **Performance**: No visual regressions, smooth animations
- ✅ **RTL Support**: Full support for RTL layouts

---

### Enhancement: Unified CategoryCard Styling (DRY Principle)
**Status**: ✅ COMPLETED (100%)

#### Overview:
Migrated the premium glassmorphism category card styles from the products page to the shared CategoryCard component, ensuring visual consistency across homepage and products page while following DRY principles.

#### Changes Made:

1. **CategoryCard Component Styling** (`src/components/cards/CategoryCard/CategoryCard.module.scss`)
   - Applied enhanced glassmorphism effects matching products page design
   - Added backdrop blur (12px) for premium glass effect
   - Implemented gradient overlay animation on hover
   - Enhanced border styling with golden accent colors
   - Added smooth cubic-bezier transitions for natural animations
   - Icon wrapper now scales and rotates on hover (scale 1.1 + rotate 3deg)
   - Added text glow effect with golden shadow on hover
   - Improved shadow system with multi-layer depth
   - Added responsive adjustments for mobile devices
   - Maintained RTL support and accessibility features

#### Benefits:
- ✅ **Consistency**: Homepage and products page now share identical category card styling
- ✅ **DRY Compliance**: Single source of truth for category card design
- ✅ **Premium UX**: Enhanced visual appeal with glassmorphism and smooth animations
- ✅ **Maintainability**: Future style updates only need to be made in one place

---

### Feature: Multi-Step Checkout Wizard with Inline Authentication
**Status**: ✅ COMPLETED (100%)

#### Overview:
Implemented a complete 4-step checkout wizard with visual progress tracking, inline OTP authentication, inline address editing, and payment method selection. This replaces the previous single-page checkout with a guided, user-friendly flow.

#### Changes Made:

1. **CheckoutStepper Component** (`src/components/checkout/CheckoutStepper.jsx` + `.module.scss`)
   - Visual stepper with 4 steps: Cart Review → Authentication → Shipping → Payment
   - Three distinct states:
     * **Active**: Accent color with pulsing glow animation
     * **Completed**: Green with checkmark icon
     * **Pending**: Gray/disabled state
   - Clickable completed steps for backward navigation
   - Custom icons for each step (cart, user, location, payment)
   - Fully responsive (horizontal on desktop, compact on mobile)
   - RTL support with proper directional transforms

2. **CartReviewStep Component** (`src/components/checkout/CartReviewStep.jsx` + `.module.scss`)
   - Displays all cart items with images, titles, quantities, and prices
   - Shows item type badges (محصول / دوره آموزشی)
   - Summary box with item count and total price calculation
   - Continue button to proceed to authentication
   - Glassmorphism card design matching site aesthetic

3. **AuthStep Component** (`src/components/checkout/AuthStep.jsx` + `.module.scss`)
   - **Smart Authentication Flow**:
     * If already logged in: Shows success message → auto-advances to next step (1.5s delay)
     * If not logged in: Displays inline OTP form (no modal popup)
   - **OTP Flow**:
     * Phone number input → Send OTP code
     * OTP verification (6 digits)
     * Success → auto-advance to shipping step
   - Reuses auth logic from AuthModal but integrated inline
   - Loading state during session check
   - Success animation with green checkmark on login

4. **ShippingStep Component** (`src/components/checkout/ShippingStep.jsx` + `.module.scss`)
   - **Display Mode**: Shows current address in card format
     * Recipient name, phone
     * City, province
     * Full address, postal code
     * Edit button to toggle edit mode
   - **Edit Mode**: Inline form with 6 fields
     * Recipient name (required)
     * Recipient phone
     * Province, City
     * Full address (required)
     * Postal code
     * Save button updates profile via `/api/addresses`
     * Cancel button returns to display mode
   - Fetches address from `/api/profile` on mount
   - Validation before allowing continue
   - Previous/Next navigation buttons
   - **No redirect to profile page** - all editing happens inline

5. **PaymentStep Component** (`src/components/checkout/PaymentStep.jsx` + `.module.scss`)
   - Order summary showing all items and total
   - Payment method selection:
     * ✅ Online payment (enabled)
     * ❌ Cash on delivery (disabled/grayed out)
   - Selected method highlighted with accent color
   - Green "Complete Payment" button
   - Mock payment processing (2s delay)
   - Redirects to `/payment/callback?status=success`
   - Previous button to go back to shipping

6. **Main Checkout Page Refactor** (`src/app/checkout/page.jsx` + `page.module.scss`)
   - **Complete rewrite** from single-page to multi-step wizard
   - **State Management**:
     * `currentStep` - tracks active step (1-4)
     * `completedSteps` - array of completed step numbers
   - **Navigation Logic**:
     * `goToNextStep()` - marks current as completed, moves forward
     * `goToPreviousStep()` - returns to previous step
     * `goToStep(n)` - jump to completed step via stepper click
   - **Empty Cart Handling**: Shows empty state with link to products
   - **Conditional Rendering**: Renders appropriate step component based on currentStep
   - Clean, centered layout with stepper at top

7. **Cart Page Integration** (`src/app/cart/page.jsx`)
   - **Simplified checkout button**: Removed auth check logic
   - Changed from auth-checking button to simple `<Link href="/checkout">`
   - **Before**: Opens AuthModal if not logged in
   - **After**: Always navigates to `/checkout`
   - Authentication now handled in step 2 of checkout flow

8. **Cart Badge Reactivity Fix** (`src/components/cards/ProductCard/ProductCard.jsx` + `CourseCard.jsx`)
   - **Problem**: After payment, cart cleared but "در سبد" badges remained on product cards
   - **Solution**: Changed from reading entire `items` array to using Zustand selector
   - **Before**: `const items = useCartStore(state => state.items); const isInCart = items.some(...)`
   - **After**: `const isInCart = useCartStore(state => state.items.some(...)) && isHydrated`
   - Ensures badges update immediately when `clearCart()` is called

#### User Flow:

**Scenario 1: User Already Logged In**
```
Cart Page → Click "ادامه فرآیند خرید"
  ↓
Step 1: Review cart items → Click "ادامه"
  ↓
Step 2: See "شما وارد شده‌اید" → Auto-advance (1.5s)
  ↓
Step 3: View address → Click "ادامه" (or edit if needed)
  ↓
Step 4: Select payment method → Click "پرداخت و تکمیل خرید"
  ↓
Redirect to payment gateway
```

**Scenario 2: User Not Logged In**
```
Cart Page → Click "ادامه فرآیند خرید"
  ↓
Step 1: Review cart items → Click "ادامه"
  ↓
Step 2: Enter phone → Receive OTP → Enter code → Login
  ↓
Auto-advance to Step 3
  ↓
Step 3: Enter address (first time) or view → Click "ادامه"
  ↓
Step 4: Select payment method → Complete payment
```

**Scenario 3: Editing Address**
```
Navigate to Step 3 → Click "ویرایش آدرس"
  ↓
Form appears inline with current values
  ↓
Modify fields → Click "ذخیره آدرس"
  ↓
Address updated via API → Form closes
  ↓
Shows updated address → Click "ادامه"
```

#### Technical Features:

- **Stepper Navigation**: Visual progress indicator with click-to-navigate for completed steps
- **Inline Authentication**: No modal popups, OTP flow integrated in checkout
- **Inline Address Editing**: No redirect to profile page, edit and save within checkout
- **Responsive Design**: Mobile-optimized stepper, stacked layouts on mobile
- **RTL Support**: All components support right-to-left direction
- **Empty State Handling**: Cart empty → message + link to products
- **Glassmorphism Design**: Consistent with site aesthetic
- **Mock Payment Logic**: Heavily commented for future Zarinpal integration

#### Mock Logic Comments:
All mock payment logic includes detailed Persian comments for future Zarinpal integration:
```javascript
/**
 * 🚨 MOCK PAYMENT LOGIC
 * این تابع فعلاً پرداخت را شبیه‌سازی می‌کند
 * 
 * برای اتصال به Zarinpal:
 * 1. این تابع را با فراخوانی API Zarinpal جایگزین کنید
 * 2. دریافت Authority و ریدارکت به درگاه Zarinpal
 * 3. تنظیم CallbackURL به /payment/callback
 */
```

#### Files Created:
- `src/components/checkout/CheckoutStepper.jsx` + `.module.scss`
- `src/components/checkout/CartReviewStep.jsx` + `.module.scss`
- `src/components/checkout/AuthStep.jsx` + `.module.scss`
- `src/components/checkout/ShippingStep.jsx` + `.module.scss`
- `src/components/checkout/PaymentStep.jsx` + `.module.scss`

**Total**: 10 new files (5 components + 5 SCSS modules)

#### Files Modified:
- `src/app/checkout/page.jsx` - Complete refactor to multi-step wizard
- `src/app/checkout/page.module.scss` - Simplified layout for stepper
- `src/app/cart/page.jsx` - Simplified checkout button (removed auth check)
- `src/components/cards/ProductCard/ProductCard.jsx` - Fixed cart badge reactivity
- `src/components/cards/CourseCard/CourseCard.jsx` - Fixed cart badge reactivity

**Total**: 5 files modified

#### Impact:
- **Better UX**: Guided checkout flow with clear progress indication
- **Reduced Friction**: Inline authentication and address editing (no page redirects)
- **Visual Feedback**: Stepper shows progress and allows backward navigation
- **Mobile Optimized**: Responsive design works seamlessly on all devices
- **Maintainable**: Modular components, clear separation of concerns
- **Future-Ready**: Mock payment logic well-documented for Zarinpal integration

**Total Lines of Code**: ~2,500 lines added/modified across 15 files

---

## [Previous] - 2025-12-17

### Fix: Product URL Redirects in Cart & Cart Dropdown
**Status**: ✅ COMPLETED (100%)

#### Problem:
Product links in the cart page and cart dropdown were using simplified URLs (`/product/{slug}`) that triggered permanent redirects to the correct category-based URLs (`/products/{category}/{subcategory}/{slug}`). This caused:
- Poor user experience with extra page loads
- SEO impact from redirect chains
- Inconsistency with product links on homepage and products page

#### Solution:
Implemented category-aware URL construction across the entire cart system by:
1. Storing category metadata when adding products to cart
2. Building proper URLs using stored category information
3. Fallback to legacy URLs for backward compatibility

#### Changes Made:

1. **ProductCard Component** (`src/components/cards/ProductCard/ProductCard.jsx`)
   - Modified `handleAddToCart()` to extract `categorySlug` and `subcategorySlug` from product's categories array.
   - Added category data to the `productToAdd` object sent to cart store.
   - Uses same logic as `constructProductUrl()` to determine primary category/subcategory.

2. **Cart Page** (`src/app/cart/page.jsx`)
   - Added `constructProductUrl()` helper function to build category-based URLs.
   - Updated product image link (line 136) to use helper function.
   - Updated product info link (line 147) to use helper function.
   - Fallback to `/product/{slug}` if category data missing (backward compatibility).

3. **Cart Dropdown** (`src/components/layout/Navbar/CartIcon.jsx`)
   - Added `constructItemUrl()` helper function for both products and courses.
   - Updated cart item links (line 207) to use helper function.
   - Product URLs now include full category paths.
   - Course URLs remain simple (`/courses/{slug}`).

#### Technical Details:
- **Category Priority**: Subcategory (with parent) → Root category → Fallback
- **Data Flow**: ProductCard → Cart Store → Cart Page/Dropdown
- **Backward Compatibility**: Existing cart items without category data still work
- **Consistency**: All cart interfaces now match homepage/products page URL structure

#### Result:
✅ No more redirects when clicking products in cart
✅ Direct navigation to correct product detail pages
✅ Improved page load performance
✅ Better SEO with canonical URLs from the start
✅ Consistent URL structure across entire application

#### Files Modified:
- `src/components/cards/ProductCard/ProductCard.jsx` (+19 lines in handleAddToCart)
- `src/app/cart/page.jsx` (+23 lines helper function, 2 lines link updates)
- `src/components/layout/Navbar/CartIcon.jsx` (+34 lines helper function, 1 line link update)

**Total**: 3 files, ~79 lines added/modified.

---

### Feature: Complete Shopping Cart System Implementation (Previous Entry)

### Feature: Complete Shopping Cart System Implementation
**Status**: ✅ COMPLETED (100%)

#### Changes Made:

1. **Cart Store Refactoring** (`src/store/useCartStore.js`)
   - Converted `totalPrice` and `itemsCount` from getters to selector functions for proper Zustand reactivity.
   - Exported `selectTotalPrice` and `selectItemsCount` selector functions.
   - Ensures components re-render correctly when cart state changes.

2. **Cart Icon Component** (`src/components/layout/Navbar/CartIcon.jsx`)
   - **Hydration Safety**: Implemented `mounted` state pattern to prevent SSR/client HTML mismatch.
   - **Mobile Behavior**: Click toggles dropdown on mobile (<1024px), navigates to `/cart` on desktop.
   - **Badge Styling**: Matched UserStatus design with circular border and bottom-left badge position.
   - **Hover Effects**: Desktop-only hover to show dropdown preview.
   - **URL Construction**: Added `constructItemUrl()` to build correct product URLs with category paths.

3. **Cart Icon Styling** (`src/components/layout/Navbar/CartIcon.module.scss`)
   - Circular wrapper: 40px × 40px with glassmorphism background.
   - Badge positioned at `bottom: -2px, left: -2px` (matching UserStatus).
   - Icon size: 22px × 22px.
   - Border: `2px solid rgba(246, 217, 130, 0.3)`.

4. **Product & Course Cards Hydration Fix**
   - **ProductCard** (`src/components/cards/ProductCard/ProductCard.jsx`):
     * Added `isHydrated` state pattern.
     * Shows "Add to Cart" button before hydration, actual state after.
     * Added category/subcategory slugs to cart items for proper URL construction.
   - **CourseCard** (`src/components/cards/CourseCard/CourseCard.jsx`):
     * Same `isHydrated` pattern.
     * Prevents disabled button from showing before client hydration.
   - Heavily commented in Persian explaining hydration logic.

5. **Cart Page** (`src/app/cart/page.jsx` & `Cart.module.scss`)
   - **Empty State**: Icon + message + "بازگشت به فروشگاه" button.
   - **Product Section**: Image, title, price, quantity controller `[−] [qty] [+]`, remove button.
   - **Course Section**: Image, title, price, label "دوره آموزشی", remove button (no quantity).
   - **Sidebar**: Sticky summary with total price and "ادامه فرآیند خرید" button.
   - **Clickable Items**: Image and info are `<Link>` components to product/course detail pages.
   - **URL Construction**: `constructProductUrl()` builds correct URLs using category data.
   - **Responsive**: Two-column grid on desktop, stacked on mobile.
   - **Glassmorphism**: All cards with vertical gradient and hover effects.

6. **Cart Dropdown** (`src/components/layout/Navbar/CartIcon.jsx`)
   - Shows first 5 items with image, title, quantity/type, price.
   - Total price display.
   - "مشاهده و تسویه حساب" button linking to `/cart`.
   - Uses `constructItemUrl()` for proper product navigation.

#### Technical Improvements:
- **No Hydration Errors**: Server and client HTML match perfectly.
- **Proper Reactivity**: Zustand selectors ensure UI updates when cart changes.
- **LocalStorage Persistence**: Cart data persists across sessions.
- **Category-Aware URLs**: Products navigate to correct category paths.
- **RTL Compliant**: All layouts support right-to-left direction.

#### User Flow:
```
Add Item to Cart → Badge Updates
  ↓
Mobile: Click Cart Icon → Dropdown Toggle
Desktop: Hover Cart Icon → Dropdown Preview
  ↓
Click Item in Dropdown/Cart → Navigate to Detail Page
  ↓
Cart Page: Manage Quantities → Remove Items → Checkout
```

#### Files Modified:
- `src/store/useCartStore.js` (selector functions)
- `src/components/layout/Navbar/CartIcon.jsx` (hydration + mobile behavior + URL construction)
- `src/components/layout/Navbar/CartIcon.module.scss` (UserStatus-matched styling)
- `src/components/cards/ProductCard/ProductCard.jsx` (hydration + category data)
- `src/components/cards/CourseCard/CourseCard.jsx` (hydration)
- `src/app/cart/page.jsx` (new cart page)
- `src/app/cart/Cart.module.scss` (new cart styles)

**Total**: 7 files (2 new), ~800 lines of code added/modified.

---

## [Previous] - 2025-12-16

### Enhancement: Navbar Mobile Layout Restructure
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Navbar Component** (`src/modules/layout/Navbar/NavbarClient.jsx`)
   - Separated `actionIcons` from `mobileMenuToggle` as independent elements.
   - Added `toggleWrapper` container for mobile menu button.
   - Structure: `[Toggle Button] [Logo] [Profile + Cart Icons]`

2. **Navbar Styles** (`src/modules/layout/Navbar/Navbar.module.scss`)
   - Implemented flexbox `order` for mobile layout control:
     * `toggleWrapper` → order: 1 (right side)
     * `logo` → order: 2 (center)
     * `actionIcons` → order: 3 (left side)
   - Added fixed widths (100px) for `toggleWrapper` and `actionIcons` in mobile.
   - Search icon hidden in mobile using `!important` specificity.
   - Removed `.actionsContainer` wrapper (no longer needed).

3. **UserStatus Dropdown** (`src/components/layout/Navbar/UserStatus.module.scss`)
   - Fixed RTL dropdown positioning for mobile view.
   - Removed unnecessary size overrides for mobile.

#### Mobile Layout:
```
[☰ Menu] ---------- [Logo] ---------- [👤 Profile] [🛒 Cart]
```

#### Impact:
- **Better Mobile UX**: Action icons now balanced on both sides of logo.
- **Cleaner Layout**: 100px fixed widths ensure consistent spacing.
- **RTL Compliant**: All elements properly positioned in right-to-left layout.

**Files Modified**: 3 files (~30 lines of code changed).

---

## [Previous] - 2025-12-16

### Enhancement: Profile Form - Address Refactoring & Edit Mode
**Status**: ✅ COMPLETED (100%)

#### Changes Made:

1. **Address Management Refactoring** (`src/components/profile/ProfileForm.jsx`)
   - Replaced single `address` field with 7 individual address fields:
     * `fullAddress` - Full detailed address (textarea)
     * `province` - Province/state
     * `city` - City
     * `postalCode` - 10-digit postal code
     * `recipientName` - Recipient full name
     * `recipientPhone` - Recipient phone number
   - Updated `useState` to include all 7 fields in component state.
   - Modified `useEffect` (fetchUserData) to populate all 7 fields from Strapi response.
   - Updated `handleSubmit` to send all 7 fields instead of hardcoded placeholders.
   - Replaced single address textarea with structured UI:
     * Section header "اطلاعات آدرس و گیرنده"
     * Full address textarea with detailed placeholder
     * Two-column grid layout for province/city
     * Two-column grid layout for postal code/recipient name
     * Recipient phone input field

2. **Edit Mode Feature** (`src/components/profile/ProfileForm.jsx`)
   - Added `isEditing` state to control form edit mode.
   - Implemented "ویرایش" (Edit) button in form header (top-left position).
   - All input fields now disabled by default until edit mode is activated.
   - Added action button group with:
     * "انصراف" (Cancel) button - exits edit mode without saving
     * "ذخیره تغییرات" (Save Changes) button - submits form and exits edit mode
   - Auto-exit edit mode after successful save.
   - Phone number field remains permanently disabled (as designed).

3. **Styling Updates** (`src/components/profile/ProfileForm.module.scss`)
   - Added `.editButton` styles with glassmorphism effect and hover animations.
   - Updated `.header` to flexbox layout for button placement.
   - Added `.buttonGroup` for action buttons with RTL direction.
   - Added `.cancelButton` styles matching glassmorphism theme.
   - Updated `.submitButton` to work in flex layout (changed from `width: 100%` to `flex: 1`).
   - Added `.addressSection` and `.addressTitle` styles for grouped address fields.
   - Responsive adjustments for mobile devices (stacked layout).

4. **API Route Fixes**
   - **Address Update Route** (`src/app/api/addresses/[id]/route.js`):
     * Fixed Next.js 15 async params error by awaiting `params` before accessing `id`.
     * Error: "Route used `params.id` before awaiting params" → RESOLVED.
   
   - **Strapi V5 Compatibility** (`src/components/profile/ProfileForm.jsx`):
     * Migrated from `addressId` to `addressDocumentId` throughout component.
     * Changed state field name: `addressId` → `addressDocumentId`
     * Updated data fetching to use `data.address?.documentId` instead of `data.address?.id`
     * Updated address update logic to use `documentId` parameter
     * Fixed new address creation to save `documentId` instead of `id`
     * Resolves 404 errors when updating addresses in Strapi V5.

5. **Debug Logging** (`src/app/api/profile/route.js`)
   - Added console.log to track user data structure returned from Strapi.
   - Helps diagnose field population issues in profile form.

#### User Flow:
```
View Profile (Read-Only) 
  ↓
Click "ویرایش" Button
  ↓
All Fields Enabled (except phoneNumber)
  ↓
User Edits Data
  ↓
Click "ذخیره" → Save & Exit Edit Mode
  OR
Click "انصراف" → Discard & Exit Edit Mode
```

#### Address Data Structure:
**Before (Single Field)**:
- `address` (textarea) → with hardcoded defaults for missing fields

**After (7 Individual Fields)**:
- `fullAddress` (textarea)
- `province` (text input)
- `city` (text input)  
- `postalCode` (text input, LTR)
- `recipientName` (text input)
- `recipientPhone` (tel input, LTR)
- `addressDocumentId` (internal state for Strapi V5)

#### Impact:
- **Better Data Quality**: Users now provide complete address information instead of single text field.
- **Improved UX**: Edit mode prevents accidental changes with clear save/cancel actions.
- **Strapi V5 Compatible**: All address operations use `documentId` instead of deprecated `id` field.
- **Visual Hierarchy**: Grouped address fields in dedicated section with clear labeling.
- **Mobile Responsive**: Two-column layout on desktop, single column on mobile.
- **API Stability**: Fixed Next.js 15 async params requirement.

#### Files Modified:
- `src/components/profile/ProfileForm.jsx` (major refactor)
- `src/components/profile/ProfileForm.module.scss` (new styles added)
- `src/app/api/addresses/[id]/route.js` (async params fix)
- `src/app/api/profile/route.js` (debug logging)

**Total**: 4 files modified, ~200 lines of code changed.

---

## [Previous] - 2025-12-16

### Feature: Profile Dashboard & Address Management System
**Status**: ✅ COMPLETED (100%)

#### Changes Made:

1. **Protected Profile Layout** (`src/app/profile/layout.js`)
   - Implemented authentication-protected route using NextAuth `useSession`.
   - Auto-redirect to home page for unauthenticated users.
   - Responsive grid layout: 25% sidebar + 75% content (desktop).
   - Mobile-first design with single column on mobile devices.
   - Loading state during authentication check.

2. **Profile Sidebar Component** (`src/components/profile/ProfileSidebar.jsx`)
   - User summary section with avatar and phone number display.
   - Navigation links with active state highlighting:
     * "اطلاعات حساب" → `/profile`
     * "سفارش‌های من" → `/profile/orders`
   - Logout button with `signOut()` integration.
   - Glassmorphism design matching AuthModal aesthetic.
   - Sticky positioning on desktop, static on mobile.
   - RTL support with directional transforms.

3. **Profile Form Component** (`src/components/profile/ProfileForm.jsx`)
   - Dual-fetch logic for User and Address data:
     * Step 1: Update user (firstName, lastName) via `/api/profile`.
     * Step 2: Create/update address via `/api/addresses`.
   - State management with `addressId` tracking for updates.
   - Form fields: firstName, lastName, phoneNumber (disabled), address.
   - Success/error notifications with auto-dismiss.
   - Loading states during fetch and save operations.

4. **Profile API Routes**:
   - **GET `/api/profile`** (`src/app/api/profile/route.js`):
     * Fetches user data with `populate=address` from Strapi.
     * Server-side authentication using `getServerSession`.
     * Uses `STRAPI_ADMIN_TOKEN` for authorization.
   
   - **PUT `/api/profile`** (`src/app/api/profile/route.js`):
     * Updates user firstName and lastName.
     * Server-side session validation.
   
   - **POST `/api/addresses`** (`src/app/api/addresses/route.js`):
     * Creates new address record in Strapi.
     * Links address to user via relation.
     * Handles all 7 address fields (title, fullAddress, province, city, postalCode, recipientName, recipientPhone).
   
   - **PUT `/api/addresses/[id]`** (`src/app/api/addresses/[id]/route.js`):
     * Updates existing address by ID.
     * Dynamic route with address ID parameter.

5. **Profile Page** (`src/app/profile/page.js`)
   - Main profile page rendering ProfileForm.
   - SEO metadata (title, description).

6. **Orders Placeholder** (`src/app/profile/orders/page.js`)
   - Placeholder page for future orders feature.
   - Glassmorphism card with floating icon animation.
   - Consistent styling with profile page.

7. **Styling** (`src/app/profile/profile.module.scss`, `src/components/profile/*.module.scss`)
   - Glassmorphism design pattern:
     * `backdrop-filter: blur(20px)`
     * Golden borders: `rgba(246, 217, 130, 0.2)`
     * Gradient backgrounds: `var(--gradient-card-vertical)`
   - Responsive breakpoints for mobile/tablet/desktop.
   - RTL support throughout.
   - Hover effects and smooth transitions.

8. **Root Layout Fix** (`src/app/layout.js`)
   - Added `suppressHydrationWarning` to `<html>` tag.
   - Fixes hydration mismatch warnings from browser extensions.

#### Address Management Flow:
```
User Submit → PUT /api/profile (User data)
           → Check addressId exists?
           → YES: PUT /api/addresses/[id] (Update)
           → NO:  POST /api/addresses (Create + Link to User)
           → Update local state with addressId
           → Show success message
```

#### Data Structure:
**User Fields**: firstName, lastName, phoneNumber (read-only)
**Address Fields**: title, fullAddress, province, city, postalCode, recipientName, recipientPhone

#### Impact:
- **User Experience**: Complete profile management with address handling.
- **Authentication**: Protected routes ensure only logged-in users access profile.
- **Data Integrity**: Separate User and Address models in Strapi.
- **Lazy Registration**: Users can complete profile after OTP login.
- **Scalability**: Address system ready for multiple addresses per user.
- **Design Consistency**: Glassmorphism aesthetic matches AuthModal.

#### Files Created:
- `src/app/profile/layout.js`
- `src/app/profile/page.js`
- `src/app/profile/profile.module.scss`
- `src/app/profile/orders/page.js`
- `src/app/profile/orders/orders.module.scss`
- `src/components/profile/ProfileSidebar.jsx`
- `src/components/profile/ProfileSidebar.module.scss`
- `src/components/profile/ProfileForm.jsx`
- `src/components/profile/ProfileForm.module.scss`
- `src/app/api/profile/route.js`
- `src/app/api/addresses/route.js`
- `src/app/api/addresses/[id]/route.js`

#### Files Modified:
- `src/app/layout.js` (hydration warning fix)

**Total**: 12 new files, 1 file modified.

---

## [Previous] - 2025-12-16

### Enhancement: API Error Handling & Console Noise Reduction
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Next.js Image Configuration** (`next.config.mjs`)
   - Added `qualities: [50, 75, 90, 100]` to images configuration.
   - Fixed warning: "Images loaded lazily and replaced with placeholders".
   - Fixed warning: "quality 100 not configured in images.qualities".
   - Ensures compatibility with Next.js 16 requirements.

2. **API Client Core** (`src/lib/apiClient.js`)
   - Implemented intelligent network error detection.
   - Added `BACKEND_UNAVAILABLE` error code for fetch failures.
   - Changed error logging strategy:
     * Network errors: `console.warn` + `console.info` with helpful messages.
     * Other errors: `console.error` only in development mode.
   - Improved error messages: "⚠️ Backend unavailable" instead of verbose stack traces.
   - Added environment-aware logging (development vs production).

3. **API Error Handler Utility** (`src/lib/apiErrorHandler.js`)
   - Created centralized error handling utility for future use.
   - Implemented `isBackendUnavailable()` helper function.
   - Implemented `logApiError()` for consistent error logging.
   - Implemented `withErrorHandling()` wrapper for API functions.

4. **Categories API** (`src/lib/categoriesApi.js`)
   - Updated `getMainCategories()` error handling.
   - Updated `getCategoryTree()` error handling.
   - Silenced errors when backend is unavailable.
   - Maintained graceful degradation (returns empty array).

5. **Articles API** (`src/lib/articlesApi.js`)
   - Updated all 5 functions with improved error handling:
     * `getArticleCategories()`
     * `getAllArticles()`
     * `getArticleBySlug()`
     * `getArticles()`
     * `getArticlesPaginated()`
   - Consistent pattern: only log errors in development when backend is available.

6. **Products API** (`src/lib/productsApi.js`)
   - Updated all 5 functions with improved error handling:
     * `getAllProducts()`
     * `getProductBySlug()`
     * `getProductCategoryPath()`
     * `getProducts()`
     * `getProductsPaginated()`
   - Maintained existing fallback values (empty arrays/null).

7. **Services API** (`src/lib/servicesApi.js`)
   - Updated all 4 functions with improved error handling:
     * `getAllServices()`
     * `getServiceBySlug()`
     * `getServices()`
     * `getServicesPaginated()`
   - Preserved detailed JSDoc comments while cleaning error logs.

8. **Courses API** (`src/lib/coursesApi.js`)
   - Updated all 4 functions with improved error handling:
     * `getAllCourses()`
     * `getCourseBySlug()`
     * `getCourses()`
     * `getCoursesPaginated()`
   - Consistent with other API modules.

9. **Testimonials API** (`src/lib/testimonialsApi.js`)
   - Updated `getAllTestimonials()` error handling.
   - Reduced console noise when backend is down.

10. **FAQ API** (`src/lib/faqApi.js`)
    - Updated `getAllFaqs()` error handling.
    - Consistent error handling pattern.

#### Error Handling Pattern:
```javascript
} catch (error) {
  if (error.message !== 'BACKEND_UNAVAILABLE' && process.env.NODE_ENV === 'development') {
    console.error('خطا در واکشی داده:', error.message);
  }
  return []; // or null, or appropriate fallback
}
```

#### Console Output Comparison:

**Before (Noisy & Repetitive):**
```
❌ خطای API Client: fetch failed
❌ خطا در واکشی ساختار درختی دسته‌ها: TypeError: fetch failed
❌ خطا در واکشی دسته‌بندی‌های مقالات: fetch failed
❌ خطا در واکشی محصولات: fetch failed
[... 20+ similar error messages ...]
```

**After (Clean & Informative):**
```
⚠️ Backend unavailable: /api/categories
💡 Make sure your Strapi server is running on http://localhost:1337
```

#### Impact:
- **Developer Experience**: Cleaner console with actionable error messages.
- **Production Ready**: Zero error logs in production when backend is unavailable.
- **Graceful Degradation**: All pages load with empty states instead of crashing.
- **Debugging Efficiency**: Real errors (non-network) still logged in development.
- **Code Consistency**: Unified error handling pattern across all 10 API modules.
- **Performance**: No impact - error handling is lightweight and conditional.
- **User Experience**: Pages load smoothly even when backend is down, showing appropriate empty states.

#### Files Modified:
- `next.config.mjs` (1 file)
- `src/lib/apiClient.js` (1 file)
- `src/lib/apiErrorHandler.js` (1 new file)
- `src/lib/categoriesApi.js` (2 functions)
- `src/lib/articlesApi.js` (5 functions)
- `src/lib/productsApi.js` (5 functions)
- `src/lib/servicesApi.js` (4 functions)
- `src/lib/coursesApi.js` (4 functions)
- `src/lib/testimonialsApi.js` (1 function)
- `src/lib/faqApi.js` (1 function)

**Total**: 11 files modified, 1 new file created, 22 functions improved.

---

## [Previous] - 2025-12-15

### Feature: Authentication System with NextAuth OTP & User Status Component
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Zustand State Management** (`src/store/authStore.js`)
   - Created global authentication state store with `isAuthModalOpen`, `authStep`, and `phoneNumber`.
   - Implemented helper functions: `openAuthModal()`, `closeAuthModal()`, `setAuthStep()`, `setPhoneNumber()`.

2. **Authentication Modal** (`src/components/auth/AuthModal.jsx` + `.module.scss`)
   - Implemented 2-step OTP authentication flow:
     * Step 1: Phone number input → Calls Strapi `/api/auth/otp/send`
     * Step 2: OTP verification → Uses NextAuth `signIn('otp-login', ...)`
   - Glassmorphism-styled modal with smooth fade-in/slide-up animations
   - Form validation (11-digit phone, 6-digit OTP)
   - Loading states and Persian error messages
   - Responsive design with RTL support

3. **NextAuth Configuration** (`src/lib/auth.js`)
   - Fixed OTP verification endpoint URL to use Strapi backend (`/api/auth/otp/verify`)
   - Implemented custom OTP credentials provider with proper error handling
   - Added JWT and session callbacks for user data persistence
   - Integrated Google OAuth provider (optional)

4. **User Status Component** (`src/components/layout/Navbar/UserStatus.jsx` + `.module.scss`)
   - Smart component with 3 states:
     * Loading: Animated shimmer skeleton
     * Unauthenticated: User icon button that opens AuthModal
     * Authenticated: Avatar with green verification badge + dropdown menu
   - Dropdown features:
     * Profile link (`/profile`)
     * Logout button with NextAuth `signOut()`
     * Delayed close (300ms) using state management and setTimeout
     * Glassmorphism styling with backdrop blur
   - Hover-controlled visibility with smooth transitions

5. **Layout Integration** (`src/app/layout.js`)
   - Wrapped children with `<Providers>` (SessionProvider from NextAuth)
   - Added `<AuthModal />` globally for site-wide access

6. **Navbar Integration** (`src/modules/layout/Navbar/NavbarClient.jsx`)
   - Replaced old login button with `<UserStatus />` component
   - Removed unused `useAuthStore` import from NavbarClient

7. **Environment Configuration** (`.env.local`)
   - Added `NEXT_PUBLIC_STRAPI_API_URL` for backend connection
   - Added `NEXTAUTH_URL` and `NEXTAUTH_SECRET` for NextAuth configuration

#### Technical Implementation:
- **State Management**: Zustand for modal state, NextAuth for session management
- **Authentication Flow**: Phone → OTP SMS → NextAuth verification → Session creation
- **API Integration**: Strapi custom endpoints for OTP send/verify
- **Styling**: Glassmorphism design with CSS modules, RTL support, responsive breakpoints
- **UX Enhancements**: 
  * Delayed dropdown close prevents accidental dismissal
  * Loading skeletons for better perceived performance
  * Clear visual feedback with verification badge
  * Smooth animations and transitions

#### Impact:
- **User Experience**: Seamless OTP-based authentication with modern UI
- **Security**: Backend-verified OTP with NextAuth session management
- **Accessibility**: Keyboard navigation, ARIA labels, high-contrast design
- **Maintainability**: Modular components, centralized state, reusable styles
- **Performance**: Conditional rendering, optimized re-renders, lazy registration flow

**Files Created**: 4 new files (~600 lines of code)
**Files Modified**: 4 files (layout, navbar, auth config, environment)

---

## [Previous] - 2025-12-12

### Feature: Search Module UX Enhancements & Fixes
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1.  **Mobile Search Integration (`src/modules/layout/Navbar/NavbarClient.jsx`)**
    *   Replaced the static mobile search input with a functional form.
    *   Added a **Search Button** (magnifying glass icon) within the mobile search form.
    *   Connected the form to `handleMobileSearch` which pushes to `/search?q=...` and closes the menu.
    *   **Styling:** Created `.mobileSearchForm` container to group input and button seamlessly with RTL support.

2.  **Search Overlay Update (`src/components/ui/SearchOverlay/SearchOverlay.jsx`)**
    *   Added a submit button (magnifying glass icon) inside the search form next to the input.
    *   Ensures consistent UX where users can click to search or press Enter.

3.  **Search Type Filter (`src/components/ui/SearchBox/SearchBox.jsx`)**
    *   Added a **"Type Selector"** dropdown (All, Products, Articles, Courses) next to the main search input.
    *   Updated `router.push` to include the `&type=...` query parameter.
    *   **API Update (`src/lib/searchApi.js`):** Modified `searchGlobal` to accept the `type` parameter and filter API endpoints strictly based on selection.

4.  **Conditional 'All' Tab Logic (`src/modules/search/SearchResults.jsx`)**
    *   Implemented smart tab logic: If results exist in only **one** category (e.g., only Products), the "All" tab is hidden.
    *   `activeTab` automatically defaults to the single available category.
    *   Ensures a cleaner UI when specific results are found.

5.  **Grid Layout Fix (`src/modules/search/SearchResults.module.scss`)**
    *   Enforced **4-column grid** on extra-large screens (`xl` breakpoint) using `grid-template-columns: repeat(4, 1fr) !important;`.

#### Impact:
*   **Mobile UX:** Fully functional search in mobile menu.
*   **Precision:** Users can now filter search by content type directly from the search box.
*   **Visual Polish:** Better grid layout on large screens and smarter tab management.
*   **Consistency:** Search buttons now available in all input contexts (Overlay, Mobile, Results).

---

### Fix: Course Populate Field Mismatch in Search API
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1.  **Search API (`src/lib/searchApi.js`)**
    *   Updated the Course query populate parameter from `'image'` to `'media'`.
    *   This ensures the API returns the correct field (`media`) which contains the course image/video data, matching the Strapi content type schema.

#### Impact:
*   **Bug Fix**: Resolved critical issue where Course images/thumbnails were missing in search results.
*   **Data Integrity**: Aligned API query with actual database schema for Courses.

---

## [Previous] - 2025-12-11

### Feature: Global Search Module (API, Overlay, & Results Page)
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1.  **Search API (`src/lib/searchApi.js`)**
    *   Implemented `searchGlobal(q, type)` function with **Parallel Fetching** (`Promise.all`) for Products, Articles, and Courses.
    *   Optimized queries using Strapi filters: `filters[title][$containsi]=q` and specific `populate` fields (images/cover/media).
    *   Return normalized object `{ products, articles, courses }`.

2.  **UI Components (Search Overlay & Box)**
    *   **SearchTrigger (`src/components/layout/SearchTrigger.jsx`):** A simplified nav button that dispatches a custom event `open-search-overlay`.
    *   **SearchOverlay (`src/components/ui/SearchOverlay`):** Full-screen, backdrop-blur modal with large input, animated transitions, and mobile responsiveness. Close on `Escape` key.
    *   **SearchBox (`src/components/ui/SearchBox`):** A reusable search component with integrated "Type Select" dropdown ("All", "Products", "Articles", "Courses") used in the Results Page.

3.  **Search Results Page (`src/app/search/page.js`)**
    *   **Server Component (SSR):** Fetches initial results based on URL queries (`?q=...&type=...`).
    *   **Strict Breadcrumb Integration:** Uses shared `Breadcrumb` component with strict import path compliance.
    *   **Dynamic Metadata:** Generates page title based on search query.

4.  **Refactor: Standardized Search Results Grid (`src/modules/search/SearchResults.jsx`)**
    *   **Standard Cards:** Reused `ProductCard`, `ArticleCard`, and `CourseCard` components instead of ad-hoc markup.
    *   **Data Normalization:** Implemented a robust **Data Normalizer** to handle complex/deep Strapi V5 JSON structures (e.g., `item.attributes.images.data[0]...`), ensuring images render correctly even with nested API responses.
    *   **Responsive Grid:** Implemented `SearchResults.module.scss` using `@include respond(md/lg/xl)` mixins for a 1-to-4 column responsive layout.
    *   **Smart Tabs:** Dynamic tabs that only appear if results exist for that category.
    *   **Absolute URL Fix:** added `STRAPI_BASE_URL` logic to ensure all images use absolute paths.

#### Impact:
*   **User Experience:** Fast, accessible site-wide search with instant visual feedback and category filtering.
*   **Maintainability:** Reuses all existing Card components, ensuring design consistency across the site.
*   **Performance:** Parallel API requests minimize wait time; SSR ensures SEO friendliness for search result pages.
*   **Reliability:** Robust image extraction logic prevents broken images from Strapi's deep nesting.

---

## [Previous] - 2025-12-09

### Fix: Critical Strapi V5 Relation Persistence and Admin Display
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1.  **Frontend Component (`src/modules/comments/CommentItem.jsx`)**
    * Updated the `handleReplyClick` function to pass the essential **`documentId`** instead of the numeric `id` to the `onReply` callback. This was crucial for proper relationship setting.
2.  **Comments API (`src/lib/commentsApi.js`)**
    * In `submitComment`, implemented the Strapi v5 **`connect`** syntax (`comment_parent: { connect: [documentId] }`) to ensure correct relationship saving in the database.
3.  **Strapi Backend Schema (`src/api/comment/content-types/comment/schema.json`)**
    * Added the **`"displayField": "content"`** property to the schema's root level to force the Admin Panel UI to display the comment text (content) instead of the Document ID in all relation fields.

#### Impact:
* Resolved the critical bug where relation fields (`comment_parent`, `comment_replies`) were saved incorrectly and displayed as empty in the Strapi Admin Panel.
* Ensures that replies created on the frontend are correctly linked in the database.
* Improved Admin UX by showing readable comment content in all relation fields.
* **System Stability:** The comments module is now fully compliant with Strapi v5 architectural requirements for relations and filters.

---

## [Previous] - 2025-12-09

### Fix: Comments API - Approved Filters & DocumentId Migration
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Comments API** (`src/lib/commentsApi.js`)
    - Added `isApproved` filter for Level 1 Replies: `'populate[comment_replies][filters][isApproved][$eq]': 'true'`
    - Added `isApproved` filter for Level 2 Replies: `'populate[comment_replies][populate][comment_replies][filters][isApproved][$eq]': 'true'`
    - Migrated all user fields from `'id'` to `'documentId'`:
        * Root comments: `'populate[user][fields][1]': 'documentId'`
        * Level 1 replies: `'populate[comment_replies][populate][user][fields][1]': 'documentId'`
        * Level 2 replies: `'populate[comment_replies][populate][comment_replies][populate][user][fields][1]': 'documentId'`
    - Improved code comments for better maintainability.

#### Impact:
- Only approved comments and replies are now displayed at all nesting levels.
- Aligned with documentId migration strategy across the entire codebase.
- Ensures consistency between comments system and other modules using documentId.

---

## [Previous] - 2025-12-09

### Fix: Enhanced Error Logging & Hardcoded User ID for Comments
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Comments API** (`src/lib/commentsApi.js`)
    - Hardcoded `user: 1` in payload (replaced `MOCK_USER_ID` constant).
    - Added descriptive comment with emoji (👇) to highlight temporary implementation.
    - Added console logging of full payload before submission for debugging.

2. **API Client** (`src/lib/apiClient.js`)
    - Enhanced error handling to log Strapi error response body.
    - Now shows detailed JSON error messages from backend (validation errors, missing fields, etc.).
    - Improved debugging capability for 400 Bad Request errors.

#### Impact:
- Better debugging visibility when comment submission fails.
- Clear identification of validation issues from Strapi backend.
- Explicit hardcoded user ID until authentication system is implemented.

---

## [Previous] - 2025-12-09

### Feature: Polymorphic Comments System with Star Ratings
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **New API Layer** (`src/lib/commentsApi.js`)
    - `getComments(entityType, entityId)` - Fetch approved comments with threading (nested replies).
    - `submitComment(commentData)` - Submit comment with Mutual Exclusivity (only one entity) and moderation (isApproved: false).
    - Uses `MOCK_USER_ID = 1` until authentication is implemented.

2. **New Components** (`src/modules/comments/`)
    - `CommentItem.jsx` - Recursive component for rendering comments with avatar, username, stars (1-5), Persian date, and reply button.
    - `CommentsSection.jsx` - Comment submission form (with validation), comments list, star rating input, success/error messages.
    - Styling: Glassmorphism with RTL support and responsive design.

3. **Page Integrations**
    - Added `<CommentsSection />` to pages:
        * `src/app/articles/[slug]/page.js`
        * `src/app/courses/[slug]/page.js`
        * `src/app/products/[category]/[subcategory]/[slug]/page.js`
    - All comments are fetched via SSR (SEO-friendly).

4. **SCSS Import Fix**
    - Changed from `@use` to `@import '@/styles/base/mixins';` for Next.js/Turbopack compatibility.

#### Impact:
- Polymorphic comments system supporting articles, products, and courses.
- Threading (nested replies) up to 3 levels deep.
- Moderation workflow: all comments pending admin approval.
- Star rating (1-5 stars) for each comment.

**Files Created**: 5 new files (~1,500 lines of code)
**Files Modified**: 3 pages (articles, courses, products)

---

## [Previous] - 2025-12-09

### Feature: Breadcrumb Navigation Integration Across All Pages
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **About Page** (`src/app/about/page.js`)
    - Imported `Breadcrumb` component from `@/components/ui/BreadCrumb/Breadcrumb`.
    - Added breadcrumb navigation with items: `خانه > درباره ما`.
    - Placed breadcrumb before the header section with proper spacing.

2. **Contact Page** (`src/app/contact/page.js`)
    - Imported `Breadcrumb` component from `@/components/ui/BreadCrumb/Breadcrumb`.
    - Added breadcrumb navigation with items: `خانه > تماس با ما`.
    - Placed breadcrumb before the header section with proper spacing.

3. **Services Module** (Already Implemented)
    - **Services Listing** (`src/app/services/page.js`):
        * Breadcrumb already implemented: `خانه > خدمات`.
    - **Service Detail** (`src/app/services/[slug]/page.js`):
        * Breadcrumb already implemented with dynamic service title: `خانه > خدمات > [Service Title]`.
        * No changes required - already using shared `Breadcrumb` component.

4. **Courses Module**
    - **Courses Listing** (`src/app/courses/page.js`):
        * Imported `Breadcrumb` component from `@/components/ui/BreadCrumb/Breadcrumb`.
        * Added breadcrumb navigation with items: `خانه > دوره‌ها`.
        * Placed breadcrumb before the header section.
    - **Course Detail** (`src/app/courses/[slug]/page.js`):
        * Imported `Breadcrumb` component from `@/components/ui/BreadCrumb/Breadcrumb`.
        * Added dynamic breadcrumb navigation: `خانه > دوره‌ها > [Course Title]`.
        * Placed breadcrumb before the main content grid.


# CHANGELOG

##latest- 2025-12-09

### Fix: Critical Strapi V5 Relation Persistence and Admin Display
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1.  **Frontend Component (`src/modules/comments/CommentItem.jsx`)**
    * Updated the `handleReplyClick` function to pass the essential **`documentId`** instead of the numeric `id` to the `onReply` callback. This was crucial for proper relationship setting.
2.  **Comments API (`src/lib/commentsApi.js`)**
    * In `submitComment`, implemented the Strapi v5 **`connect`** syntax (`comment_parent: { connect: [documentId] }`) to ensure correct relationship saving in the database.
3.  **Strapi Backend Schema (`src/api/comment/content-types/comment/schema.json`)**
    * Added the **`"displayField": "content"`** property to the schema's root level to force the Admin Panel UI to display the comment text (content) instead of the Document ID in all relation fields.

#### Impact:
* Resolved the critical bug where relation fields (`comment_parent`, `comment_replies`) were saved incorrectly and displayed as empty in the Strapi Admin Panel.
* Ensures that replies created on the frontend are correctly linked in the database.
* Improved Admin UX by showing readable comment content in all relation fields.
* **System Stability:** The comments module is now fully compliant with Strapi v5 architectural requirements for relations and filters.

---

## [Previous] - 2025-12-09

### Fix: Comments API - Approved Filters & DocumentId Migration
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Comments API** (`src/lib/commentsApi.js`)
    - Added `isApproved` filter for Level 1 Replies: `'populate[comment_replies][filters][isApproved][$eq]': 'true'`
    - Added `isApproved` filter for Level 2 Replies: `'populate[comment_replies][populate][comment_replies][filters][isApproved][$eq]': 'true'`
    - Migrated all user fields from `'id'` to `'documentId'`:
        * Root comments: `'populate[user][fields][1]': 'documentId'`
        * Level 1 replies: `'populate[comment_replies][populate][user][fields][1]': 'documentId'`
        * Level 2 replies: `'populate[comment_replies][populate][comment_replies][populate][user][fields][1]': 'documentId'`
    - Improved code comments for better maintainability.

#### Impact:
- Only approved comments and replies are now displayed at all nesting levels.
- Aligned with documentId migration strategy across the entire codebase.
- Ensures consistency between comments system and other modules using documentId.

---

## [Previous] - 2025-12-09

### Fix: Enhanced Error Logging & Hardcoded User ID for Comments
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Comments API** (`src/lib/commentsApi.js`)
    - Hardcoded `user: 1` in payload (replaced `MOCK_USER_ID` constant).
    - Added descriptive comment with emoji (👇) to highlight temporary implementation.
    - Added console logging of full payload before submission for debugging.

2. **API Client** (`src/lib/apiClient.js`)
    - Enhanced error handling to log Strapi error response body.
    - Now shows detailed JSON error messages from backend (validation errors, missing fields, etc.).
    - Improved debugging capability for 400 Bad Request errors.

#### Impact:
- Better debugging visibility when comment submission fails.
- Clear identification of validation issues from Strapi backend.
- Explicit hardcoded user ID until authentication system is implemented.

---

## [Previous] - 2025-12-09

### Feature: Polymorphic Comments System with Star Ratings
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **New API Layer** (`src/lib/commentsApi.js`)
    - `getComments(entityType, entityId)` - Fetch approved comments with threading (nested replies).
    - `submitComment(commentData)` - Submit comment with Mutual Exclusivity (only one entity) and moderation (isApproved: false).
    - Uses `MOCK_USER_ID = 1` until authentication is implemented.

2. **New Components** (`src/modules/comments/`)
    - `CommentItem.jsx` - Recursive component for rendering comments with avatar, username, stars (1-5), Persian date, and reply button.
    - `CommentsSection.jsx` - Comment submission form (with validation), comments list, star rating input, success/error messages.
    - Styling: Glassmorphism with RTL support and responsive design.

3. **Page Integrations**
    - Added `<CommentsSection />` to pages:
        * `src/app/articles/[slug]/page.js`
        * `src/app/courses/[slug]/page.js`
        * `src/app/products/[category]/[subcategory]/[slug]/page.js`
    - All comments are fetched via SSR (SEO-friendly).

4. **SCSS Import Fix**
    - Changed from `@use` to `@import '@/styles/base/mixins';` for Next.js/Turbopack compatibility.

#### Impact:
- Polymorphic comments system supporting articles, products, and courses.
- Threading (nested replies) up to 3 levels deep.
- Moderation workflow: all comments pending admin approval.
- Star rating (1-5 stars) for each comment.

**Files Created**: 5 new files (~1,500 lines of code)
**Files Modified**: 3 pages (articles, courses, products)

---

## [Previous] - 2025-12-09

### Feature: Breadcrumb Navigation Integration Across All Pages
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **About Page** (`src/app/about/page.js`)
    - Imported `Breadcrumb` component from `@/components/ui/BreadCrumb/Breadcrumb`.
    - Added breadcrumb navigation with items: `خانه > درباره ما`.
    - Placed breadcrumb before the header section with proper spacing.

2. **Contact Page** (`src/app/contact/page.js`)
    - Imported `Breadcrumb` component from `@/components/ui/BreadCrumb/Breadcrumb`.
    - Added breadcrumb navigation with items: `خانه > تماس با ما`.
    - Placed breadcrumb before the header section with proper spacing.

3. **Services Module** (Already Implemented)
    - **Services Listing** (`src/app/services/page.js`):
        * Breadcrumb already implemented: `خانه > خدمات`.
    - **Service Detail** (`src/app/services/[slug]/page.js`):
        * Breadcrumb already implemented with dynamic service title: `خانه > خدمات > [Service Title]`.
        * No changes required - already using shared `Breadcrumb` component.

4. **Courses Module**
    - **Courses Listing** (`src/app/courses/page.js`):
        * Imported `Breadcrumb` component from `@/components/ui/BreadCrumb/Breadcrumb`.
        * Added breadcrumb navigation with items: `خانه > دوره‌ها`.
        * Placed breadcrumb before the header section.
    - **Course Detail** (`src/app/courses/[slug]/page.js`):
        * Imported `Breadcrumb` component from `@/components/ui/BreadCrumb/Breadcrumb`.
        * Added dynamic breadcrumb navigation: `خانه > دوره‌ها > [Course Title]`.
        * Placed breadcrumb before the main content grid.
 

 #### Navigation:
 Home (خانه) ├── About (درباره ما) ├── Contact (تماس با ما) ├── Services (خدمات) │ └── Service Detail (عنوان خدمت) └── Courses (دوره‌ها) └── Course Detail (عنوان دوره)


#### Impact:
- **Consistent Navigation**: All main pages now follow the same breadcrumb pattern using the centralized `Breadcrumb` component.
- **Better UX**: Users can easily navigate back through the site hierarchy.
- **SEO Improvement**: Breadcrumbs provide structured navigation signals to search engines.
- **Accessibility**: Screen readers can better understand page hierarchy.
- **Unified Styling**: All breadcrumbs use consistent spacing, RTL support, and design tokens.
- **JSON-LD Support**: Breadcrumb component handles structured data for SEO rich snippets (if implemented).

## [Previous] - 2025-12-07

### Refactor: ArticleGrid URL-Driven Sorting
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **ArticleGrid Component** (`src/modules/articles/ArticleGrid/ArticleGrid.jsx`)
   - Imported Next.js navigation hooks: `useRouter` and `useSearchParams`.
   - Replaced local state (`useState`) with URL-based sorting logic.
   - Standardized SORT_OPTIONS values to direct Strapi format:
     * `'publishedAt:desc'` (instead of `'latest'`)
     * `'publishedAt:asc'` (instead of `'oldest'`)
   - Created `handleSortChange` handler that updates URL search params via `router.push`.
   - Removed mapping logic in fetch calls - now uses `sortBy` directly from URL.

2. **Articles Page** (`src/app/articles/page.js`)
   - Extract `sort` parameter from URL search params.
   - Pass `sort` value to `getArticlesPaginated()` for SSR.
   - Default value: `'publishedAt:desc'` when no sort param is present.

#### Impact:
- **URL-Driven State**: Sort preference is now in the URL, enabling bookmarking and sharing.
- **SSR Compatible**: Server-side rendering respects the sort parameter from URL.
- **Consistent Architecture**: Now matches ProductGrid pattern exactly.
- **No Breaking Changes**: Default sort remains "newest first" (publishedAt:desc).

---

### Fix: ArticleGrid & ProductGrid Empty State Handling
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **ArticleGrid & ProductGrid Components**
   - Refactored `ArticleGrid.jsx` and `ProductGrid.jsx` to handle empty states internally.
   - Now checks if `!isLoading && items.length === 0` to display `EmptyState` component.
   - Hides Sort Controls and Grid when empty, but KEEPS Category Filters visible (critical fix).

2. **Removed ListGuard Blocking**
   - Removed `ListGuard` wrapper from `src/app/articles/page.js` and `src/app/products/page.js`.
   - `ListGuard` was preventing the render of children (including filters) when the list was empty, causing a "dead end" UI.
   - Removing it allows the internal logic of the Grids to take over, displaying the friendly empty state while allowing users to navigate away via filters.

#### Impact:
- **Better UX**: Users are no longer stuck on a blank screen when a category is empty.
- **Navigation Preserved**: Category filters remain visible so users can easily switch to a different category.
- **Visual Feedback**: Clearly shows "No items found" message using the standard `EmptyState` component.

---

### Refactor: Standardized Page Headers (Services, Contact, About)
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Services Page** (`src/app/services/page.js` + `.module.scss`)
   - Simplified header structure: removed subtitle and complex hero classes.
   - Standardized class names to `.header` and `.title` matching Products/Articles pages.
   - Removed BEM-style (`.servicesPage__hero`) in favor of clean CSS modules.

2. **Contact Page** (`src/app/contact/page.js` + `.module.scss`)
   - Removed subtitle (`pageSubtitle`) for consistency.
   - Simplified header to just title.
   - Fixed alignment: removed `text-align: center` to match left/right-aligned standard.

3. **About Page** (`src/app/about/page.js` + `.module.scss`)
   - Converted large "Hero Section" to standard simple Header.
   - Removed introductory hero text.
   - Wrapped content in a single top-level `container` and stripped redundant inner containers.

#### Impact:
- **Visual Consistency**: All main pages (Products, Articles, Courses, Services, Contact, About) now share the exact same header styling.
- **Code Maintainability**: Unified CSS structure across all page modules.
- **Cleaner UI**: Removed inconsistent subtitles and center-alignments.

---

### Feature: About Us & Contact Us Pages Implementation
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **New Pages & Routes**
   - Created `src/app/about/page.js` featuring Hero, Mission, Team, and CTA sections with responsive layout.
   - Created `src/app/contact/page.js` featuring a functional contact form and information grid (Phone, Email, Address, Socials).
   - Applied Glassmorphism design system with RTL support using `page.module.scss` files.

2. **Contact Form Module** (`src/modules/contact/components/ContactForm.jsx`)
   - Implemented functional form component using `react-hook-form`.
   - Added validation rules: Name (required), Contact Info (Email/Phone regex), Body (min 20 chars).
   - Integrated logic for loading state, success feedback, and auto-reset.

3. **API Integration** (`src/lib/contactApi.js`)
   - Added `submitContactMessage` function to handle POST requests to `/api/contact-messages`.
   - Follows Repository Pattern consistent with `servicesApi` and `productsApi`.

#### Impact:
- Complete "About Us" and "Contact Us" sections now available.
- Functional integration with Strapi backend for message collection.
- Consistent visual experience with existing pages (Dark/Gold theme, Glassmorphism).

---

### Refactor: Services Module Standardization (Breadcrumbs, Styling & Empty States)
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **ServiceGrid Component** (`src/modules/services/ServiceGrid/ServiceGrid.jsx`)
   - Replaced manual empty state check with `ListGuard` wrapper.
   - Standardized "No services found" message consistent with other modules.
   - Removed component-level empty state logic in favor of shared component.

2. **Service Main Page** (`src/app/services/page.js` + `.module.scss`)
   - Added `Breadcrumb` navigation component to the listing page.
   - Standardized layout padding (`padding-top/bottom`) using design tokens to match Products page.
   - Renamed CSS classes (`.servicesPage` → `.main`, `.servicesPage__hero` → `.header`) for consistency with codebase patterns.

3. **Service Detail Page** (`src/app/services/[slug]/page.js` + `.module.scss`)
   - Replaced inline breadcrumbs HTML with shared `<Breadcrumb />` component.
   - Replaced custom empty state UI with standard Next.js `notFound()` call.
   - Applied consistent vertical rhythm (`--space-section-top-desktop`) to match listing pages.
   - Removed 230+ lines of unused styles (cleanup of breadcrumbs and empty state classes).

#### Impact:
- **Visual Consistency**: Services module (List & Detail) now matches Products module pixel-perfectly.
- **Improved Navigation**: Breadcrumbs now present on all Service pages.
- **Code Reduction**: Significant deletion of technical debt and duplicate styles.
- **Improved SEO**: Proper schema markup for breadcrumbs and handling of 404s.

---

## [Previous] - 2025-11-29

### Feature: IntroTextSection Module
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **New Module** (`modules/home/IntroTextSection/IntroTextSection.jsx` + `.module.scss`)
   - Created simple intro section with Persian text between HeroSection and AboutMentorSection.
   - Used design tokens for colors and responsive spacing.

#### Impact:
- Homepage now features the intro text between hero and about mentor sections.

---

##latest- 2025-11-27

### Enhancement: Hero Scroll CTA matches ornate badge asset
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Scroll CTA Component** (`components/ui/ScrollCTAButton/ScrollCTAButton.jsx`)
   - Replaced generic `.card-button` markup with bespoke hero button structure.
   - Added default Persian label fallback and preserved smooth-scroll behavior.
   - Wired new SCSS module className handling so other sections can extend styles safely.

2. **Component Styling** (`components/ui/ScrollCTAButton/ScrollCTAButton.module.scss`)
   - Implemented ornate badge background using `/images/herobutton.png`.
   - Added hover/focus states, RTL alignment, and typography aligned with hero art direction.
   - Ensured overlay text sits centered with non-interactive label span.

3. **Design Tokens** (`styles/base/_variables.scss`)
   - Introduced hero button sizing + shadow custom properties for reuse across sections.

#### Impact:
- Hero CTA now visually matches the provided badge image with centered copy.
- Keeps accessible text, smooth scroll UX, and reusable tokens for future ornate CTAs.

---

## [Previous] - 2025-11-24

### Fix: ArticleGrid Load More Button Logic (Replicated from ProductGrid)
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **ArticleGrid Component** (`modules/articles/ArticleGrid/ArticleGrid.jsx`)
   - Added `initialMeta` prop to receive pagination metadata from server.
   - Changed `hasMore` calculation from `initialArticles.length === PAGE_SIZE` to `(page < pageCount)` logic.
   - Updated initial state to use `initialMeta.pagination.page` and `initialMeta.pagination.pageCount`.
   - Refactored `useEffect` sync logic to compare `page` vs `pageCount` instead of array length vs total.
   - Refactored sort effect to use `(p?.page || 1) < (p?.pageCount || 1)` pattern.
   - Refactored `handleLoadMore` to use `(p?.page || nextPage) < (p?.pageCount || 1)` pattern.
   - Changed `setArticles([...articles, ...result.data])` to `setArticles(prev => [...prev, ...result.data])`.

2. **Articles Page** (`app/articles/page.js`)
   - Extracted `initialMeta = result.meta` from API response.
   - Passed `initialMeta` prop to ArticleGrid component.

#### Technical Details:
- **Before**: `hasMore` compared `articles.length < total` which was unreliable.
- **After**: `hasMore` compares `currentPage < totalPageCount` matching ProductGrid pattern.
- **Logic Pattern**: `setHasMore((p?.page || nextPage) < (p?.pageCount || 1))`
- **State Management**: Uses pagination metadata from Strapi response for accurate button visibility.

#### Impact:
- Load More button now disappears correctly when all articles are loaded.
- Behavior matches ProductGrid exactly (working reference implementation).
- No more false-positive button display when pagination is exhausted.
- Clean Architecture maintained with API Layer abstraction.

---

## [Previous] - 2025-11-24

### Refactor: Centralized Pagination Constants (Single Source of Truth)
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **New Constants File** (`lib/constants.js`)
   - Created centralized configuration file for pagination settings.
   - Defined `ARTICLES_PAGE_SIZE = 6` as single source of truth.
   - Added constants for other entities: `PRODUCTS_PAGE_SIZE`, `COURSES_PAGE_SIZE`, `SERVICES_PAGE_SIZE`.

2. **ArticleGrid Component** (`modules/articles/ArticleGrid/ArticleGrid.jsx`)
   - Removed hardcoded `const PAGE_SIZE = 1`.
   - Imported `ARTICLES_PAGE_SIZE` from `lib/constants`.
   - Updated all references from `PAGE_SIZE` to `ARTICLES_PAGE_SIZE`.

3. **Articles Page** (`app/articles/page.js`)
   - Imported `ARTICLES_PAGE_SIZE` from `lib/constants`.
   - Changed hardcoded `6` to `ARTICLES_PAGE_SIZE` in `getArticlesPaginated` call.
   - Updated comments to reference constants file.

4. **API Route Handler** (`app/api/articles/route.js`)
   - Imported `ARTICLES_PAGE_SIZE` from `lib/constants`.
   - Changed default pageSize from `'6'` to `String(ARTICLES_PAGE_SIZE)`.
   - Updated JSDoc to reflect dynamic default value.

#### Impact:
- **Single Source of Truth**: Only need to change `ARTICLES_PAGE_SIZE` in one file (`lib/constants.js`).
- **Consistency**: All three layers (Client, Server, API) now use the same value.
- **Maintainability**: Easier to manage pagination settings across the app.
- **Scalability**: Template ready for other entities (products, courses, services).
- **No Breaking Changes**: Existing functionality preserved, only improved architecture.

---

## [Previous] - 2025-11-24

### Fix: ArticleGrid Category Filter Persistence & Load More Logic
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **ArticleGrid Component** (`modules/articles/ArticleGrid/ArticleGrid.jsx`)
   - **FIX 1: Category Filter Persistence**
     * Added `categoryParam` variable that includes `activeSlug` in fetch URLs for sorting and pagination.
     * Now passes `&categorySlug=${activeSlug}` to all `/api/articles` requests when a category is selected.
     * Ensures category filter persists across sort changes and load more actions.
   
   - **FIX 2: Load More Button Visibility**
     * Changed `hasMore` calculation from comparing page count to comparing total items.
     * New logic: `setHasMore(articles.length < meta.pagination.total)` or `setHasMore(updatedArticles.length < totalArticles)`.
     * Prevents button from showing when all items are loaded.
   
   - **FIX 3: State Synchronization**
     * Added `activeSlug` to useEffect dependency array for props sync.
     * Resets `isInitialMount` ref when `activeSlug` changes to allow proper re-filtering.

2. **API Route Handler** (`app/api/articles/route.js`)
   - Added `categorySlug` query parameter support.
   - Extracts `categorySlug` from search params and passes to `getArticlesPaginated()`.
   - Updated JSDoc comments to document the new parameter.

#### Technical Details:
- Sort effect now constructs URL: `/api/articles?page=1&pageSize=${itemsToFetch}&sort=${sortQuery}${categoryParam}`
- Load more constructs URL: `/api/articles?page=${nextPage}&pageSize=${PAGE_SIZE}&sort=${sortQuery}${categoryParam}`
- Where `categoryParam = activeSlug ? `&categorySlug=${activeSlug}` : ''`
- API route signature: `getArticlesPaginated(page, pageSize, sort, categorySlug)`

#### Impact:
- Category filter now works correctly with sorting and pagination.
- Load more button accurately reflects when all articles are loaded.
- No data duplication or missing items during filter transitions.
- Maintains Clean Architecture with proper data flow through API layers.

---

### Fix: ArticleGrid stale state sync
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. `modules/articles/ArticleGrid/ArticleGrid.jsx`
   - Added hydration effect to align local `articles`, `page`, and `hasMore` with new `initialArticles` props when category filter updates.
   - Prevented stale grids when navigating between categories via SSR data refresh.

---

### Feature: Navbar Articles MegaMenu
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Server Component Updates (modules/layout/Navbar/Navbar.jsx)**
   - Imported `getArticleCategories` from articlesApi.
   - Fetched article categories in parallel with product categories.
   - Passed `articleCategoriesSnapshot` to client.

2. **Client Component Updates (modules/layout/Navbar/NavbarClient.jsx)**
   - Implemented generic `activeMegaMenu` state ('products' | 'articles').
   - Added "Articles" MegaMenu with image card layout.
   - Updated Mobile Drawer to include "Articles" tab and list.
   - Refactored hover logic to support multiple mega menus.

3. **Styling (modules/layout/Navbar/Navbar.module.scss)**
   - Added `.articleImage` and `.viewAllLink` styles for Article MegaMenu cards.
   - Reused existing grid and column styles.

---

## [Previous] - 2025-11-24

### Feature: Article Category Filter Integration
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Server Component Updates (app/articles/page.js)**
   - Imported `getArticleCategories` from articlesApi
   - Fetch categories and articles in parallel with Promise.all
   - Extract `activeCategory` from searchParams
   - Pass `categorySlug` to `getArticlesPaginated` for filtered results
   - Pass `categories` and `activeSlug` props to ArticleGrid

2. **Client Component Updates (ArticleGrid.jsx)**
   - Imported `ArticleCategoryFilter` component
   - Accept `categories` and `activeSlug` props
   - Render filter component before SortControls
   - No prop-drilling errors

3. **Integration Verification**
   - Category filter appears at top of articles page
   - Clicking category updates URL with ?category=slug
   - Server re-renders with filtered articles
   - Clean Architecture maintained with API Layer abstraction

---

##latest- 2025-11-24

### Fix: Article filter populate consistency
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Updated `lib/articlesApi.js` → `getArticlesPaginated` to include `articles_categories` in `populate` while keeping the confirmed filter key `filters[articles_categories][slug][$eq]` for reliable Strapi filtering.

---

### Fix: Article category filter query alignment
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Updated `lib/articlesApi.js` → `getArticles` and `getArticlesPaginated` to use Strapi’s exact relation key `filters[articles_categories][slug][$eq]` when filtering by category slug.
2. Ensured both limited and paginated article fetchers query the correct `articles_categories` relation to restore backend filtering.

---

### Feature: ArticleCategoryFilter Component
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Created `src/modules/articles/components/ArticleCategoryFilter.jsx` (client component):
   - Accepts `categories` array and `activeSlug` from URL query param.
   - Handles category selection via Next.js router.push with query param `?category=slug`.
   - Resets pagination on category change (removes `page` param).
   - Flat category structure (no subcategories logic).
   - Includes "نمایش همه" clear button when a category is active.

2. Created `src/modules/articles/components/ArticleCategoryFilter.module.scss`:
   - Glassmorphism design matching ProductCategoryFilter aesthetics.
   - Uses design tokens (`var(--color-text-primary)`, `var(--color-title-hover)`, etc.).
   - Responsive grid layout (auto-fit, minmax).
   - Active category indicator with checkmark badge.
   - Hover effects with transform, glow, and backdrop-filter.
   - RTL-friendly layout with proper spacing and alignment.

3. Visual Features:
   - Category cards with circular icon wrappers.
   - Gold accent borders and hover states.
   - Active state highlighting with checkmark badge.
   - Smooth transitions (0.4s cubic-bezier).
   - Responsive breakpoints (1024px, 600px).

#### Architecture Maintained:
- Client-side component for interactivity.
- URL-driven state management via Next.js router.
- Clean separation from products CategoryFilter (no code reuse).
- SCSS Modules with design token system.
- Accessibility features (aria-label, aria-current, focus states).

#### Impact:
- Articles listing page can now integrate category filtering UI.
- Consistent visual language with products section.
- SEO-friendly URL structure with query params.
- Ready for integration with ArticleGrid and /articles page

---

##latest- 2025-11-22

### Feature: ListGuard SSR Wrapper & EmptyState Upgrade
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Added `src/components/layout/ListGuard.jsx` server component to guard list views by rendering children, filter-based empty states, or global empty messaging using configurable `entityName` and `resetLink`.
2. Enhanced `src/components/ui/EmptyState/EmptyState.jsx` to support optional `description`, `actionLabel`, and `actionHref` props, enabling reusable messaging with contextual CTAs.
3. Updated `src/components/ui/EmptyState/EmptyState.module.scss` with design tokens, layout refinements, and button styling to match the dark theme with gold accents while remaining RTL-friendly.
4. Integrated `ListGuard` across `/products`, `/articles`, `/courses`, and `/services` pages so all grids now share consistent empty feedback driven by URL filters.

##latest- 2025-11-19

### Feature: Nested Route Structure for Products
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Nested Routing Implementation**
   - Created `src/app/products/[category]/page.js`: SSR listing page for categories.
   - Created `src/app/products/[category]/[subcategory]/page.js`: SSR listing page for subcategories.
   - Created `src/app/products/[category]/[subcategory]/[slug]/page.js`: SSR single product page (deep link).
   - Created `src/app/products/[category]/[subcategory]/[slug]/page.module.scss`: Styles for the deep product page.

2. **SSR Data Flow**
   - Listing pages await `params` and `searchParams` (Next.js 15 compatible).
   - Listing pages fetch data via `getProductsPaginated` with `categorySlug` or `subCategorySlug` filters.
   - Listing pages fetch `getCategoryTree` for sidebar/filter context.
   - Single product page fetches data via `getProductBySlug`.

3. **Component Reuse**
   - Reused `ProductsPageClient` for listing pages, passing initial data and category context.
   - Reused `ProductGallery` and styles for the single product page.

#### Architecture Maintained:
- **Clean Architecture**: API calls abstracted in `lib/productsApi.js`.
- **SSR First**: All initial data fetching happens on the server for SEO.
- **Client Hydration**: `ProductsPageClient` takes over for interactive filtering.
- **Backward Compatibility**: Old `src/app/product/[slug]` route preserved for future redirects.

### Feature: Breadcrumb Navigation & Legacy Redirects
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Added `src/components/ui/Breadcrumb.jsx` + `.module.scss` with LD-JSON schema output, RTL styling, and design tokens.
2. Injected `<Breadcrumb />` into `/products`, `/products/[category]`, `/products/[category]/[subcategory]`, and the deep product page to mirror URL hierarchy.
3. Refactored legacy `src/app/product/[slug]/page.js` into a pure `permanentRedirect` handler that resolves canonical category paths via `productsApi`.

#### Impact:
- Ensures consistent UX breadcrumbs with structured data for SEO rich snippets.
- Guarantees 301 redirects from legacy `/product/:slug` URLs to the nested `/products/:category/:sub/:slug` routes.
- Keeps Clean Architecture by reusing API layer helpers and SCSS modules.

### Enhancement: ProductCard Nested Route Support
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **ProductCard Component** (`src/components/cards/ProductCard/ProductCard.jsx`)
   - Added `constructProductUrl()` logic to dynamically build nested route URLs based on product categories.
   - Implements priority-based URL construction:
     * Priority 1: Subcategory (category with parent) → `/products/{parent}/{subcategory}/{slug}`
     * Priority 2: Root category → `/products/{category}/{slug}`
     * Priority 3: Fallback to legacy route → `/product/{slug}` (triggers 301 redirect)
   - Gracefully handles missing category data with fallback mechanism.

2. **Data Layer Enhancement** (`src/lib/strapiUtils.js`)
   - Updated `formatStrapiProducts()` to include `categories` array with parent relationship.
   - Each category now includes: `{ slug, name, parent: { slug, name } | null }`.
   - Supports both nested Strapi v4 structure (`attributes.parent.data`) and flat structure.

#### Impact:
- ProductCard now generates SEO-friendly nested URLs automatically.
- Backward compatible: cards without category data fall back to legacy route with 301 redirect.
- Maintains Clean Architecture with data formatting in strapiUtils layer.
- No breaking changes to existing ProductCard usage.

### Refactor: Extract ProductDetails Reusable Component
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **New Component** (`src/modules/products/ProductDetails/ProductDetails.jsx`)
   - Extracted UI structure from single product page into reusable component.
   - Props: `{ product, breadcrumbItems }`.
   - Renders: Breadcrumb, ProductGallery, product info (title, price, description), add-to-cart button.
   
2. **Styles Module** (`src/modules/products/ProductDetails/ProductDetails.module.scss`)
   - Migrated styles from page-level to component-level.
   - Responsive grid layout (2 columns desktop, 1 column mobile).
   - Uses design tokens for colors, spacing, and typography.

3. **Page Refactor** (`src/app/products/[category]/[subcategory]/[slug]/page.js`)
   - Removed inline JSX and styles import.
   - Now imports and renders `<ProductDetails />` component.
   - Keeps SSR data fetching logic (getProductBySlug, getCategoryTree, getProductBreadcrumbs).
   - Maintains metadata generation for SEO.

#### Impact:
- ProductDetails component ready for reuse in hybrid `/product/[slug]` route.
- Separates UI concerns from data fetching logic.
- Maintains Clean Architecture and SSR patterns.
- No visual or functional changes to the product detail page.

### Feature: Hybrid Route Resolver for /products/[category]/[subcategory]
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Hybrid Route Logic** (`src/app/products/[category]/[subcategory]/page.js`)
   - Step 1: Fetch category tree and validate category param.
   - Step 2: Check if `subcategory` param is a valid subcategory of `category`:
     * **YES**: Render subcategory listing page with ProductsPageClient.
     * **NO**: Attempt to fetch product using `getProductBySlug(subcategory)`:
       - **Product found**: Render ProductDetails component with correct breadcrumbs.
       - **Product not found**: Call `notFound()`.

2. **Dynamic Metadata Generation**
   - Detects route type (subcategory vs product) before generating metadata.
   - Subcategory: Generates listing page title/description.
   - Product: Generates product detail title/description using product data.
   - Invalid route: Returns "صفحه یافت نشد".

3. **Smart Breadcrumb Resolution**
   - For products: Attempts to match product's categories with URL category param.
   - Falls back to URL-based category if no match found in product data.
   - Ensures breadcrumbs remain accurate regardless of URL structure.

4. **Component Reuse**
   - Uses existing `ProductsPageClient` for listing pages.
   - Uses new `ProductDetails` component for product detail pages.
   - Maintains consistent UI/UX across both route types.

#### Architecture Maintained:
- **SSR First**: All data fetching on server for SEO optimization.
- **Clean Architecture**: API Layer abstraction (productsApi, categoriesApi).
- **DRY Principle**: Reuses ProductDetails component instead of duplicate JSX.
- **Next.js 15 Compatible**: Properly awaits params and searchParams.

#### Impact:
- Resolves routing collision: `/products/cat/slug` now intelligently determines if "slug" is a subcategory or product.
- Eliminates need for separate deep route at `/products/[category]/[subcategory]/[slug]`.
- Maintains backward compatibility with existing subcategory listing pages.
- Improves SEO with dynamic metadata generation based on route type.

##latest- 2025-11-18

### Fix: Remove forced `page` query resets in ProductsPageClient
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. `src/modules/products/ProductsPageClient/ProductsPageClient.jsx`
   - Deleted the `params.set('page', '1')` calls in `handleSelectCategory`, `handleSelectSub`, and `handleChangeSort`.
   - Keeps existing search params intact so URLs no longer auto-append `?page=`.

#### Impact:
- Prevents redundant `page` query strings during category/subcategory selection and sort changes.
- Router updates now preserve whatever paging logic already exists in the URL or defaults.

### UI Update: Modern Glassmorphism Styles for CategoryFilter
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. `src/modules/products/ProductsPageClient/components/CategoryFilter.module.scss`
    - Completely overhauled styles to use modern glassmorphism.
    - Added `backdrop-filter: blur`, semi-transparent backgrounds, and subtle borders.
    - Enhanced gradients for cards and active states to match the dark/gold theme.
    - Improved hover and focus states with smooth transitions, scaling, and shadows.
    - Refined layout spacing and responsiveness.
    - Used complex box-shadows for depth and glowing effects.

#### Impact:
- Significant visual upgrade to the category filter on the products page, aligning it with a more premium, modern aesthetic.

##latest- 2025-11-17

### Enhancement: Products List Category UI Refresh
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Rebuilt the `/products` category selector UI to visually match `ProductCategoriesSection`:
   - Updated `src/modules/products/ProductsPageClient/components/CategoryFilter.jsx` to render circular icon cards with Next/Image and fallback placeholders.
   - Added structured layout states: full grid when no category is selected and split layout (selected card on the right, subcategories list on the left) when a category is active.
2. Created `src/modules/products/ProductsPageClient/components/CategoryFilter.module.scss` mirroring the home category card styling:
   - Reused theme tokens (`var(--color-overlay)`, `var(--gradient-card-vertical)`, etc.), RTL grid, hover/focus transitions, and pill-style subcategory highlights.
   - Added responsive rules to collapse the two-column layout on tablet/mobile while keeping typography/spacing consistent with the homepage.
3. Preserved SPA filtering contracts by keeping `onSelectCategory`/`onSelectSubCategory` signatures untouched and including reset actions ("بازگشت" + "همه زیر‌دسته‌ها") to avoid URL or pagination regressions.
4. Fixed back navigation by preventing the category reset button from firing the subcategory router logic, eliminating the stuck state on `/products/{category}`.
5. Added `public/images/placeholder.png` fallback so circular category icons always render even when Strapi lacks an image.

#### Impact:
- Category icons, typography, and spacing now stay consistent between Home and Products pages.
- Subcategory list provides clear active states without interfering with existing load-more, sort, or routing logic.


##latest- 2025-11-06

### Enhancement: productsApi.getProductsPaginated category/subcategory filters
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Extended `src/lib/productsApi.js` → `getProductsPaginated(page, pageSize, sort, options?)` to accept optional `{ categorySlug, subCategorySlug, subSlugs = [] }` without breaking existing callers.
2. Implemented Strapi v4 filters via `URLSearchParams`:
   - If `subCategorySlug` → `filters[subCategory][slug][$eq] = {subCategorySlug}`
   - Else if `categorySlug` → OR filter:
     - `filters[$or][0][category][slug][$eq] = {categorySlug}`
     - `filters[$or][1][subCategory][slug][$in] = subSlugs.join(',')` (when provided)
3. Preserved output structure: `{ data, meta }` unchanged, including pagination meta and error fallback.

#### Notes:
- `subSlugs` should be provided by SSR from `categoriesApi` when a category is selected to include all its subcategories.
- No UI/component changes required; API remains backward-compatible with previous 3-arg calls.

### Feature: SSR Initial Render from URL for /products
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Updated `src/app/products/page.js` to read `searchParams` → `category`, `sub`, `sort`, `page`.
2. When `category` present and `sub` absent, fetched `getCategoryTree()` and derived `subSlugs` for that category.
3. Called `getProductsPaginated(page, 6, sort, { categorySlug, subCategorySlug, subSlugs })` for initial SSR data.
4. Fetched categories snapshot via `getCategoryTree()` and passed to client.
5. Added `src/modules/products/ProductsPageClient/ProductsPageClient.jsx` (client) to receive initial props and render `ProductGrid`.

#### Impact:
- Enables direct URL-driven SSR filtering and pagination for better SEO and first paint.
- Keeps Clean Architecture with API Layer and server→client data flow.

### Feature: CategoryFilter component (UI-only, emits events)
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Added `src/modules/products/components/CategoryFilter.jsx` (client component) with two states:
   - No active category: shows scrollable category pills.
   - Active category: large selected box on the right + subcategory pills on the left.
2. Added `src/modules/products/components/CategoryFilter.module.scss` styled with theme tokens:
   - Dark background, gold accents, RTL-friendly layout.
   - Hover/active states, subtle elevation, responsive grid (2→1 columns on mobile).
3. Accessibility: `aria-label`, `aria-pressed`, `type="button"` on interactive elements.

#### Usage Notes:
- Receives data via props; emits only events: `onSelectCategory(slug)`, `onSelectSubCategory(slug)`.
- Update URL handling in the parent to set `?category=` and `?sub=` as needed.

### Enhancement: ProductGrid filtering + pagination reset logic
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. `src/modules/products/ProductGrid/ProductGrid.jsx`
   - Added `category` and `sub` query params to all fetches.
   - On `sort`, `activeCategory`, or `activeSubCategory` change: refetch from page 1 and replace products.
   - On load more: fetch next page in current filters and append.
   - Standardized `PAGE_SIZE` to 6 and aligned `page`/`hasMore` with API meta.

### Enhancement: /api/products route supports filters (category, sub, sort, page, pageSize)
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Updated `src/app/api/products/route.js` to parse `category`, `sub`, `sort`, `page`, `pageSize`.
2. When a category is selected without a subcategory, derive `subSlugs` via `getCategoryTree()` for inclusive filtering.
3. Call `getProductsPaginated(page, pageSize, sort, { categorySlug, subCategorySlug, subSlugs })` and return `{ data, meta }` unchanged.

#### Impact:
- Ensures URL-driven UX for `/products`, `/products/{category}`, and `/products/{category}/{sub}` with correct filtering and pagination.

### Fix: Align product filters with `categories` relation slug equality
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Updated `src/lib/productsApi.js` → `getProductsPaginated` query build to:
   - If `subCategorySlug`: `filters[categories][slug][$eq] = subCategorySlug`
   - Else if `categorySlug`: `filters[categories][slug][$eq] = categorySlug`

#### Rationale:
- Backend expects filtering via unified `categories` relation; removes previous OR/in logic.

---

##latest- 2025-11-04

### Enhancement: Dropdown Arrow on "محصولات" with Open State
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Added a toggleable arrow icon next to the "محصولات" link that rotates upward when the MegaMenu is open.
2. Updated `src/modules/layout/Navbar/NavbarClient.jsx` to render the arrow SVG and bind its open state to `isMegaMenuOpen`.
3. Updated `src/modules/layout/Navbar/Navbar.module.scss` with `.navLinkContent`, `.dropdownIcon`, and `.dropdownIconOpen` classes using design tokens (no hard-coded values).

#### Impact:
- Clear affordance for dropdown behavior on desktop navigation, aligned with RTL and theme tokens.

---

[2025-11-03] - Global Fonts Registration (100% Complete)
- Added src/styles/base/_fonts.scss with @font-face for IRANSans (200/300/400/500/700), Lalezar, and Far Shafigh
- Imported fonts in src/styles/main.scss before typography to ensure availability
- No runtime changes; typography now can use registered families via CSS

##latest- 2025-11-03

### Refactor: Navbar MegaMenu Hover Stability & Animation
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. Always-render mega menu with state-driven visibility class (`.megaMenuVisible`) to prevent flicker and gaps.
2. Unified SCSS: removed duplicate `.megaMenu` blocks and hover-coupled open rules; single transition source of truth.
3. Smooth open/close with opacity+translate transitions; pointer-events managed for stability.
4. Hover intent: delayed close (300ms) with enter/leave handlers to avoid accidental dismiss.
5. Accessibility: added `aria-label` and `aria-hidden`, and focus enter/leave to support keyboard.

#### Files Updated:
- `src/modules/layout/Navbar/NavbarClient.jsx`
- `src/modules/layout/Navbar/Navbar.module.scss`

#### Architecture & Styling:
- Preserved SSR snapshot from `Navbar.jsx` and API layer.
- SCSS Modules with design tokens; RTL friendly.

### Migration: Product Detail Page Route Refactor
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **Route Migration**
   - Moved: `src/app/products/[slug]/page.js` → `src/app/product/[slug]/page.js`
   - Moved: `src/app/products/[slug]/page.module.scss` → `src/app/product/[slug]/page.module.scss`
   - Rationale: Distinguish singular product detail page from plural products listing page

2. **Component Updates**
   - `src/components/cards/ProductCard/ProductCard.jsx`: Updated link from `/products/${slug}` to `/product/${slug}`
   - ProductGrid.jsx uses ProductCard component, so links are automatically updated

3. **Verification**
   - ✅ SSR rendering preserved (metadata generation intact)
   - ✅ Dynamic route params properly handled with `await params`
   - ✅ API Layer abstraction maintained (getProductBySlug from productsApi.js)
   - ✅ Error handling preserved (notFound() for invalid slugs)
   - ✅ Styles and layout unchanged

#### Architecture Maintained:
- Clean Architecture with API Layer abstraction
- Repository Pattern (productsApi.js)
- Server-Side Rendering (SSR) for SEO
- SCSS Module structure
- RTL layout support

#### Old Route:
- `src/app/products/[slug]/page.js` still exists (can be cleaned up or repurposed for categories)

#### New Route:
- `src/app/product/[slug]/page.js` now active for singular product detail pages
[2025-11-02] - Categories & Products API System Rebuild v2.1 (100% Complete)
|- Refactored src/lib/categoriesApi.js with three new API functions
|- Added getMainCategories() - fetches parent categories (parent=null) for Home page
|- Added getCategoryTree() - fetches hierarchical category structure for Navbar
|- Added getCategoryWithProducts(slug) - fetches complete category with sub-categories and products
|- Each function implements error handling with try/catch and returns empty array on failure
|- All functions follow Clean Architecture with apiClient abstraction layer
|- All functions use formatStrapiCategories for consistent data formatting
|- Refactored src/lib/strapiUtils.js with new category data structure
|- Replaced formatStrapiCategories() to work with Strapi's attributes structure
|- Function now properly handles item.attributes pattern for category data
|- Added formatStrapiProduct() helper function for product formatting within categories
|- Function extracts id, title, slug, price, and image URL from Strapi product data
|- Enhanced formatStrapiCategories() to handle nested subCategories structure
|- Function supports both parent categories and their sub-categories with products
|- Properly formats image URLs using formatSingleImage() with fallback handling
|- Fixed corrupted strapiUtils.js file by removing duplications and cleaning up code
|- Removed duplicate function definitions and corrupted code sections
|- Preserved all other formatters: formatStrapiProducts, formatStrapiArticles, formatStrapiCourses, formatStrapiServices, formatStrapiTestimonials, formatStrapiFaqs
|- All functions follow Repository Pattern for API abstraction
|- API queries properly use Strapi populate syntax with nested relations
|- Data structure compliance: Category → SubCategory → Product relationships intact
|- Zero hardcoded values - all configurations follow project standards
|- Zero linting errors - code quality verified across both files
|- Followed CONTEXT_CATEGORIES_SYSTEM_API_v2.md specifications exactly
|- Architecture compliance: ✅ Clean Architecture, ✅ API Layer abstraction, ✅ Error handling, ✅ SSR ready
|- All code follows project modular structure in src/lib/

## [Previous] - 2025-11-02

### Feature: Navbar MegaMenu with SSR Category Fetching
**Status**: ✅ COMPLETED (100%)

#### Changes Made:
1. **SSR Category Fetching**
   - Created: `src/modules/layout/Navbar/NavbarClient.jsx` (client component)
   - Updated: `src/modules/layout/Navbar/Navbar.jsx` (server component)
   - Integrated: `getCategoryTree()` from `categoriesApi.js` with ISR caching (revalidate: 300s)
   - Data flows from SSR parent to client child component

2. **Desktop MegaMenu**
   - Hover-activated dropdown with 3-column grid (responsive: 2 columns @ tablet, 1 @ mobile)
   - Category titles with subcategories listed beneath each parent
   - Smooth fade-in/fade-down animation (translateY + opacity)
   - Backdrop blur effect with dark overlay background
   - Persistent on hover with pointer-events management

3. **Mobile Accordion Menu**
   - "محصولات" button expands to show all categories
   - Each category displays with nested subcategories
   - Animated slideDown expansion with border accent
   - Proper state management for open/closed accordion
   - Closes menu on navigation link click

4. **SCSS Module Updates**
   - Added: `.megaMenuWrapper`, `.megaMenu`, `.megaMenuGrid`, `.megaMenuColumn`
   - Added: `.mobileCategoryToggle`, `.mobileSubMenu`, `.mobileCategoryGroup`, `.mobileSubList`
   - Used design tokens: `var(--color-overlay)`, `var(--color-text-primary)`, `var(--font-md)`, etc.
   - Applied mixins: `@include m.hover-transition`, `@include m.respond()`
   - Animations: slideDown keyframe for mobile accordion

5. **Accessibility Features**
   - aria-expanded on mobile category toggle
   - aria-label for navigation elements
   - role="menu" and role="menuitem" for semantic structure
   - Keyboard-friendly buttons
   - Focus management with body scroll lock

#### Architecture Maintained:
- Server-Side Rendering (SSR) for category data
- Client-Side Interactivity (CSR) for UI state
- Clean component separation (Navbar → NavbarClient)
- API Layer abstraction (categoriesApi.js)
- SCSS Modules with design tokens
- RTL layout support
- Performance optimized (useCallback, ISR caching)

#### Technical Implementation:
- Server Component: Fetches categories via `getCategoryTree()`
- Client Component: Handles state (menu open/close, accordion expansion)
- Prop drilling: categories passed from server to client
- URL structure: `/products?category={slug}&subcategory={subslug}`

---

[2025-10-27] - ProductCategoriesSection Strapi Integration (100% Complete)
- Refactored categoriesApi.js to fetch only main categories (parent=null)
- Updated API query to include image population: populate[image][fields]
- Modified formatStrapiCategories() in strapiUtils.js to use item.attributes structure
- Updated CategoryCard.jsx to handle full Strapi image URLs (already prefixed)
- Added unoptimized flag for placeholder images
- ProductCategoriesSection now receives data from /api/home → getAllCategories()
- Removed dependency on mock category data
- All category icons prefixed with STRAPI_API_URL via formatSingleImage()
- Added top-level documentation comments: "This section now uses live Strapi categories via API Layer abstraction"
- Architecture compliance: ✅ Clean Architecture, ✅ API Layer abstraction, ✅ SSR data flow
- Data flow: HomePage → /api/home → getAllCategories() → formatStrapiCategories() → ProductCategoriesSection

[2025-10-25] - Domain Pages Refactoring to Use Centralized API Modules (100% Complete)
- Refactored /src/app/products/page.js to use getAllProducts() from productsApi.js
- Removed direct fetch() call and manual URL construction
- Removed getInitialProducts() helper function (no longer needed)
- Removed STRAPI_API_URL constant and formatStrapiProducts import
- Simplified to single await: const initialProducts = await getAllProducts()
- Added architectural documentation explaining API Layer abstraction
- Refactored /src/app/products/[slug]/page.js to use getProductBySlug()
- Removed local getProductBySlug() function with direct fetch
- Removed generateStaticParams() (not needed with current approach)
- Uses API Layer abstraction for metadata generation
- Clean implementation following Repository Pattern
- Refactored /src/app/articles/page.js to use getAllArticles() from articlesApi.js
- Removed direct fetch() call and manual URL construction
- Removed getInitialArticles() helper function (no longer needed)
- Removed STRAPI_API_URL constant and formatStrapiArticles import
- Simplified to single await: const initialArticles = await getAllArticles()
- Added architectural documentation explaining API Layer abstraction
- Refactored /src/app/articles/[slug]/page.js to use getArticleBySlug()
- Removed local getArticleBySlug() function with direct fetch
- Removed generateStaticParams() (not needed with current approach)
- Uses API Layer abstraction for metadata generation
- Clean implementation following Repository Pattern
- Refactored /src/app/courses/page.js to use getAllCourses() from coursesApi.js
- Removed direct fetch() call and manual URL construction
- Removed getInitialCourses() helper function (no longer needed)
- Removed STRAPI_API_URL constant and formatStrapiCourses import
- Simplified to single await: const initialCourses = await getAllCourses()
- Added architectural documentation explaining API Layer abstraction
- Refactored /src/app/courses/[slug]/page.js to use getCourseBySlug()
- Removed local getCourseBySlug() function with direct fetch
- Removed generateStaticParams() (not needed with current approach)
- Uses API Layer abstraction for metadata generation
- Clean implementation following Repository Pattern
- All refactored pages now follow Clean Architecture principles
- Zero direct fetch() calls anywhere in domain pages
- Consistent architecture across Products, Articles, Courses, Services domains
- All pages use SSR with API Layer abstraction (Repository Pattern)
- Reduced code duplication by removing local data fetching functions
- Improved maintainability - backend changes only affect API modules
- Better separation of concerns - pages focus on UI, API modules handle data
- Each refactored file includes clear documentation comments:
  * "Data fetched via API Layer abstraction (XApi.js)"
  * Architecture compliance notes
  * Repository Pattern benefits
  * Clean separation of concerns
- Architecture compliance: ✅ No direct fetch, ✅ Repository Pattern, ✅ Clean Architecture, ✅ SSR

[2025-10-25] - Domain API Modules Implementation (100% Complete)
- Created /src/lib/productsApi.js following Repository Pattern architecture
- Function getAllProducts() fetches all products with populated relations
- Endpoint: /api/products?populate=*
- Uses apiClient for HTTP communication (follows abstraction pattern)
- Uses formatStrapiProducts from strapiUtils.js for data formatting
- Returns empty array on error for graceful degradation (never throws)
- Function getProductBySlug(slug) fetches single product by slug
- Implements Strapi filter syntax: filters[slug][$eq]={slug}&populate=*
- Returns null on error or if product not found
- Added 180+ lines of comprehensive documentation in Persian
- All comments explain Repository Pattern, SSR flow, error handling
- Data contract: id, slug, title, price, shortDescription, images, image
- Created /src/lib/articlesApi.js following Repository Pattern architecture
- Function getAllArticles() fetches all articles sorted by publishedAt
- Endpoint: /api/articles?populate=*&sort=publishedAt:desc
- Uses apiClient for HTTP communication (follows abstraction pattern)
- Uses formatStrapiArticles from strapiUtils.js for data formatting
- Returns empty array on error for graceful degradation (never throws)
- Function getArticleBySlug(slug) fetches single article by slug
- Implements Strapi filter syntax: filters[slug][$eq]={slug}&populate=*
- Returns null on error or if article not found
- Added 180+ lines of comprehensive documentation in Persian
- All comments explain Repository Pattern, SSR flow, error handling
- Data contract: id, slug, title, excerpt, date, cover
- Created /src/lib/coursesApi.js following Repository Pattern architecture
- Function getAllCourses() fetches all courses sorted by createdAt
- Endpoint: /api/courses?populate=*&sort=createdAt:desc
- Uses apiClient for HTTP communication (follows abstraction pattern)
- Uses formatStrapiCourses from strapiUtils.js for data formatting
- Returns empty array on error for graceful degradation (never throws)
- Function getCourseBySlug(slug) fetches single course by slug
- Implements Strapi filter syntax: filters[slug][$eq]={slug}&populate=*
- Returns null on error or if course not found
- Added 180+ lines of comprehensive documentation in Persian
- All comments explain Repository Pattern, SSR flow, error handling
- Data contract: id, slug, title, price, shortDescription, image
- All three modules follow identical architecture pattern for consistency
- No direct fetch() calls anywhere - all use apiClient abstraction
- KISS principle maintained with clean, readable code
- Architecture compliance: ✅ Repository Pattern, ✅ Domain-specific APIs, ✅ Graceful error handling
- Each module includes comprehensive documentation explaining:
  * Purpose and role in architecture
  * Repository Pattern benefits
  * SSR data flow (5-step process)
  * Error handling strategy (Graceful Degradation)
  * Caching strategy and revalidation options
  * Data contract with example structure
  * Four architectural benefits (Separation, Testability, Maintainability, Flexibility)
  * Usage examples showing wrong vs correct patterns

[2025-10-25] - Service Single Page Implementation (100% Complete)
- Created /src/app/services/[slug]/page.js as dynamic route for individual service details
- Implements Server-Side Rendering (SSR) with async/await pattern for optimal SEO
- Uses Next.js 15 async params pattern: const { slug } = await params
- Fetches data via getServiceBySlug(slug) from servicesApi.js (no direct fetch)
- Implements generateMetadata() for dynamic SEO with service title and description
- Handles invalid slugs gracefully with inline EmptyState (no crashes)
- EmptyState displays user-friendly message with breadcrumbs and return link
- Renders ServiceSingle component when service is found
- Includes Breadcrumbs navigation: خانه / خدمات / [Service Title]
- Breadcrumbs provide hierarchical navigation and SEO benefits
- Added 350+ lines of comprehensive architectural documentation explaining:
  * Purpose and SSR implementation details
  * Architecture compliance checklist
  * Complete data flow from URL to HTML (8-step visualization)
  * Params extraction pattern for Next.js 15
  * Why NOT to use fetch() directly with comparison
  * Empty state handling strategy vs notFound()
  * SEO optimization with metadata generation
  * Error handling at 3 levels (API, page, component)
  * Future enhancements (structured data, related services, reviews)
  * Testing strategy and performance considerations
  * Accessibility features and semantic HTML
- Created /src/app/services/[slug]/page.module.scss with BEM naming
- Implements breadcrumbs navigation styles with hover effects
- Styles for empty state (title, message, button) with centered layout
- Responsive design with mobile-first approach (desktop → tablet → mobile)
- Uses design tokens from styles.md for colors, typography, spacing
- Includes hover effects on links and buttons with smooth transitions
- RTL support with direction-agnostic flex layouts
- Full accessibility compliance (color contrast, focus states, semantic structure)
- Added 200+ lines of SCSS documentation explaining:
  * BEM structure and component organization
  * Design system compliance checklist
  * Responsive breakpoints and adjustments
  * Color and typography token usage
  * Hover effects and transitions
  * RTL and accessibility support

[2025-10-25] - Services Page API Abstraction Layer Implementation (100% Complete)
- Created /src/lib/apiClient.js as foundational HTTP wrapper for all API communication
- Implements base fetch abstraction following Repository Pattern architecture
- Function apiClient(endpoint, options) handles all HTTP requests to Strapi backend
- Constructs full URLs using NEXT_PUBLIC_STRAPI_API_URL environment variable
- Merges custom headers with default Content-Type: application/json
- Defaults to cache: 'no-store' for always-fresh SSR data (can be overridden)
- Implements comprehensive error handling with try/catch and status logging
- Throws errors to allow domain APIs to handle recovery strategies
- Added 100+ lines of detailed documentation in Persian (Farsi) as per CONTEXT_apiClient.md
- All comments explain architecture, data flow, usage patterns in Persian
- Documented why components should never use fetch() or apiClient() directly
- Explained layered architecture: Component → Domain API → apiClient → Strapi
- Includes future extensibility notes (JWT Authentication, Retry Logic, Interceptors, Rate Limiting)
- Created /src/lib/servicesApi.js as domain-specific API layer for services
- Function getAllServices() fetches all services with populated image relation
- Endpoint: /api/services?populate=image&sort=createdAt:desc
- Uses apiClient for HTTP communication (follows abstraction pattern)
- Uses formatStrapiServices for data formatting (strapiUtils.js)
- Returns empty array on error for graceful degradation (never throws)
- Function getServiceBySlug(slug) fetches single service (future use for detail page)
- Implements Strapi filter syntax: filters[slug][$eq]={slug}
- Returns null on error or if service not found
- Added 150+ lines of comprehensive documentation in Persian (Farsi) as per CONTEXT_servicesApi.md
- All comments explain Repository Pattern, SSR flow, error handling in Persian
- Added detailed Persian documentation explaining:
  * Architecture role and design pattern (Repository Pattern)
  * SSR data flow (6-step process from Next.js to browser)
  * Error handling strategy and graceful degradation
  * Caching strategy and revalidation options
  * Data contract (service object structure)
  * Architectural benefits (separation of concerns, testability, maintainability, flexibility)
  * Usage examples showing wrong vs correct patterns
- Refactored /src/app/services/page.js to use new API abstraction layer
- Replaced direct fetch() call with getAllServices() import from servicesApi.js
- Removed getInitialServices() helper function (no longer needed)
- Removed STRAPI_API_URL constant and manual URL construction
- Removed fetch configuration (options, error handling, response parsing)
- Simplified page to single await: const services = await getAllServices()
- Added 200+ lines of architectural documentation explaining:
  * Purpose and SSR implementation
  * Architecture compliance checklist (✅ Server Component, API layer, project structure, etc.)
  * Complete SSR data flow (6 steps from Next.js to HTML response)
  * Why SSR benefits (SEO, performance, UX, bundle size)
  * Component type and execution context
  * All 6 architectural layers involved
  * Error handling and empty state flow
  * Why NOT to use fetch() directly (3 reasons with examples)
  * Hero section design and responsive typography
  * ServiceGrid behavior and data shape
  * Separation of concerns visualization
  * Data flow ASCII diagram (Browser → Next.js → getAllServices → apiClient → Strapi → format → render)
  * Future enhancements (breadcrumbs, filtering, search, pagination, loading.js, error.js)
  * Testing strategy (mocking, empty state, various counts, visual regression)
  * Performance considerations (SSR benefits, caching, images, bundle size)
- Zero direct fetch() calls in any component (follows ARCHITECTURE_RULES.md)
- Zero hardcoded API URLs in components (only in apiClient.js)
- All Strapi query logic centralized in servicesApi.js
- Consistent error handling pattern across all API functions
- Enhanced maintainability: API changes only need updates in API layer
- Improved testability: API layer can be mocked independently
- Better code organization: Clear separation between UI and data layers
- Zero linter errors - code quality verified across all files
- Followed CONTEXT_ServicePage.md specifications for API structure
- Followed ARCHITECTURE_RULES.md Rule 2.2: All fetch through lib/api.js pattern
- All code heavily documented with JSDoc, inline comments, and architectural notes
- Implementation includes comprehensive comments explaining SSR data flow and architectural compliance per requirements

[2025-10-25] - Complete API Layer Refactor - Data Fetching Centralization (100% Complete)
- Created centralized API abstraction layer: src/lib/api.js per ARCHITECTURE_RULES.md Rule 2.2
- Refactored ALL Server Components to eliminate direct fetch() calls
- All API communication now flows through single source of truth: api.js
- Implemented 12 primary API functions for Products, Articles, Courses, Services, Categories
- All functions return standardized { data, error } format for consistent error handling
- Added comprehensive JSDoc documentation for every API function with @param and @returns
- Documented Strapi v5 query syntax (populate, filters, sort, pagination) in detail
- Products API: getProducts() and getProductBySlug() with pagination and sorting support
- Articles API: getArticles() and getArticleBySlug() with publishedAt sorting
- Courses API: getCourses() and getCourseBySlug() with price sorting
- Services API: getServices() and getServiceBySlug() - fetches all services at once (no pagination)
- Categories API: getCategories() for future category navigation
- Encapsulated complex Strapi filter syntax: filters[slug][$eq] for exact match queries
- Implemented pagination syntax: pagination[page] and pagination[pageSize]
- All API functions use populate: '*' to fetch all related fields
- Refactored /products page.js to use getProducts() instead of direct fetch
- Removed STRAPI_API_URL constant and URL construction logic from component
- Updated getInitialProducts() to use centralized API with proper error handling
- Validates { data, error } response and provides empty array fallback
- Refactored /products/[slug] page.js to use getProductBySlug()
- Updated generateStaticParams() to use getProducts() for SSG slug generation
- Removed all hardcoded API URLs and query parameter logic
- Refactored /articles page.js to use getArticles() instead of direct fetch
- Updated getInitialArticles() with centralized API and formatStrapiArticles
- Removed console.log debugging statements and STRAPI_API_URL references
- Refactored /articles/[slug] page.js to use getArticleBySlug()
- Updated to use getStrapiURL() for consistent image URL handling
- Removed manual URL construction and fetch configuration
- Refactored /courses page.js to use getCourses() instead of direct fetch
- Updated getInitialCourses() with proper error handling and data validation
- Removed manual population parameter construction
- Refactored /courses/[slug] page.js to use getCourseBySlug()
- Updated to use getStrapiURL() for media/image URL handling
- Improved curriculum mapping and fallback handling
- Refactored /services page.js to use getServices() instead of direct fetch
- Updated extensive existing documentation to reflect API layer usage
- Simplified getInitialServices() by removing manual fetch and URL logic
- Refactored /services/[slug] page.js to use getServiceBySlug()
- Updated to use getStrapiURL() for consistent URL construction
- Removed all STRAPI_API_URL constant references from component
- All pages now import API functions: import { getProducts } from '@/lib/api'
- Removed unused strapiUtils imports where formatters are not needed
- Added error logging with descriptive messages for debugging
- All components handle { data, error } destructuring pattern consistently
- Validate data.data exists before accessing (Strapi structure)
- All generateStaticParams() functions now use centralized API
- Use large pageSize (1000) to fetch all items for static generation
- Filter out items without slugs to prevent build errors
- All functions include try/catch blocks for error resilience
- Empty array returns on error allow graceful degradation
- Pages still render with empty states when API unavailable
- Zero direct fetch() calls remaining in any Server Component
- Zero hardcoded STRAPI_API_URL references in pages (only in api.js and strapiUtils.js)
- All Strapi query logic centralized and documented in one place
- Consistent code patterns across all listing and detail pages
- Improved maintainability: API changes only need updates in api.js
- Better testability: API layer can be mocked independently
- Reduced code duplication: No repeated fetch logic across components
- Enhanced error handling: Centralized error management with { data, error }
- Zero linter errors - code quality verified across all refactored files
- Followed ARCHITECTURE_RULES.md Rule 2.2: "All fetch requests must be handled by lib/api.js"
- All code heavily commented explaining refactor purpose and centralized architecture

[2025-10-25] - StrapiUtils Core API Functions Enhancement (100% Complete)
- Enhanced strapiUtils.js with comprehensive documentation following CONTEXT_StrapiUtils.md
- Added getStrapiURL(path) function for URL construction with base URL handling
- Function combines NEXT_PUBLIC_STRAPI_API_URL with any path (e.g., "/api/services")
- Handles both empty paths and paths with/without leading slashes
- Added fetchStrapiAPI({ endpoint, params, options }) as primary API communication function
- Function is the single source of truth for all Strapi fetch() operations per ARCHITECTURE_RULES.md
- Converts params object to URLSearchParams for proper query string encoding
- Handles complex Strapi filters like 'filters[slug][$eq]' correctly in URLs
- Implements comprehensive error handling with try/catch for network and parsing errors
- Returns standardized { data, error } format as mandated by architecture rules
- Success response: { data: {...}, error: null }
- Error response: { data: null, error: { status: number, message: string } }
- Logs errors with status codes and URLs for debugging purposes
- Merges user-provided headers with default Content-Type: application/json
- Added extensive JSDoc documentation for all functions with @param and @returns
- Added detailed inline comments explaining every step of the fetch process
- Enhanced formatSingleImage() with comprehensive documentation
- Function handles both absolute URLs (http/https) and relative paths
- Prepends STRAPI_API_URL for relative paths, uses absolute URLs as-is
- Provides placeholder image (Picsum) when image data is missing
- Extracts alternativeText for accessibility with empty string fallback
- Enhanced all formatter functions (Products, Articles, Courses, Services) with detailed JSDoc
- Added Strapi structure documentation showing input data format
- Added output structure documentation showing formatted data format
- Documented rich text extraction pattern: description[0].children[0].text
- Explained image handling differences (arrays vs single objects) per content type
- Products: Multiple images array + single thumbnail for cards
- Articles: Single cover image field
- Courses: Media array (takes first item)
- Services: Single image field + optional link field
- Added detailed inline comments throughout all formatters explaining each transformation
- Organized file with clear section headers and separators
- All functions follow consistent documentation patterns
- Zero linter errors - code quality verified
- File structure: Core API → Image Utilities → Domain Formatters
- All code heavily commented to explain purpose, logic, and architecture decisions

[2025-10-25] - Single Service Detail Page Implementation (100% Complete)
- Created /services/[slug] dynamic route: src/app/services/[slug]/page.js as async Server Component
- Implemented comprehensive JSDoc and inline documentation explaining every aspect of the code
- Added detailed block comments for component purpose, architecture, data flow, SEO, and user flow
- Documented getServiceBySlug async function with full API details, data formatting, and error handling
- Function fetches single service by slug: /api/services?populate=image&filters[slug][$eq]={slug}
- Implemented ISR (Incremental Static Regeneration) with 3600 second (1 hour) revalidation
- Added extensive image formatting logic handling Strapi flat structure with fallback placeholder
- Extracts description from Strapi rich text structure: description[0].children[0].text
- Handles relative/absolute image URLs with proper prepending of STRAPI_API_URL
- Implemented generateStaticParams for SSG (Static Site Generation) at build time
- Function fetches all service slugs and generates pre-rendered pages for optimal performance
- Added generateMetadata for dynamic SEO with Persian title and description
- Fallback metadata for 404 cases: "خدمت یافت نشد"
- Main component uses Next.js 15 async params pattern (await params)
- Implements notFound() for proper 404 handling when service doesn't exist
- Added back button navigation to /services listing with arrow icon and hover effects
- Implemented two-column layout: Details (left in RTL) + Image (right in RTL)
- Details section includes: title (h1), description, and CTA button
- CTA button links to service.link (external page, contact form, or booking)
- External link detection: Opens in new tab with rel="noopener noreferrer" for security
- Internal links stay in same tab (better UX)
- Conditional rendering: CTA only shown if service.link exists
- Used Next.js Image component with priority loading for above-the-fold content
- Responsive sizes prop for image optimization: "(max-width: 768px) 100vw, 50vw"
- All code follows accessibility best practices with aria-labels and semantic HTML
- Created page.module.scss with extensive block and inline comments
- Documented every CSS class with multi-line comments explaining purpose, layout, and behavior
- Added detailed header explaining styling strategy, BEM methodology, and design system
- Implemented .servicePage container with responsive vertical padding (120/100px → 96/80px → 64/56px)
- Created .backButtonWrapper with spacing below (56px → 40px → 32px)
- Styled .backButton with transparent background, gold border, hover fill effect
- Button hover: Background fills with gold, text inverts to dark, subtle scale(1.02)
- Added focus states for keyboard navigation accessibility
- Implemented .contentGrid with CSS Grid: 2 columns (desktop) → 1 column (mobile/tablet)
- Grid gap: 48px (desktop) → 40px (md) → 32px (sm)
- Used align-items: center for vertical alignment of columns
- Mobile layout: Image moves to top using order: -1 (better UX)
- Created .details section with flexbox column layout and right text alignment (RTL)
- Styled .title with responsive typography: --font-xl (32px → 28px → 24px)
- Styled .description with generous line-height (1.8) and flex-grow for available space
- Typography scales: --font-lg (20px) → --font-md (16px) → --font-sm (14px)
- Implemented .ctaButton with gold background, dark text, prominent styling
- Button padding: 16px/48px (desktop) → 14px/40px (md) → 12px/32px (sm)
- Full-width button on mobile for easier touch target
- Hover effects: Lift up (translateY -2px), shadow, color change
- Active state: Scale down for tactile feedback
- Created .imageWrapper with 4:3 aspect ratio (desktop), 16:9 (mobile)
- Rounded corners: 16px → 12px → 8px responsive scaling
- Subtle border with low opacity for definition
- Background color during image loading for smooth experience
- All colors use CSS variables (--color-text-primary, --color-bg-primary, --color-card-text, --color-overlay)
- All spacing uses CSS variables (--space-section-top-desktop, --space-title-content-desktop, --space-gap-desktop, --space-title-text-desktop)
- All typography uses CSS variables (--font-xl, --font-lg, --font-md, --font-sm, --font-weight-bold, --font-weight-medium, --line-height-lg, --line-height-md)
- Imported mixins with relative path following project rules
- Full responsive design with @include respond() mixin for md and sm breakpoints
- Zero linter errors - code quality verified
- Followed all project architecture rules from CONTEXT_RULES.md and CONTEXT_SingleServicePage.md
- All code heavily commented to explain purpose and logic as requested

[2025-10-25] - Services Listing Page Implementation (100% Complete)
- Created /services page route: src/app/services/page.js as async Server Component
- Implemented comprehensive JSDoc and inline documentation explaining every aspect of the code
- Added detailed block comments for component purpose, architecture, data flow, and SEO strategy
- Documented getInitialServices async function with full API details and error handling
- Added formatStrapiServices utility function to strapiUtils.js with heavy commenting
- Function extracts id, slug, title, description (from rich text), image, and optional link
- Handles Strapi's nested rich text structure: description[0].children[0].text
- Uses formatSingleImage for consistent image URL formatting with fallbacks
- API endpoint: /api/services?populate=image&sort=createdAt:desc
- Implemented ISR (Incremental Static Regeneration) with 3600 second (1 hour) revalidation
- No pagination or "Load More" - fetches all services in single request per specifications
- Added metadata export for SEO: Persian title and description
- Implemented hero section with title "خدمات ما" and descriptive subtitle
- Created responsive services grid that renders ServiceCard components
- Implemented empty state handling with Persian message when no services available
- Empty state message: "در حال حاضر هیچ خدمتی ثبت نشده است."
- Conditional rendering: Grid display vs empty state based on services array
- Created services.module.scss with extensive block and inline comments
- Documented every CSS class with multi-line comments explaining purpose and strategy
- Added detailed header explaining styling strategy, BEM methodology, design system integration
- Implemented .main container with responsive vertical padding (120/100px → 96/80px → 64/56px)
- Created .hero section with centered layout and responsive bottom margin (56px → 48px → 40px)
- Styled .title with responsive typography: --font-xl (32px → 28px → 24px)
- Styled .subtitle with max-width 800px, responsive font sizes (--font-lg → md → sm)
- Implemented responsive CSS Grid layout for services
- Grid columns: 3 cols (desktop) → 3 cols (lg) → 2 cols (tablet) → 1 col (mobile)
- Grid gap: 48px (desktop) → 40px (xl) → 32px (lg) → 24px (md) → 16px (sm)
- Used align-items: stretch for equal card heights across rows
- Implemented .emptyState section with centered layout and responsive padding
- All colors use CSS variables (--color-title-hover, --color-card-text)
- All spacing uses CSS variables (--space-section-top-desktop, --space-title-content-desktop, --space-gap-desktop)
- All typography uses CSS variables (--font-xl, --font-lg, --font-md, --font-sm, --font-weight-bold, --line-height-lg)
- Imported mixins with relative path following project rules
- Full responsive design with @include respond() mixin for xl, lg, md, sm breakpoints
- Zero linter errors - code quality verified
- Followed all project architecture rules from CONTEXT_RULES.md and CONTEXT_ServicesListingPage.md
- All code heavily commented to explain purpose and logic as requested

[2025-10-25] - ServiceCard Component Heavy Documentation (100% Complete)
- Completely rewrote ServiceCard.jsx with comprehensive JSDoc documentation and inline comments
- Added detailed block comments explaining component purpose, architecture, and data flow
- Documented all props with @param JSDoc tags including types and descriptions
- Added inline comments for every major code block: guard clauses, destructuring, Link wrapper, Image section, content section
- Enhanced accessibility with aria-label for screen readers in Persian
- Updated image handling to support both object (image.url) and string formats
- Added priority={false} for lazy loading below-the-fold images
- Changed CTA from Link to span element (.ctaText) since card itself is wrapped in Link
- Completely rewrote ServiceCard.module.scss with extensive block and inline comments
- Added detailed header documentation explaining styling strategy, BEM methodology, and design system integration
- Documented every CSS class with multi-line block comments explaining purpose, layout strategy, and behavior
- Added inline comments for every CSS property explaining its purpose and values
- Documented hover states, focus states, and responsive breakpoints with clear explanations
- Explained flexbox layout strategy, aspect ratios, transitions, and accessibility features
- Enhanced styling with transparent dark background, subtle borders, and premium hover effects
- Implemented card lift animation (translateY -8px), image zoom (scale 1.08), and title color change on hover
- Added focus state for keyboard navigation accessibility
- Enhanced CTA text with color change and RTL-aware translateX movement on hover
- Implemented responsive typography: --font-lg → --font-md → --font-sm
- Implemented responsive spacing: 24px → 20px → 16px padding
- Added multi-line text truncation with -webkit-line-clamp: 3 lines (desktop) → 2 lines (mobile)
- All colors use CSS variables (--color-text-primary, --color-card-text, --color-title-hover)
- All spacing uses CSS variables (--space-title-text-desktop, --space-text-button-desktop)
- All typography uses CSS variables (--font-lg, --font-md, --font-sm, --font-weight-medium)
- Imported mixins with relative path following project rules
- Full responsive design with @include respond() mixin for md and sm breakpoints
- Zero linter errors - code quality verified
- Followed all project architecture rules from CONTEXT_RULES.md and CONTEXT_ServiceCard.md
- Component heavily commented to explain purpose and logic as requested

[2025-10-25] - All Card Components Styling Refactor (100% Complete)
- Refactored ProductCard.module.scss with BEM naming and design system
- Refactored ArticleCard.module.scss with BEM naming and design system
- Refactored CourseCard.module.scss with BEM naming and design system
- Refactored CategoryCard.module.scss with BEM naming and design system
- Refactored TestimonialCard.module.scss with BEM naming and design system
- All cards now use @include card-container mixin
- All cards use gradient backgrounds: var(--gradient-card-vertical)
- All cards have hover effect: var(--gradient-card-horizontal-rtl)
- Consistent hover effects: translateY(-4px), box-shadow, image scale(1.05)
- All cards use responsive aspect ratios: 16/10 → 4/3 → 16/9
- All cards have responsive padding and gap spacing
- All typography uses CSS variables (--font-lg, --font-md, --font-sm)
- All spacing uses CSS variables from styles.md
- All colors use CSS variables from styles.md
- Title hover color change to --color-title-hover on all cards
- Imported mixins using relative paths in all SCSS files
- Full responsive design with @include respond() across all breakpoints
- Zero hardcoded values - all styling from design tokens

[2025-10-25] - Services Page Implementation (100% Complete)
- Created page.js in src/app/services/ for services listing route
- Implemented getInitialServices async function for SSR data fetching
- API endpoint: /api/services?populate=image&sort=createdAt:desc
- Configured ISR with 3600 second revalidation
- Uses formatStrapiServices from strapiUtils for data formatting
- Added metadata for SEO: title and description in Persian
- Created hero section with title "خدمات ما" and descriptive subtitle
- Integrated ServiceGrid component with initialServices prop
- Created services.module.scss following BEM naming convention
- Imported mixins using relative path
- Section padding: 120px/100px → 96px/80px → 64px/56px responsive
- Hero section centered with bottom margin: 56px → 48px → 40px
- Title styling: --font-xl (32px → 28px → 24px), --font-weight-bold
- Subtitle styling: --font-lg (→ md → sm), max-width 800px, centered
- All spacing uses CSS variables from styles.md
- All typography uses CSS variables from styles.md
- All colors use CSS variables from styles.md
- Full responsive design with @include respond() mixin
- Followed CONTEXT_ServicePage.md specifications
- Zero hardcoded values

[2025-10-25] - ServiceSingle Component & Page Implementation (100% Complete)
- Created ServiceSingle.jsx server component in src/modules/services/ServiceSingle/
- Implemented detailed service view with image, title, description, and CTA link
- Added comprehensive JSDoc documentation explaining component structure and data flow
- Component receives formatted service data from parent page component
- Implemented responsive two-column layout: image right (RTL), content left on desktop
- Mobile layout stacks vertically: image top, content bottom using CSS Grid order property
- Enhanced CTA link with external link detection (opens in new tab with rel="noopener noreferrer")
- Created ServiceSingle.module.scss following strict BEM naming convention
- Imported mixins using relative path (@import '../../../styles/base/mixins')
- Used CSS Grid for responsive layout with automatic column adjustment
- Applied grid order property for RTL layout: content (order: 1), image (order: 2)
- Implemented responsive aspect ratios: 16/10 (desktop), 16/9 (tablet), 4/3 (mobile)
- Title styling: --font-xl (32px), --font-weight-bold, responsive down to 24px mobile
- Description styling: --font-md, --line-height-lg (1.8) for optimal readability
- CTA button: 2px border, 16px/48px padding, hover fill effect with scale transform
- Responsive spacing: padding reduces from 120px/100px to 64px/56px on mobile
- Gap between image and content: 48px (desktop) down to 24px (mobile)
- All spacing uses CSS variables (--space-section-top-desktop, --space-gap-desktop, --space-title-text-desktop)
- All typography uses CSS variables (--font-xl, --font-lg, --font-md, --font-sm, --font-weight-bold)
- All colors use CSS variables (--color-text-primary, --color-card-text, --color-bg-primary, --color-title-hover)
- Created page route: src/app/services/[slug]/page.js for dynamic service pages
- Implemented getServiceBySlug async function for server-side data fetching
- API endpoint: /api/services?populate=image&filters[slug][$eq]={slug}
- Configured ISR with 3600 second (1 hour) revalidation for optimal performance
- Handles image URL formatting: prepends STRAPI_API_URL for relative paths
- Extracts description text from Strapi rich text structure (array of blocks)
- Implements proper error handling with try/catch and null returns
- Added generateStaticParams for Static Site Generation (SSG) at build time
- Fetches all service slugs and generates static paths for pre-rendering
- Added generateMetadata function for dynamic SEO optimization
- Sets page title and description based on service data
- Fallback metadata for 404 cases: "خدمت یافت نشد"
- Main page component uses Next.js 15 async params pattern
- Implements notFound() for proper 404 handling when service doesn't exist
- Wraps ServiceSingle in .container class for consistent page width
- Added extensive inline comments explaining data fetching logic, formatting, and flow
- Followed all project architecture rules from CONTEXT_RULES.md and CONTEXT_ServiceSingle.md
- Zero hardcoded values - all styling references design tokens from styles.md
- Component and page follow Next.js App Router SSR/ISR best practices

[2025-10-25] - ServiceGrid Component Implementation (100% Complete)
- Created ServiceGrid.jsx client component in src/modules/services/ServiceGrid/
- Implemented responsive grid layout displaying ServiceCard components
- Grid columns adapt to viewport: 4 cols (desktop), 3 cols (tablet/lg), 2 cols (mobile)
- Added comprehensive JSDoc documentation explaining component purpose and props
- Implemented empty state handling with Persian message: "در حال حاضر هیچ خدمتی ثبت نشده است."
- Component receives initialServices prop from server-side data fetching
- Created ServiceGrid.module.scss following strict BEM naming convention
- Imported mixins using relative path (@import '../../../styles/base/mixins')
- Used CSS Grid with responsive gap spacing: 48px (desktop), 40px (xl), 32px (lg), 24px (md), 16px (sm)
- Applied justify-items: center for proper alignment when fewer items than columns
- Set align-items: stretch to ensure equal card heights across grid rows
- Implemented width control (width: 100%, min-width: 0) to prevent grid blowout
- Created empty state section with centered message and responsive padding
- All spacing uses CSS variables (--space-gap-desktop, --space-section-top-desktop)
- All typography uses CSS variables (--font-lg, --font-md, --font-weight-medium)
- All colors use CSS variables (--color-text-primary)
- Full responsive breakpoints using @include respond() mixin (xl, lg, md, sm)
- Added formatStrapiServices function to strapiUtils.js for data formatting
- Function extracts id, slug, title, description (from rich text), image, and link fields
- Uses formatSingleImage helper for consistent image URL formatting with fallback
- Filters out invalid items and maps Strapi response to clean service objects
- Added detailed inline comments explaining grid behavior, layout logic, and responsive strategy
- Component follows 'use client' pattern for future state management capabilities
- Followed all project architecture rules from CONTEXT_RULES.md and CONTEXT_ServiceGrid.md
- Zero hardcoded values - all styling references design tokens from styles.md

[2025-10-25] - ServiceCard Component Refactor (100% Complete)
- Refactored ServiceCard.jsx with comprehensive JSDoc documentation and inline comments
- Updated component to use proper BEM naming convention in JSX (serviceCard__imageWrapper, serviceCard__content, etc.)
- Enhanced error handling with optional chaining for image data and fallback placeholder
- Improved Image component with responsive sizes prop for better performance across devices
- Completely rewrote ServiceCard.module.scss following strict BEM methodology
- Imported mixins using relative path (@import '../../../styles/base/mixins')
- Applied card-container mixin for consistent card styling with padding override for full-width images
- Implemented gradient background with vertical gradient base and horizontal-rtl gradient on hover
- Added smooth hover effects: translateY(-4px), box-shadow, and background gradient transition
- Implemented image zoom effect (scale 1.05) on card hover using nested selector
- Added responsive aspect ratios: 16/10 (desktop), 4/3 (tablet), 16/9 (mobile)
- Title color transitions from --color-text-primary to --color-title-hover on hover
- Description uses -webkit-line-clamp for text truncation: 3 lines (desktop/mobile), 4 lines (tablet)
- Created styled CTA link button with border, hover background fill, and scale effect
- All spacing uses CSS variables (--space-title-text-desktop, --space-text-button-desktop)
- All typography uses CSS variables (--font-lg, --font-md, --font-sm, --font-weight-bold, --line-height-md)
- All colors use CSS variables (--color-text-primary, --color-card-text, --color-title-hover)
- Full responsive breakpoints with mobile-first approach using @include respond() mixin
- Added detailed SCSS comments explaining purpose of each section and style rule
- Component is server-only with no client-side interactivity (no 'use client' directive)
- Followed all project architecture rules from CONTEXT_RULES.md and CONTEXT_ServiceCard.md
- Zero hardcoded values - all styling references design tokens from styles.md

[2025-10-10] - Courses Listing Page & CourseGrid Component Implementation (100% Complete)
- Created courses listing page route: src/app/courses/page.js
- Implemented page.js as async server component with Next.js App Router patterns
- Added getInitialCourses function to fetch 6 courses from Strapi (PAGE_SIZE=6)
- Implemented formatStrapiCourses utility helper function in strapiUtils.js
- Function handles cover image formatting with fallback to placeholder
- Extracts price as toman object and shortDescription from rich text array
- Created CourseGrid client component: src/modules/courses/CourseGrid/CourseGrid.jsx
- Integrated CourseCard component for displaying course items
- Added SortControls component with three sorting options: جدیدترین, ارزان‌ترین, گران‌ترین
- Implemented getSortQuery helper function to map sort values to Strapi query parameters
- Sort options: 'latest' (createdAt:desc), 'price:asc', 'price:desc'
- Implemented sorting functionality with useRef to prevent initial mount fetch
- Added "Load More" pagination functionality for loading more courses
- Reused ArticleGrid.module.scss for consistent styling across listing pages
- Reused articles.module.scss for page-level styling
- Added metadata for SEO with Persian title and description
- Used CSS variables for all styling (colors, spacing, typography)
- Followed all project rules: server components, client directives, SCSS modules, no hardcoded styles

[2025-10-09] - SortControls Reusable Component Implementation (100% Complete)
- Created SortControls.jsx as a reusable client component in src/components/ui/SortControls/
- Implemented generic sorting button interface with options array, currentSort, and onSortChange props
- Added SortControls.module.scss with transparent button styling and active state
- Styled buttons with border, hover effects, and gold accent colors matching project theme
- Active button displays filled background with var(--color-text-primary)
- Used CSS variables for all styling (colors, spacing, transitions)
- Component accepts options array with value/label pairs for flexibility
- Followed all project rules: client component, SCSS modules, no hardcoded styles

[2025-10-09] - Articles Listing Page Implementation (100% Complete)
- Created dynamic articles listing route: src/app/articles/page.js
- Implemented page.js as async server component with Next.js App Router patterns
- Added getInitialArticles function to fetch 6 articles from Strapi (PAGE_SIZE=6)
- Implemented formatStrapiArticles utility helper function in strapiUtils.js
- Added formatStrapiImage helper function to handle Strapi media field formatting
- Created ArticleGrid client component: src/modules/articles/ArticleGrid/ArticleGrid.jsx
- Integrated ArticleCard component for displaying article items
- Added sorting functionality with two options: جدیدترین (latest) and قدیمی‌ترین (oldest)
- Implemented useEffect hook for handling sort changes without extra initial fetch
- Added "Load More" pagination functionality similar to ProductGrid
- Created ArticleGrid.module.scss with sortControls, grid, and loadMore styles
- Sort controls styled with transparent background and hover effects
- Implemented articles.module.scss reusing styles from products page
- Added 3-column responsive grid layout (3 → 2 → 1 columns on breakpoints)
- Added metadata for SEO with Persian title and description
- Used CSS variables for all styling (colors, spacing, typography)
- Followed all project rules: server components, SCSS modules, no hardcoded styles, heavy commenting

[2025-10-09] - Single Course Page Dynamic Route Implementation (100% Complete)
- Created dynamic route structure: src/app/courses/[slug]/
- Implemented page.js as async server component with Next.js App Router patterns
- Added getCourseBySlug function with ISR (revalidate: 3600 seconds / 1 hour)
- Implemented generateStaticParams for static path generation at build time
- Added generateMetadata for dynamic SEO with Persian fallback
- Integrated existing Accordion component for curriculum display
- Created two-column responsive grid layout (media + details)
- Used Next.js Image component with fill layout for course media
- Implemented page.module.scss with responsive breakpoints
- Added curriculum section with centered layout (max-width 800px)
- Added extensive inline comments explaining every function and code block
- Used CSS variables for all styling (colors, spacing, typography)
- Integrated notFound() for proper 404 handling
- Added price display with Persian number formatting and "رایگان" for free courses
- Implemented CTA button for course enrollment
- Followed all project rules: server components, SCSS modules, no hardcoded styles, heavy commenting

[2025-10-09] - Single Article Page Dynamic Route Implementation (100% Complete)
- Created dynamic route structure: src/app/articles/[slug]/
- Implemented page.js as async server component with Next.js App Router patterns
- Added getArticleBySlug function with ISR (revalidate: 3600 seconds / 1 hour)
- Implemented generateStaticParams for static path generation at build time
- Added generateMetadata for dynamic SEO with Persian fallback
- Integrated 'marked' library to convert markdown/rich text content to HTML
- Used Next.js Image component with priority loading for cover images
- Implemented dangerouslySetInnerHTML for safe CMS content rendering
- Created responsive single-column layout with centered content (max-width 800px)
- Implemented page.module.scss with cinematic 16:9 aspect ratio for cover images
- Added extensive inline comments explaining every function and code block
- Styled HTML elements (h2, h3, p, a, ul, ol) within article content
- Used CSS variables for all styling (colors, spacing, typography)
- Integrated notFound() for proper 404 handling
- Added Persian date formatting using fa-IR locale
- Followed all project rules: server components, SCSS modules, no hardcoded styles, heavy commenting

[2025-10-08] - Single Product Page Dynamic Route Implementation (100% Complete)
- Created dynamic route structure: src/app/products/[slug]/
- Implemented page.js as server component with Next.js App Router patterns
- Added getProductBySlug function with ISR (revalidate: 60 seconds)
- Implemented generateStaticParams for static path generation at build time
- Added generateMetadata for dynamic SEO with Persian fallback
- Integrated formatStrapiProducts utility for consistent data formatting
- Created two-column responsive grid layout (gallery + details)
- Used Next.js Image component for optimized image loading
- Implemented page.module.scss with responsive breakpoints
- Added mobile-first responsive design (stacks vertically on tablets/mobile)
- Used CSS variables for all styling (colors, spacing, typography)
- Integrated notFound() for proper 404 handling
- Followed all project rules: server components, SCSS modules, no hardcoded styles

[2025-10-07] - ProductGrid Component Refactor with Load More Pagination (100% Complete)
- Refactored ProductGrid.jsx to add "Load More" pagination functionality
- Added page state management with initial page set to 1
- Added hasMore state to track if more products are available
- Modified fetchProducts to accept isLoadMore parameter for append vs replace logic
- Implemented Strapi pagination query parameters (pagination[page] and pagination[pageSize]=20)
- Added handleLoadMore function to fetch next page and append products to existing list
- Integrated meta.pagination from Strapi response to determine if more pages exist
- Added loading indicator that displays during fetch operations
- Created loadMoreButton component that only shows when hasMore is true and not loading
- Updated ProductGrid.module.scss with loadingText, loadMoreContainer, and loadMoreButton styles
- Styled button with gold background and hover effects matching project theme
- Ensured sort functionality resets pagination to page 1
- Fixed useEffect dependency to only trigger on sortBy changes (not on page changes)
- Followed all project rules: modern syntax, SCSS modules, no hardcoded styles

[2025-10-05] - TestimonialsSection Component Implementation (100% Complete)
- Created TestimonialsSection.jsx with BaseSlider and TestimonialCard integration
- Implemented TestimonialsSection.module.scss with centered layout and decorative bird ornaments
- Configured BaseSlider with 1 slide per view and loop enabled
- Added Persian section title "نظرات"
- Positioned decorative bird images using absolute positioning on left and right sides
- Used mockTestimonials data from mock.js
- Implemented responsive design that hides ornaments on mobile
- Applied CSS variables for spacing and colors
- Followed all project rules: modern syntax, SCSS modules, no hardcoded styles

[2025-10-05] - TestimonialCard Component Implementation (100% Complete)
- Created TestimonialCard.jsx with header and text layout
- Implemented TestimonialCard.module.scss with semi-transparent background and border
- Added centered flexbox header with author name and user icon
- Used CSS variables for colors, fonts, and spacing
- Set max-width to 500px for optimal readability
- Applied centered text alignment and appropriate line height
- Followed all project rules: modern syntax, SCSS modules, no hardcoded styles

[2025-10-05] - Accordion Component Refactor with Framer Motion (100% Complete)
- Refactored Accordion.jsx to use framer-motion library for animations
- Replaced CSS transitions with motion.div and AnimatePresence for content animations
- Used motion.span for smooth icon rotation animation (0deg to 180deg)
- Implemented smooth height animation using variants (collapsed/open states)
- Added custom easing function [0.04, 0.62, 0.23, 0.98] for natural accordion feel
- Simplified Accordion.module.scss by removing max-height, transition properties
- Removed .iconActive and .contentActive classes (no longer needed)
- Moved padding from conditional classes to .contentInner for consistency
- Framer Motion now handles all animation logic (height, opacity, rotation)
- Maintained all accessibility features and responsive design
- Followed all project rules: modern syntax, SCSS modules, no hardcoded styles

[2025-10-05] - FaqSection Component Implementation (100% Complete)
- Created FaqSection.jsx as server component in modules/home/FaqSection
- Implemented FaqSection.module.scss with centered layout and section spacing
- Integrated Accordion component to display FAQ items
- Used mockFaqs data from mock.js (6 FAQ items)
- Added Persian section title "سوالات متداول" (Frequently Asked Questions)
- Centered title with responsive margin-bottom spacing
- Limited accordion wrapper to max-width 900px for better readability on large screens
- Applied responsive design with breakpoints for mobile, tablet, and desktop
- Used CSS variables for colors, fonts, and spacing
- Followed all project rules: modern syntax, SCSS modules, no hardcoded styles

[2025-10-05] - Accordion Component Implementation (100% Complete)
- Created Accordion.jsx as 'use client' component with state management
- Implemented Accordion.module.scss with smooth open/close animations
- Added support for array of items with id, title, and content properties
- Only one item can be expanded at a time (controlled accordion behavior)
- Included accessibility features with aria-expanded attribute
- Added icon rotation animation for visual feedback
- Styled with dark theme matching project design system (overlay background, gold text)
- Implemented responsive design with breakpoints for mobile, tablet, and desktop
- Used CSS variables for colors, fonts, and spacing
- Applied hover effects on accordion titles with color transition
- Followed all project rules: modern syntax, SCSS modules, no hardcoded styles

[2025-10-04] - ArticlesSection Component Implementation (100% Complete)
- Created ArticlesSection.jsx with BaseSlider and ArticleCard integration
- Implemented ArticlesSection.module.scss with minimal styling matching ProductsSection
- Added Persian section title "مقالات" and "مشاهده همه مقالات ..." link to /articles
- Configured BaseSlider with 3 slides per view and loop enabled as per specifications
- Used mockArticles data from mock.js for development
- Maintained visual consistency with other homepage sections (ProductsSection, CoursesSection)
- Ensured RTL support and Persian text integration
- Followed all project rules: modern syntax, SCSS modules, no hardcoded styles

[2025-10-04] - ServicesSection & ServiceCard Implementation (100% Complete)
- Created ServiceCard.jsx with image, title, description, and CTA button
- Implemented ServiceCard.module.scss identical to CourseCard styling for visual consistency
- Created ServicesSection.jsx with BaseSlider and ServiceCard integration
- Implemented ServicesSection.module.scss matching CoursesSection styling
- Added Persian section title "خدمات" and "مشاهده همه خدمات ..." link to /services
- Configured BaseSlider with 2 slides per view and loop enabled
- Used mockServices data from mock.js for development
- Maintained visual consistency across all homepage sections
- Ensured RTL support and Persian text integration
- Followed all project rules: modern syntax, SCSS modules, no hardcoded styles

[2025-10-04] - ProductCategoriesSection Component Implementation (100% Complete)
- Created ProductCategoriesSection.jsx with BaseSlider and CategoryCard integration
- Implemented ProductCategoriesSection.module.scss with minimal styling matching other homepage sections
- Added Persian section title "دسته بندی" and "مشاهده همه دسته بندی ها ..." link to /categories
- Configured BaseSlider with 6 slides per view on desktop as per specifications
- Used mockCategories data from mock.js for development
- Maintained visual consistency with CoursesSection and ProductsSection styling
- Ensured RTL support and Persian text integration
- Followed all project rules: modern syntax, SCSS modules, no hardcoded styles

[2025-10-04] - CategoryCard Component Implementation (100% Complete)
- Created CategoryCard.jsx with exact JSX structure from specifications
- Implemented CategoryCard.module.scss with transparent background and subtle gradient overlay on hover
- Added centered flexbox layout with column direction and gap between icon and name
- Used CSS variables for colors, spacing, and typography following design system
- Implemented hover effects with scale transform and box-shadow
- Added circular icon wrapper with background changes on hover
- Ensured focus accessibility with outline styles
- Followed all project rules: modern syntax, SCSS modules, no hardcoded styles

[2025-10-04] - HakimElahiSection Component Implementation (100% Complete)
- Created HakimElahiSection.jsx with responsive two-column grid layout
- Implemented HakimElahiSection.module.scss using CSS Grid with text left, image right
- Added responsive design that stacks vertically on screens ≤768px with image on top
- Used CSS variables from design system (_variables.scss) for colors and spacing
- Implemented image wrapper with Next.js Image optimization and proper sizing
- Added gradient CTA button with hover effects matching design system
- Ensured RTL support and Persian text integration
- Followed all project rules: modern syntax, SCSS modules, no hardcoded styles

[2025-09-30] - ArticleCard Component Implementation (100% Complete)
- Created ArticleCard.jsx with image, title, excerpt, and date display
- Implemented ArticleCard.module.scss following project card patterns
- Added 3-line excerpt truncation with ellipsis using -webkit-line-clamp
- Integrated Persian date formatting using fa-IR locale
- Links to /articles/{slug} route
- Used CSS variables for colors, spacing, and typography
- Maintained consistency with CourseCard and ProductCard styling
- Ensured RTL support and proper aspect ratio (16:10) for article images

[2025-09-30] - CoursesSection Module Implementation (100% Complete)
- Created CoursesSection.jsx with BaseSlider integration
- Implemented CoursesSection.module.scss with identical structure to ProductsSection
- Configured slider with 2 slides per view as per specifications
- Integrated CourseCard component for rendering course items
- Added Persian section title "آخرین دوره‌ها" and "مشاهده همه" link to /courses
- Used mockCourses data from mock.js
- Maintained visual consistency with ProductsSection styling
- Ensured RTL support and responsive design

[2025-09-30] - CourseCard Component Implementation (100% Complete)
- Created CourseCard.jsx with identical structure to ProductCard for visual consistency
- Implemented CourseCard.module.scss with same styling as ProductCard
- Added support for free courses (displays "رایگان" when price is 0)
- Implemented 2-line text truncation with ellipsis for shortDescription
- Added CTA button styled as div with "ثبت‌نام در دوره" text
- Links to /courses/{slug} route
- Ensured RTL support and Persian text integration
- Used global card classes (card, vertical-gradient, card-title, card-button)

[2025-09-30] - AboutMentorSection Component Implementation (100% Complete)
- Created AboutMentorSection.jsx with responsive two-column grid layout
- Implemented AboutMentorSection.module.scss using CSS Grid
- Added responsive design that stacks vertically on screens ≤768px
- Used CSS variables from design system (_variables.scss)
- Implemented image wrapper with Next.js Image optimization
- Added hover effects on CTA button with gradient background
- Ensured RTL support and Persian text integration

[2025-09-29] - ScrollCTAButton Client Component (100% Complete)
- Added src/components/ui/ScrollCTAButton/ScrollCTAButton.jsx as 'use client'
- Implemented smooth scroll to targetId using scrollIntoView with behavior 'smooth'
- Accepts className for external styling; no SCSS file created

[2025-09-29] - HeroSection Component Implementation (100% Complete)
- Created HeroSection.jsx with full-width, full-height hero layout
- Implemented HeroSection.module.scss using only provided CSS variables
- Added responsive design with mobile breakpoints
- Integrated Next.js Image optimization with background cover
- Added overlay effect and RTL Persian text support
- Implemented CTA button with hover animations

[2025-09-27] Footer Component Implementation (90% Complete)
- Created Footer.jsx with RTL support and responsive grid layout
- Implemented Footer.module.scss with dark theme and gold accents
- Added newsletter subscription form
- Added responsive breakpoints for tablet and mobile
TODO: Replace social media icon placeholders 

[2025-09-27] - Navbar Component Creation (100% Complete)
- Created Navbar.jsx as a client component with mobile-first approach
- Implemented responsive navigation with mobile drawer
- Added scroll-based header styling
- Integrated action icons with SVG icons
- Added RTL support and Persian text
- Implemented mobile menu animations and overlay 

[2025-09-27] - ProductCard Component Fix (100% Complete)
- Split interactive button into separate client component (AddToCartButton)
- Fixed runtime error with event handlers in server component
- Maintained all existing functionality and styling 