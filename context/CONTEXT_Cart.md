# CONTEXT_Cart.md

## 🎯 Purpose
مدیریت سبد خرید شامل محصولات و دوره‌ها با قابلیت ذخیره‌سازی در LocalStorage.

## ⚙️ Component Type
`use client` (Zustand Store)

## 🌐 Data Structure (CartItem)
- id: string
- slug: string
- title: string
- price: number
- image: string
- quantity: number
- type: 'product' | 'course' (برای جداسازی در UI)

## 🧠 State Logic (Zustand)
1. **Actions:** - `addItem`: اگر محصول بود تعداد زیاد شود، اگر دوره بود فقط یکبار اضافه شود.
   - `removeItem`: حذف کامل آیتم.
   - `updateQuantity`: افزایش/کاهش تعداد (فقط برای محصولات).
   - `clearCart`: پاکسازی کامل.
2. **Computed States:**
   - `totalPrice`: مجموع قیمت محصولات و دوره‌ها.
   - `itemsCount`: تعداد کل آیتم‌ها (برای نمایش روی آیکون Cart).

## 🎨 UI/UX Rules
- **Navbar Integration:** آیکون سبد خرید در صورت وجود آیتم، تعداد را نشان می‌دهد. در Hover، منوی کوچک (Dropdown) مشابه پروفایل باز می‌شود.
- **Cart Page:** نمایش دو بخش مجزا: محصولات در بالا، دوره‌ها در پایین (اگر هر دو موجود باشند).
- **Cards:** در `ProductCard` و `CourseCard` دکمه "افزودن" به کنترلر تعداد (+ / -) تبدیل می‌شود.