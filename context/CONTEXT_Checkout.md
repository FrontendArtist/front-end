# CONTEXT_Checkout.md

## 🎯 Purpose
مدیریت فرآیند نهایی خرید، انتخاب آدرس و شبیه‌سازی پرداخت (Mock Payment).

## ⚙️ Component Type
`use client` (Form handling, Auth check, Store access)

## 📂 Page Structure
1.  `/checkout` (صفحه اصلی تسویه حساب)
2.  `/payment/callback` (صفحه نتیجه پرداخت - شبیه‌سازی شده)

## 🧠 Logic Flow
1.  **Auth Guard:** اگر کاربر لاگین نیست، ریدارکت به لاگین.
2.  **Address Section:**
    - نمایش آدرس پیش‌فرض کاربر (از پروفایل).
    - دکمه "تغییر یا افزودن آدرس" (لینک به پروفایل).
    - اگر آدرس نداشت، دکمه غیرفعال شود و اخطار دهد.
3.  **Payment Simulation:**
    - دکمه "پرداخت و تکمیل خرید".
    - با کلیک، یک `setTimeout` (2 ثانیه) اجرا شود.
    - سپس ریدارکت به `/payment/callback?status=success&refId=MOCK_12345`.

## 🎨 UI/UX
- نمایش خلاصه فاکتور (تعداد اقلام، قیمت کل) در یک کارت چسبان (Sticky).
- استفاده از Loading State روی دکمه پرداخت هنگام شبیه‌سازی.