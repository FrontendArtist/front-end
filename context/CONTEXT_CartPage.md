# CONTEXT_CartPage.md

## 🎯 Purpose
نمایش لیست کامل اقلام سبد خرید با قابلیت مدیریت تعداد و مشاهده فاکتور نهایی.

## ⚙️ Component Type
`use client` (نیازمند دسترسی به Store و LocalStorage)

## 📐 Layout Structure
یک طراحی دو ستونه (Grid):
1. **Main Column (Left/Right depending on RTL):**
   - لیست محصولات (بالا)
   - لیست دوره‌ها (پایین)
   - هر سطر محصول شامل: عکس، عنوان، قیمت، کنترلر تعداد، دکمه حذف.
   - هر سطر دوره شامل: عکس، عنوان، قیمت، دکمه حذف (بدون کنترلر تعداد).
2. **Sidebar (Summary):**
   - تعداد کل اقلام.
   - مجموع قیمت (Total Price).
   - دکمه "ادامه جهت تسویه حساب" (Proceed to Checkout).

## 🧠 State & Logic
1. **Hydration Check:** استفاده از `useEffect` و `useState(false)` برای جلوگیری از ارور عدم تطابق سرور و کلاینت.
2. **Empty State:** اگر سبد خالی بود، یک تصویر گرافیکی یا آیکون بزرگ با دکمه "بازگشت به فروشگاه" نمایش داده شود.
3. **Data Splitting:** فیلتر کردن `items` به دو آرایه `products` و `courses` برای نمایش تفکیک شده.

## 🎨 Styles
- استفاده از گلس‌مورفیسم برای Sidebar.
- ریسپانسیو کامل (در موبایل، سایدبار به پایین منتقل شود و دکمه پرداخت چسبان (Sticky) شود).