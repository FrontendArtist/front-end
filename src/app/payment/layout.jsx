export const metadata = {
    title: 'نتیجه پرداخت',
    robots: {
        index: false,
        follow: false,
    },
};

export default function PaymentLayout({ children }) {
    return children;
}
