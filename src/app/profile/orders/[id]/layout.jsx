export const metadata = {
    title: 'جزئیات سفارش',
    robots: {
        index: false,
        follow: false,
    },
};

export default function OrderDetailLayout({ children }) {
    return children;
}
